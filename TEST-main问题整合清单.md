# TEST-main 量化平台 · 问题整合清单

> 整合截至 2026-08-12 的全部问题核查结论：早期「功能 P1-P10」+ 今日两轮 ML 核查（6 项 + 本轮 4 项）。
> 已对跨报告重复项去重合并（例如 功能P8=时间段错配、P10=报告空/仓位图，与 ML 轮为同一问题）。
> 状态图例：`[待修复]` 未处理 ｜ `[部分修复]` 后端已支持但前端/链路不全 ｜ `[已落地]` 代码已实现 ｜ `[需求]` 新增功能诉求
> 核查方式：静态代码核查（backend/app/ml.py、routers/ml.py、routers/reports.py、reporting.py、scheduler.py、adapters.py、frontend/src/views/*.vue、utils/format.js）。本轮及今日上午的 ML 项为**当日新鲜核查**；功能 P 系列为更早核查、标「待复核」。

---

## 模块一 · ML 机器学习

### M1 历史长度与时间段窗口错配（高优先 P0）
- 现象：训练/回测设较大「历史长度」（如 1400 天）并指定历史时间段后，报"有效样本不足"或直接空结果；用户预期"为所选时间段提供 N 天数据"，实际两端对不上。
- 定位：`ml.py:1107` 回测抓 `kline(code, hist+n+25)` 结束于**今天**；`ml.py:199` 训练同理；`ml.py:1194-1198 / 254-266` 用 start/end 过滤调仓日/样本；回测 `hist` 默认仅 180（`routers/ml.py:319`）。前端提示"留空=用最近 hist 天"（`MLView.vue:439`）印证 hist 锚定今天。
- 影响：无法做"过去某段历史区间"的回测/训练，只能做"最近 N 天"。
- 修复：传时间段时让 kline 抓"结束于 end_date、长度覆盖 hist"的窗口；前端联动回填建议 hist；做越界校验并明确报错。
- 状态：`[待修复]`

### M2 回测/训练报告空（M1 的下游）
- 现象：导出 HTML/Excel/PDF 报告几乎无内容。
- 定位：报告接线正确——`backtest_model` 返回 `longShort/groupSummary/icSeries/positionLedger/metrics`（`ml.py:1381-1391`），`reporting.payload_from_result` 与 `/reports/backtest`、前端 `MLView.vue:316-327` 字段对齐。空报告是 M1 导致上游产出空数组的必然结果。
- 状态：`[待修复]`（随 M1 修复而解）

### M3 持仓变化图恒隐藏 + 无仓位变化过程
- 现象：回测报告与前端只有多空曲线，看不到每期多头/空头持仓数量与换股。
- 定位：`reporting.py` 中 `chartPos` 默认 `display:none`，仅当 `positionLedger` 非空才显示；`backtest_model` 的 `positionLedger` 多数路径为空，前端未渲染。
- 状态：`[待修复]`

### M4 调参初始值非恒等 1.0（取特征重要性）
- 现象：人工调参面板初始权重直接取模型特征重要性，未改即"保存"会扭曲原模型预测。
- 状态：`[待修复]`

### M5 另存为新模型失效（权重不自动应用）
- 现象：「调参→另存为新模型」生成的新模型，在后续打分/回测中权重并未生效，等于原模型。
- 定位：`clone_model_with_adjust` 仅写侧车 JSON，但 `score_latest/backtest_model` 只在外部显式传 `adjust` 时才应用权重，不自动读取模型自身附带的调整权重。
- 状态：`[待修复]`

### M6 平仓/调仓个股信号不可见
- 现象：ML 回测为组合多空分层再平衡，前端只画曲线，不展示个股级平仓/调仓信号。
- 状态：`[待修复]`

### M7 人造/手动模型
- 现象（历史）：不支持手动加权模型进入回测。
- 现状：后端 `create_manual_model` 已落地（`routers/ml.py` manual），前端入口待复核是否完整。
- 状态：`[已落地，待复核前端]`

---

## 模块二 · 行情与数据接入

### D1 北交所单只导入失败
- 现象：市场行情/选股无法导入北交所股票（8xxxxx / 920xxx）。
- 定位：`frontend/src/utils/format.js` 的 `normalizeCode` 只接受 sh/sz+6 位数字，拒绝 8xxxxx/920xxx。
- 状态：`[待修复]`

### D2 期货等数据未接入模型计算
- 现象：期货行情不能加入 ML 模型计算（仅候选池走 `FUTURE_UNIVERSE`，快照/因子未接）。
- 状态：`[待修复]`

### D3 因子无法手动添加
- 现象：因子集固定，不支持用户新增自定义因子。
- 状态：`[待修复]`

### D4 自定义日期时间段无前端控件
- 现象：后端支持 `startDate/endDate`，但选股/回测页面无日期选择控件，用户无法自定义时间段。
- 定位：`BacktestView.vue` / `SelectView.vue` 无日期控件（ML 页有，但行为见 M1）。
- 状态：`[待修复]`

---

## 模块三 · 选股与回测

### S1 行业板块回测/选股缺失
- 现象：不能选"生物制药"这类行业板块做回测/选股。
- 定位：`adapters.BOARD_MATCHERS` 仅交易所/指数板块，无行业分类；`fetch_sector_map` 仅用于中性化。
- 状态：`[待修复]`

### S2 生成回测报告功能（结构化导出/可视化）
- 现象（需求）：希望一键生成可读回测报告。
- 现状：后端已有 HTML（含 3 张 ECharts 图）/ Excel / PDF 导出，但 Excel/PDF 无图，且依赖上游有数据（见 M2）。
- 状态：`[部分修复 / 需求]`

---

## 模块四 · 盯盘调度

### W1 盯盘只能盯自选股票
- 现象：无论规则模式还是模型模式，监控对象始终是 watchlist，无法监控板块/全市场/模型 TopN。
- 定位：`scheduler.py:209` 永远从 `watchlist` 表取标的；模型模式也只对 watchlist 调用 `score_codes(modelId, codes)`，`monitor_board` 仅用于全市场分位分布，不扩展标的池。
- 影响：想"盯某行业板块异动"或"模型全市场打分后盯 TopN"都做不到。
- 修复：盯盘配置增加"标的来源"（watchlist/板块/全市场/模型TopN），按来源动态取池。
- 状态：`[部分修复]`（模型模式后端已支持，但标的池仍限自选股）

---

## 修复优先级建议
- **P0**：M1（hist 与时间段窗口语义）——一处修复连锁解 M1/M2，并消除与 S2、D4 的耦合。
- **P1**：W1（盯盘标的池扩展）、M5（克隆模型权重自动应用）、S1（行业板块）。
- **P2**：M3（持仓变化图）、D1（北交所导入）、M4/M6（调参初始值、个股信号）。
- **P3**：D2/D3（期货接入、因子自定义）。

## 备注
- 今日上午的 ML 核查还标注过 8 项额外发现（编号 O1-O8），其详细报告文件因沙箱清理已丢失；如需，可择机对 `ml.py`/`routers/ml.py`/`reporting.py` 重做一轮扫描补全。
- 本清单为诊断文档，未改动任何代码（延续"只告诉问题"约定）。
