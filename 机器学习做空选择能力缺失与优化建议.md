# 机器学习模块「做空选择能力」缺失与优化建议

> 投诉：机器学习也没办法选是否空头（不只分层回测，整条 ML 链路都无法选择做空）。
> 全部结论基于当前代码逐行复核（非凭记忆）。
> 核心代码位置：
> - 打分/选股：`backend/app/ml.py`  `score_codes`（:1441）/ `score_latest`（:954）
> - 人造模型：`backend/app/ml.py`  `create_manual_model`（:1720）
> - 分层回测：`backend/app/ml.py`  `backtest_model`（:1044，多空双腿 :1301）
> - 盯盘模型模式：`backend/app/scheduler.py`  信号 :336-341；自动调仓 :524-641
> - 前端：`frontend/src/views/MLView.vue`（打分/人造模型/回测）；`MonitorView.vue`（盯盘）
> 
> 约定：本建议原为纯诊断，现已按 P0 全部 + P1 盯盘空单联动落地实现（见第 6 节「实施记录」）。

---

## 0. 结论（一句话）
**整个机器学习模块没有任何"是否做空 / 交易方向"的开关**——`routers/ml.py` 零相关参数；`ml.py` 的打分、人造模型、回测均无 `direction`/`short`/`allowShort` 概念；前端 `MLView.vue` 只有"多空累计"这类**展示标签**，没有任何方向控件（已用 grep 全量验证：`routers/ml.py` 0 匹配、`ml.py` 仅 `backtest_model` 内部硬编码双腿、`MLView.vue` 无 short/direction 控件）。

---

## 1. 四处缺做空选择的现状（逐项实证）

### 1.1 打分 / 选股（`score_codes` / `score_latest`）
- `score_codes`（:1441）返回 `[{code, score}]`，仅按模型预测分排序，**不区分多头候选 / 空头候选**，也无 `allowShort` 入参。
- `score_latest`（:954）同理，只是对板块/自选股批量打分。
- **后果**：用户无法说"给我应做空的票"；"做多还是做空"完全由下游回测硬编码决定，打分这层根本不暴露方向。

### 1.2 人造模型（`create_manual_model`）
- 签名 `create_manual_model(name, weights, threshold=None, ...)`（:1720），**无 `direction` 字段**。
- 手搓模型保存后，被回测强制塞进"多空双腿"逻辑，用户无法声明"这个模型只用于做多/只用于做空"。

### 1.3 分层回测（`backtest_model`）—— 已在《分层回测优化建议.md》问题 A 详述
- 方向硬编码 `ls_ret = top_ret - bottom_ret`（:1301），无 `direction` 参数、前端无控件。
- 本报告建议的 `direction` 入参（long_short/long_only/short_only）正是解这里的。

### 1.4 盯盘模型模式 —— 已在《盯盘调度一键买入优化建议.md》详述
- 模型打分后按分位出 看多/看空/中性（scheduler.py:336-341），但 `看空` 对非持仓股**从不变真实空单**（自动调仓只匹配平仓/减仓，:538-544），做空从未实现。
- 本报告"统一方向"设计需此处联动：当模型/监控配置为 `allowShort` 时，看空信号应生成真实空单（融券方案）。

---

## 2. 统一优化设计：模块级「方向 / 做空」概念

核心思路：**把"是否做空、交易方向"提升为 ML 模块的一等公民**，从模型元数据贯穿到打分、回测、盯盘，而不是各链路各自硬编码。

### 2.1 模型元数据增加方向字段（P0）
在模型 sidecar JSON（`import_model_file` 落盘的元数据，:1516）与 `create_manual_model` 增加：
```json
{
  "direction": "long_short",   // long_short | long_only | short_only
  "allowShort": true           // 是否允许做空（影响打分/回测/盯盘是否产出空头腿）
}
```
`score_latest` / `backtest_model` / `score_codes`（盯盘复用）默认读模型 `direction`，同时允许调用方显式覆盖（参数优先级 > 模型默认）。

### 2.2 打分输出多头 / 空头清单（P0）
`score_codes` / `score_latest` 返回结构升级：
```json
{
  "longList":  [{"code":"sh600519","score":0.92}, ...],   // 高分=做多候选
  "shortList": [{"code":"sz000001","score":0.08}, ...],   // 低分=做空候选（allowShort 时才有）
  "scores":    [{"code":..., "score":...}]               // 兼容旧调用
}
```
前端打分结果页据此渲染"做多候选 / 做空候选"两个分组，用户第一次能"选是否空头"。

### 2.3 回测透传方向（P0，引用《分层回测优化建议.md》问题 A）
`backtest_model` 接收 `direction`：long_only 只做多腿、short_only 只做空腿、long_short 双腿（现状）。模型 `direction` 作为默认值，UI 可覆盖。

### 2.4 盯盘看空→真实空单（P1，引用《盯盘调度一键买入优化建议.md》做空方案）
当模型/监控 `allowShort=true` 且信号为看空：对非持仓股生成**融券空单**（或至少单列"做空候选"不丢弃）。与 2.1 的 `allowShort` 联动，避免"看空信号被静默丢弃"。

### 2.5 人造模型加方向字段（P0，配合 2.1）
`create_manual_model` 增加 `direction`/`allowShort`；前端"新建人造模型"卡片（MLView.vue:499 区）增加方向下拉，保存即写入 sidecar。

### 2.6 前端统一方向控件（P0）
在四处复用同一方向控件（`<select>`：多空对冲 / 仅做多 / 仅做空）：
- 人造模型创建（MLView.vue:499 附近）
- ML 打分（runScore 入口，:229）
- ML 回测（runMLBacktest，:235，与《分层回测优化建议.md》A3 合并）
- 盯盘模型模式（MonitorView.vue 信号来源旁，配合做空开关）

---

## 3. 改造草图（核心片段）

### 3.1 模型 sidecar schema + create_manual_model
```python
def create_manual_model(name, weights, threshold=None,
                        direction="long_short", allow_short=True, **kw):
    meta = {"name": name, "weights": weights, "threshold": threshold,
            "direction": direction, "allowShort": allow_short, "manual": True}
    # 落盘 ml_models/<id>.json sidecar
    ...
```

### 3.2 打分返回 long/short 清单
```python
def score_codes(mid, codes, adjust=None, direction=None):
    bundle = _load_model(mid)
    direction = direction or bundle["meta"].get("direction", "long_short")
    allow_short = bundle["meta"].get("allowShort", True) and direction != "long_only"
    preds = ...  # 原逻辑
    ranked = sorted(zip(codes, preds), key=lambda x: x[1], reverse=True)
    long_list  = [{"code": c, "score": p} for c, p in ranked[:max(1,len(ranked)//3)]]
    short_list = [{"code": c, "score": p} for c, p in ranked[-max(1,len(ranked)//3):]] if allow_short else []
    return {"longList": long_list, "shortList": short_list,
            "scores": [{"code": c, "score": p} for c, p in ranked]}
```

### 3.3 前端方向控件（接 2.6）
```html
<select v-model="modelDirection">
  <option value="long_short">多空对冲</option>
  <option value="long_only">仅做多</option>
  <option value="short_only">仅做空</option>
</select>
```

---

## 4. 优先级与验收

| 优先级 | 项 | 验收标准 |
|---|---|---|
| P0 | 2.1 模型元数据 direction/allowShort | 模型文件含方向字段，落盘正确 |
| P0 | 2.2 打分 long/short 清单 | 打分结果页出现"做多候选/做空候选"两组 |
| P0 | 2.3 回测透传方向 | 与《分层回测优化建议.md》A 系列合并验收 |
| P0 | 2.5 人造模型方向 | 创建人造模型可指定方向并生效 |
| P0 | 2.6 前端统一控件 | 四处方向下拉一致、调用正确 |
| P1 | 2.4 盯盘看空→真实空单 | allowShort 时看空生成空单（引用盯盘文档做空方案） |

**实施顺序建议**：P0 的五项可一次串起来（元数据 → 打分 → 回测 → 人造 → 前端），把"方向"作为贯穿字段；P1 的盯盘空单与《盯盘调度一键买入优化建议.md》的做空方案合并实施，避免重复。

---

## 6. 实施记录（全部落地）

| 项 | 状态 | 改动位置 |
|---|---|---|
| 2.1 模型元数据 direction/allowShort | ✅ | `ml.py` `train_final_model`/`import_model_file`/`create_manual_model` 落盘 sidecar；`load_model_meta`/`list_models` 透出字段 |
| 2.2 打分 long/short 清单 | ✅ | `ml.py` 新增 `split_directional_scores` + `model_direction`；`routers/ml.py` `/score` 返回 `{scores,longList,shortList,direction,allowShort}` |
| 2.3 回测透传方向 | ✅ | 上一轮已实现（`backtest_model` `direction` 参数 + 前端下拉）；本轮补充模型方向作为回测默认值 |
| 2.4 盯盘看空→空单联动 | ✅ | `scheduler.py` `_enrich_signal`/`enrich_signals` 增 `allow_short` 门控；模型 `long_only` 或 `allowShort=False` 时看空信号 `action=none`，不再产出空单（空单执行链路此前已由盯盘文档落地） |
| 2.5 人造模型方向 | ✅ | `create_manual_model` 增 `direction/allowShort`；`ManualModelBody` 同步；前端人造模型卡加方向下拉 + 允许做空勾选 |
| 2.6 前端统一控件 | ✅ | 回测方向下拉（上轮）、人造模型方向下拉、打分结果分「做多候选/做空候选」两组；盯盘 `tradeDirections` 控件已有 |

### 关键实现细节
- 模型 sidecar JSON 新增 `direction ∈ {long_short, long_only, short_only}` 与 `allowShort: bool`；旧模型无字段时 `model_direction()` 回退默认 `long_short/True`。
- `split_directional_scores(rows, direction, allow_short)`：`long_only` 只出 longList、`short_only` 只出 shortList、`long_short` 两侧都出（各取约 1/3 头部/尾部）。
- 盯盘门控只关「信号产出动作」，真正下单仍受既有 `tradeDirections`（long/short/both）与 `auto_trade` 全局开关约束，双重保险。
- `/ml/score` 返回结构由纯数组升级为 `{scores,longList,shortList,direction,allowShort}`；前端 `runScore` 兼容 `res.scores ?? res`。

**验收**：模型含 direction/allowShort 且落盘正确；打分结果页出现「做多候选/做空候选」两组（仅做空模型只出做空组）；创建人造模型可指定方向并生效；盯盘模型 long_only 时看空信号不产生空单动作。

---

## 5. 与既有文档的关系（避免重复劳动）
- **《分层回测优化建议.md》问题 A**：解决"回测不可选方向"——本报告 2.3 直接引用其 `direction` 参数设计，不重复。
- **《盯盘调度一键买入优化建议.md》**：解决"盯盘看空不变空单 / 无多空筛选"——本报告 2.4 引用的做空方案即出自此处。
- **《机器学习模块优化设计方案.md》**：ML 模块整体路线图（U1-U3/M5/M6/M8）；本报告补齐其中未覆盖的"方向/做空选择"这一独立缺口。
- 三者 + 本报告共同构成 ML 模块完整性优化的完整拼图：数据窗口(U1) → 方向选择(本报告) → 信号/规则(M6) → 报告(U2)。
