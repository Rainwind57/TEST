# 优化建议：盯盘批量买入与资金分配（避免"只买到几只"）

> 针对 08-17 诊断发现的"盯盘按分配策略买入只能成交几只"问题，给出优化方向。
> 文档只描述现象与改进方向，不贴完整代码；仅保留少量定位线索。未改动任何业务代码。

---

## 一、现状与问题回顾（现象）

盯盘的一键 / 批量买入由**统一分配引擎** `plan_allocations` 计算，受三个参数控制（盯盘配置可改）：

- `allocMode`：equal 等权 / fixed 固定金额 / risk 风险预算（当前 risk 仅为 equal 近似）
- `perPositionPct`（单标仓位上限）：默认 `0.2`，即每只最多占现金 20%
- `maxPositions`（最大持仓数）：默认 `5`

观察到的现象：

1. **硬截断**：候选列表被 `codes[:maxPositions]` 一刀砍到最多 5 只，无论用户选了多少只。
2. **预算上限压死单标**：单标预算 = 现金 × perPositionPct（默认仅 20%），再整手取整（`qty = int(预算/价) // 100 * 100`）。当这 20% 买不起一手（100 股）时 `qty=0`，该只被直接跳过且预算**不回流**。
3. **现金逐单递减**：equal 模式按 `min(预算, 剩余现金/剩余数)` 迭代，但"预算上限"把每只压在 20%，前面几只吃满后，后面标的分到的预算更小、更买不起一手，最终成交往往只剩几只。
4. **跳过不透明**：无行情（价≤0）、或成交时金额+佣金超过剩余现金、或 `qty<100` 的标的都被静默跳过，用户看不到原因。

**结果**：用户选了十几只，实际只成交几只，且不清楚为什么。

---

## 二、优化方向（按落地优先级）

### P0（核心）：剩余资金"尽力分配"，避免只买几只

目标：根据**剩余资金**动态分配仓口，让现金尽量用满、能买几只就买几只，而不是卡在固定 20% 上限上。

- **方向 A（推荐）——预算回流 + 余数下滚**：
  - 任一只因资金不足被跳过时，把它的预算并回剩余现金，重新均分给剩余标的；
  - 整手取整产生的"零头"（不足 100 股的余钱）不丢弃，累计到下一只继续分配；
  - 直到现金不足以再买一手或候选耗尽为止。
- **方向 B——以"目标持仓数"反推单标上限**：
  - 将 `maxPositions` 语义明确为"希望买几只"，单标预算 = `剩余现金 / 剩余标的数`；
  - `perPositionPct` 退化为"可选的单标上限保护"（不超过该比例即可），不再作为死上限；
  - 现金充足时自然铺开到 N 只，现金有限时按剩余资金尽量多买。

**预期效果**：批量买入时按剩余资金把仓口铺开到尽可能多的标的，不再因固定上限 + 整手取整导致只成交几只。

### P0：maxPositions 默认值与语义修正

- 现状：`maxPositions` 默认 5，并作为"单次买入只数"硬截断。
- 方向：默认提高到更合理值（如 20）；若其本意是"总持仓上限"，应与"本次买入只数"区分，避免把用户想批量买入的候选一刀砍到 5。

### P1：perPositionPct 默认值与模式

- 默认 0.2 偏保守，想分散买多只时应更小。建议默认 0.1，或提供"分散 / 集中"预设：
  - 分散 = 现金 ÷ 目标持仓数；
  - 集中 = 较高单标占比。

### P1：预览回显"被跳过原因"

- `alloc-preview` 已能预览每只计划股数 / 金额 / 占比。
- 方向：对 `plannedQty=0` 的标的显式标注原因（无行情 / 资金不足 / 不足一手），让用户一眼看懂"为什么只买到了几个"。

### P2（顺带）：risk 模式真实化

- 当前 `risk` 仅作 equal 近似，可后续实现按波动率 / 风险预算的真实分配（不影响 P0 目标）。

---

## 三、预期效果

- 批量买入时，系统按**剩余资金**把仓口铺开到尽可能多的标的，不再因固定 20% 上限 + 整手取整而只成交几只。
- 用户可直观看到每只计划仓位与未成交原因，决策更透明。

---

## 四、已确认无需改动（保留）

- **整手取整 `//100*100`**：符合 A 股一手 100 股规则，不需去掉，只需让"余数下滚"（见 P0 方向 A）。
- **现金不足校验**（金额+佣金 > 剩余现金则跳过）：必要的风控，保留。
- **无行情跳过**：行情缺失时不应下单，保留，但应在预览中标注原因。

---

## 五、建议落地顺序

1. **P0 × 2**：剩余资金尽力分配（预算回流/余数下滚 或 目标持仓数反推）+ maxPositions 默认值/语义修正 —— 直接解决"只买到几只"。
2. **P1 × 2**：perPositionPct 默认与模式、预览回显跳过原因 —— 提升可用性与透明度。
3. **P2 × 1**：risk 模式真实化。
