# 应用优化建议：机器学习时间长度过长跑不出结果

> 诊断日期：2026-08-18
> 关联排查文档：`应用排查_机器学习时间长度过长_20260818.md`
> 性质：优化建议（方案级）。**2026-08-18 已部分落地，见「落地状态」一节。**

## 落地状态（2026-08-18 更新）

| 建议项 | 状态 | 实现 |
|--------|------|------|
| P0 失败计数 + 分类回显 | ✅ 已落地 | `ml.py::_classify_kline_error` + `_kline_retry_classified` / `_kline_window_retry_classified`；`build_dataset` 统计「超时/限流/无数据」三类并在报错中回显 |
| P0 重试退避 | ✅ 已落地（含训练/评估路径） | `build_dataset` 非窗口路径改走 `_kline_retry_classified`（原为裸 `kline_fn` 无重试） |
| P0 缓存优先 | ✅ 已有 | `adapters.fetch_kline` 三级缓存（内存→SQLite→网络） |
| P1 候选池×hist 上限 | ✅ 已落地（hist 天花板） | `_MAX_HIST_CEILING=4000`，自动抬升超限即报错（`build_dataset` + `backtest_model`） |
| P1 trial 可配置 | ✅ 已有 | `nTrials` 请求字段（默认 30） |
| P1 寻优早停 | ⚠️ 不适用 | 目标为单次 OOS Sharpe，无中间步骤可 report，Optuna pruner 无法套用；未实施 |
| P1 ML 回测丢线程池 | ✅ 已落地 | `backtest_model` 逐调仓日打分循环移入嵌套 `_run_loop()`，经 `run_in_executor` 执行 |
| P2 自动抬升提示 | ✅ 已有 | 前端 `suggestedHist`/`histInsufficient` 提示 + 后端 `histWarning` 回显 |
| P2 自动抬升天花板 | ✅ 已落地 | 同 P1 天花板，超限明确报错要求缩短时间段 |
| P2 前端成本预估 | ✅ 已落地 | `MLView.vue::estimatedSamples`，训练/评估按钮旁显示「预计样本量 ≈ hist × poolSize」 |

## 1. 问题回顾（简述）

"时间长度过长跑不出结果"并非单一 bug，而是三层问题叠加：

- **A 数据抓取层**：`hist` 超过约 1500 天后改走东财分页，页数随天数线性增加；超长窗口 + 多股并发易触发上游限流/超时，且**抓取失败被静默跳过该股票**（`except → continue`），整池拉不到才抛"有效样本不足"。
- **B 计算层**：样本数 ≈ 候选池 × 单票 K 线长度；叠加超参寻优（默认约 30 trial 各重训）或分层回测（每调仓日重训，调仓次数 ∝ 历史/间隔）呈组合爆炸；**ML 回测训练直接在事件循环执行、未丢线程池**，长任务阻塞后端使轮询卡死。
- **C 隐藏放大**：填 `startDate/endDate` 跨度过大时，后端自动把 `hist` 抬升到"跨度 + 约 300 天"，即使未手动调大 `hist` 也会触发 A、B。

## 2. 优化建议清单（按优先级）

### P0 — 抓取失败可见化 + 重试退避

- **现象**：单只股票 K 线拉取异常被 `except → continue` 静默跳过，整池失败才报错；用户无法判断是哪几只、为何失败。
- **影响**：长窗口下大量样本静默丢弃，任务要么直接失败，要么样本严重缩水却不被告知。
- **建议**：
  - 对失败股票计数并区分「网络超时 / 上游限流 / 无数据」，在任务结果里明确回显「N 只拉取失败（其中限流 X 只）」；
  - 对单只失败做有限次**指数退避重试**，而非一次性丢弃；
  - **优先命中本地 K 线缓存**，减少重复网络请求（长窗口最耗网络）。

### P1 — 长窗口计算量设上限 / 分块

- **现象**：样本数 ≈ 候选池 × 单票 K 线长度，`hist` 越大样本越多；超参寻优默认 30 trial 各重训、分层回测每调仓日重训，组合爆炸。
- **建议**：
  - 对「候选池大小 × hist」设合理上限（或分页批处理），超限给出明确提示而非硬跑；
  - 提交前提供「预计样本量 / 预计耗时」预估，让用户在跑之前判断是否可行；
  - 超参寻优的 trial 数改为**可配置项**（默认降低），并支持早停。

### P1 — ML 回测训练丢线程池（避免阻塞后端）

- **现象**：`ml-backtest` 的训练直接在事件循环执行、未丢线程池，长任务会阻塞整个后端，连进度轮询接口都卡死。
- **建议**：将回测内每个调仓日的模型训练用 `run_in_executor` 丢到线程池（与 `ml-train` / `ml-evaluate` 保持一致），保证后端事件循环不被长时间 CPU 任务阻塞。

### P2 — 时间段自动抬升 hist 加提示 + 天花板

- **现象**：填 `startDate/endDate` 跨度过大时，后端自动把 `hist` 抬升到「跨度 + 约 300 天」，用户无感知。
- **建议**：
  - 自动抬升时在前端**明确提示**「已按时间段将历史长度自动调整为 N 天」；
  - 对自动抬升设**上限**（如不超过某天数），超限要求用户缩短时间段或降低候选池。

### P2 — 前端提交前透明化

- **建议**：训练 / 评估按钮旁实时显示「将拉取约 X 天 × Y 只 = 预计 Z 条样本 / 预计耗时」，让用户对"时间长度"的成本有直观感知，避免盲目调大。

## 3. 验收建议

- 用三档实测：
  - `hist=1024`（默认，应正常出结果）；
  - `hist=3000`（长窗口，应不再整池静默失败，或明确提示失败只数而非硬崩）；
  - 候选池 × hist 超限（应提示而非崩溃）。
- ML 回测长时间任务下，验证进度轮询接口**仍可响应**（不被阻塞）。

## 4. 结论

三层问题需分别治理：**抓取层**做「可见化 + 重试 + 缓存优先」，**计算层**做「上限 + 分块 + 线程池隔离」，**交互层**做「自动抬升提示 + 成本预估」。三者补齐后，合理时间长度内可稳定出结果，超范围也能优雅提示而非卡死 / 硬失败。
