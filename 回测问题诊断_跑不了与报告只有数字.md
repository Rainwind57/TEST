# 回测功能问题诊断（整合版）

> 整合两类回测问题：
> **A. 回测跑不了 / 失败** —— 基于 2026-08-07 实测验证（selection.py 与上游行情）
> **B. 回测报告只有数字、缺内容解读** —— 基于 2026-08-07 对 reporting.py 的最新复核
>
> 全程只定位问题、未改动任何代码。每条标注证据行号与修复方向。

---

## 〇、代码演进修正声明（重要，勿沿用旧结论）

本应用在多轮对话间持续演进，下列早期诊断结论**已过时**，本轮实测已修正：

1. **"回测报告不含模型/调仓方式/图表"** → ❌ 过时。
   `reporting.py` 现已包含：回测概要块（起止日/历史长度/成本/候选池）、ML 模型详情块、调仓方式文字行、3 个 ECharts 图表、图表本地化（echarts.min.js 内联 base64）。见 `reporting.py:85-175, 263-265, 13-35`。
2. **"盯盘选大盘 / 无减持平仓 / 自动调仓空开关"** → 部分 ❌ 过时。
   `scheduler.py` 现已实现三态信号（看多/看空/中性）、平仓/减仓逻辑、自动调仓真实下单（`scheduler.py:200, 229-248, 365-470`）。但"减仓实为全平""score_codes 口径仍不一致"等细节问题仍存。
3. **"回测必挂"** → ❌ 过时。
   本轮实测 `board=all + 分组 2` 在池 60 / 300 下均 **HTTP 200 跑通**（effectiveStocks=59 / 297）。

本文聚焦**当前仍确凿存在**的问题。

---

## 一、回测跑不了（失败）根因与解决

### 1.1 报错触发机制（selection.py:453-460）

```python
fetched = await asyncio.gather(*(fetch_one(c) for c in codes))
series = {code: kl for code, kl in fetched if len(kl) >= 40}   # 仅保留 K 线 ≥40 根
if len(series) < groups * 3:                                    # 有效样本须 ≥ 分组数×3
    raise HTTPException(422, "有效股票样本不足：候选池 X 只，"
        f"K线拉取失败 {fetch_fail} 只，历史不足被剔除 {B} 只，K线≥40日的仅 {N} 只。")
```

另有候选池为空分支（selection.py:427-432）：
```python
if not pool:
    raise HTTPException(502, "候选池为空：上游行情列表获取失败或被限流（非参数问题）。")
```

### 1.2 两类报错对照

| 报错 | 触发位置 | 含义 |
|---|---|---|
| **502 候选池为空** | selection.py:427 | 候选池拉取失败（上游限流）→ pool 为空 |
| **422 样本不足…K线≥40日仅 N 只** | selection.py:455 | 候选池有数据，但有效样本 < groups×3 |

422 文案含 **4 个数字**，用来定位根因：
- `候选池 X`：原始候选数
- `K线拉取失败 A`：上游返回空（限流/网络）
- `历史不足被剔除 B`：拉到了但 K 线 <40 根（新股/停牌/退市）
- `K线≥40日的仅 N`：最终有效数（须 ≥ groups×3）

### 1.3 实测验证（确凿）

| 场景 | 请求 | 结果 |
|---|---|---|
| 全部 A 股 + 分组 2 + 池 60 | `board=all, groups=2, poolSize=60` | ✅ 200，effectiveStocks=59 |
| 全部 A 股 + 分组 2 + 池 300（拉满） | `board=all, groups=2, poolSize=300` | ✅ 200，effectiveStocks=297 |
| 自选 8 只 + 分组 5（默认） | `codes=[8只], groups=5` | ❌ 422（A=0, B=0, N=8，需 15 只） |
| **沪深 300 板块** | `board=hs300, poolSize=60` | ❌ **502 候选池为空**（上游限流） |

### 1.4 根因分类与解决

**① 参数约束：有效股票数 < groups×3（最常见，非 bug）**
- 前端默认 `groups=5`（需 15 只）、`poolSize=60`（BacktestView.vue:33,29）。自选几只 + 默认分组 5 必触发 422。
- 解决：分组数调到 **2**（仅需 6 只）或 **3**（需 9 只）；或增大候选池。

**② 上游限流 → 502 候选池为空（具体板块）**
- `fetch_market_list_multi` 拉板块候选池被上游限流。实测 `board=hs300`（沪深 300）、`zz500` 等具体板块直接 502。
- **"全部 A 股"(`board=all`) 实测稳定可跑**；问题集中在具体板块。
- 解决：错峰重试；或先到行情页确认该板块列表能加载；根治需给候选池拉取加失败重试 + 本地缓存降级。

**③ 上游限流 → 422（K 线拉取失败 A 大）**
- 候选池拉到了，但并发拉几百只 K 线时上游限流，A 接近候选池总数 → N 不足。
- 解决：错峰；减小 poolSize；根治需给 K 线拉取加退避重试（adapters._kline_retry 已有一层，但并发高仍会被掐）。

**④ 间歇限流（行情高峰）**
- 9:30 开盘、15:00 收盘前后上游最易限流。同一请求高峰失败、平峰成功（本轮实测平峰全通）。

**⑤ 代码缺 `sh`/`sz` 前缀（仅手动调接口时）**
- 腾讯接口对裸码 `600519` 返回空 → K 线全失败。正常 UI 流程自动带前缀；手动 curl 才会踩。

### 1.5 修复方向（按性价比）

| 优先级 | 修复 | 位置 |
|---|---|---|
| P1 | 候选池拉取 `fetch_market_list_multi` 失败自动重试 + 本地缓存降级，避免直接 502 | adapters.py / selection.py:423 |
| P1 | K 线并发加退避重试 + 失败样本显式提示（区分"限流"与"真无数据"） | adapters._kline_retry |
| P2 | 前端分组数/候选池与 groups×3 不匹配时提前校验提示，而非等 422 | BacktestView.vue:128 |
| P2 | 报错文案区分"数量不足"与"网络失败"，减少误判 | selection.py:457 |

---

## 二、回测报告"只有数字"问题

### 2.1 已修复（勿沿用旧结论）

`reporting.py` 当前已具备下列内容，报告**并非纯数字**：
- 回测概要块：起止日、历史长度、成本、候选池、板块、策略来源、模型 ID（reporting.py:85-175）
- ML 模型详情块：模型类型、特征数、超参数、Top 特征重要性（reporting.py:118-148，**仅 ML 回测且有 `_modelMeta` 时渲染**）
- 调仓方式文字行（reporting.py:150-157）
- 3 个 ECharts 图表：多空累计收益与回撤、分组平均收益、IC/RankIC 时序（reporting.py:263-265）
- 图表本地化：echarts.min.js 内联 base64，离线自包含（reporting.py:13-35）
- 警告横幅：前视偏差 / 生存偏差 / 仅多头（reporting.py:100-116）

### 2.2 仍缺失（本报告核心问题："只有数字"的真��含义）

**2.2.1 无"结果解读 / 结论"文字段（P1）**
- 报告有 KPI 数字卡、IC 统计一句话、调仓方式一行，但**整篇没有一个段落用自然语言解释结果好坏**。
- 例：Sharpe 1.86、最大回撤 -37%、胜率 56.5%、IC 胜率 56.5% —— 数字摆着，但没人告诉用户"这策略风险调整后收益好但回撤偏大、因子有效性中等"。
- 证据：全文 grep `解读/interpret/结论/评价` 零匹配；报告结构无解读段落（reporting.py:77-330）。
- 修复：新增"结果解读"区块，对 Sharpe/回撤/胜率/IC 给定性评价（高/中/低）+ 一句策略结论。

**2.2.2 技术因子回测无"因子说明"块（P1）**
- 模型详情块仅在 `strategy_kind=="ML模型"` 且 payload 带 `_modelMeta` 时渲染（reporting.py:118-121）。
- 绝大多数分层回测是**技术因子**（无 modelId），此时 `strategy_name = factorLabel`（如"60日动量"），**报告里只有因子名，无定义/计算方式/方向/经济含义**。
- 用户体感"报告没有模型"——对技术因子回测确实如此（只有一个因子名 + 数字）。
- 修复：新增"因子说明"块，渲染因子定义、方向（动量/反转）、窗口、经济逻辑；因子列表可复用 `factors.py` 的 `FACTORS` 元数据。

**2.2.3 调仓方式文字过简（P2）**
- `rebalance_desc`（reporting.py:152-157）仅一行："分层回测·每 N 日按因子值全市场排序分 G 组·做多 Top 组·交易成本已计入·T+1 开盘入场"。
- 无：调仓成本对收益的侵蚀说明、与选股打分口径的关系、为何选 Top 组、再平衡频率的取舍。
- 修复：扩充为 2-3 句，含成本影响与风控提示。

**2.2.4 图表无配套文字说明（P2）**
- 3 张图（reporting.py:263-265）直接渲染，图下无标题性文字解释"看什么、怎么读"。
- 修复：每张图加 1 行图注（如"上图蓝线为做多 Top 组累计收益，阴影为回撤"）。

### 2.3 证据行号速查

| 问题 | 行号 |
|---|---|
| 无结果解读段落 | reporting.py:77-330（全程无 interpret/结论） |
| 因子说明块缺失（仅 ML 有详情） | reporting.py:95, 118-121 |
| 调仓方式文字过简 | reporting.py:152-157 |
| 图表无图注 | reporting.py:263-265 |

### 2.4 修复方向

| 优先级 | 修复 | 位置 |
|---|---|---|
| P1 | 新增"结果解读"区块（Sharpe/回撤/胜率/IC 定性评价 + 策略结论） | reporting.py render_html |
| P1 | 技术因子回测新增"因子说明"块（定义/方向/窗口/逻辑） | reporting.py:118 分支补充 |
| P2 | 调仓方式文字扩充（成本侵蚀/风控提示） | reporting.py:152 |
| P2 | 每张图表加图注 | reporting.py:263-265 |

---

## 三、优先级总表

| # | 问题 | 类别 | 优先级 | 证据 |
|---|---|---|---|---|
| 1 | 候选池拉取上游限流直接 502（板块） | 跑不了 | P1 | selection.py:427, 实测 hs300 |
| 2 | K 线并发上游限流 → 422 | 跑不了 | P1 | selection.py:453, fetch_fail |
| 3 | 参数约束 groups×3（默认分组 5 易触发） | 跑不了 | P2 | BacktestView.vue:33, selection.py:454 |
| 4 | 报告无"结果解读"文字段 | 只有数字 | P1 | reporting.py:77-330 |
| 5 | 技术因子回测无因子说明块 | 只有数字 | P1 | reporting.py:95,118-121 |
| 6 | 调仓方式文字过简 | 只有数字 | P2 | reporting.py:152-157 |
| 7 | 图表无图注 | 只有数字 | P2 | reporting.py:263-265 |

## 四、修复顺序建议

1. **先治"跑不了"的根因 ①②**：给候选池拉取与 K 线拉取加失败重试 + 缓存降级 + 退避（直接消灭 502/422 限流报错，无论选板块还是全部 A 股都能扛）。
2. **再补"报告内容"根因 ④⑤**：新增结果解读段 + 技术因子说明块（这是"只有数字"的核心，改完报告才有可读性）。
3. **最后打磨 ③⑥⑦**：前端参数校验提示、调仓方式扩充、图表图注。

---

*整合时间：2026-08-07。基于 selection.py / reporting.py / BacktestView.vue / adapters.py 代码复核与 API 实测。*
