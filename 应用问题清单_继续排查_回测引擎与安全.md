# TEST 量化平台 · 继续排查（回测引擎 / 因子表达式 / 安全权限）

> 时间：2026-08-10 12:34。应用运行中（前端 5899 / 后端 8899）。
> 方法：3 路并行审查（回测与交易引擎 / 因子表达式与行情数据 / 其余路由功能与安全）+ 关键项实测复现。
> 范围：补充前几轮（初始清单、运行时、图表报告、盯盘选股、量化致命问题、验证报告）**尚未覆盖**的模块，找确凿的新 bug 与优化点。
> 结论一句话：**本轮发现一批新的高严重度问题，其中安全类最致命——任意登录用户可读/删他人任务与报告、匿名可启停调度并打开自动交易；回测引擎仍有伪造流水、末日强行撮合、ML 回测崩溃等硬伤；因子表达式与行情数据存在候选池必崩、复权基准漂移污染历史 K 线等问题。**
> 全程只读、未改任何代码。

---

## ⚠️ 对 2026-08-10 10:04 验证报告的更正（实测复现）

该报告有 2 条 P0 经本次实测/读码确认属**误报**，撤回：

1. **"回测报告 500（reporting 变量未定义）" —— 不成立。**
   当前报告渲染接口实测正常返回内联图表的 HTML（HTTP 200，含模型详情块/因子说明块/图表）。当时 422/400 是我复测 payload 缺字段所致，非代码 bug。
2. **"盯盘默认信号被清空（logger 未导入）" —— 不成立。**
   调度模块顶部已正确导入 logger（全文件唯一导入点），信号生成路径正常，默认状态下能看到信号。当时审查 agent 看错行号。

> 其余项目（分时图空、风险归因 500、选股过滤正则误杀、ML 前视/CV 未动、优化取消空开关等）本轮未逐一重验，仍按原结论保留，后续如改动需再复核。

---

## 一、安全与权限（最致命，建议最先修）

### 1.1 任务（jobs）无用户归属，全局互通 —— P0
任务表没有记录归属用户，查询与删除都不按用户过滤。任一登录用户都能列出他人任务、读取完整结果（选股名单、模型配置、回测参数）、取消他人正在跑的任务。

### 1.2 中间产物（artifacts）零归属 —— P0
产物存档不写用户，列举/读取/删除全库共享。任意登录用户可读、删全部中间结果。

### 1.3 盯盘模块整模块无鉴权 —— P0
盯盘相关写接口全部不校验登录。匿名即可启停调度器、打开自动交易开关、绕过交易日判断触发全量扫描（算力放大、可被滥用）。

### 1.4 登录防护形同虚设 —— P1（安全）
登录与注册接口被全局限流白名单豁免，仅剩极弱的按 IP 频次限制：无按账号锁定（多 IP 打同一账号不受限）、无验证码、登录成功不清零计数、置于反代后全体共用一 IP 易被全局误锁；注册零限流可批量刷号。登录令牌长效且无吊销机制。

### 1.5 报告跨用户可读可删 —— P1（安全）
报告查询允许 `user_id = 0`（匿名归档）的记录被当作全员公共池；报告 HTML 内容零转义，可构造注入脚本，打开即执行。

### 1.6 读接口泄露匿名资产 —— P2（安全）
部分读接口用宽松的"可选登录"而非强制登录，未登录即可返回含完整配置的策略等数据。

---

## 二、回测与交易引擎

### 2.1 带模型的回测整体崩溃 —— P0
任何带模型标识的回测，在回测主流程里引用了两个从未赋值的变量，直接抛错，被外层裸异常吞成"ML 回测失败"，模型回测功能整体不可用。

### 2.2 事件回测伪造成交流水、订单静默蒸发 —— P0
成交记录在数量校验之前就已写入，随后因现金不足或超单股上限把成交量削为 0 并退出，但成交记录已留下、订单也不再回队列。结果出现"成交了但资金市值没动"的幽灵记录，资金不足的买单永久丢失而非递延，回测结果不可信。

### 2.3 末日信号强行撮合，无视涨跌停 —— P1
回测循环结束后，对当日刚生成的信号立即以当日开盘价撮合，并跳过涨跌停判断。末日信号用当日收盘因子却成交在当日开盘属反向前视，且涨停板也能买入。

### 2.4 事件回测重复下单洪泛 —— P1
以"当前持仓为 0"判断建仓，但订单 T+1 才撮合，挂单期间持仓仍为 0，信号持续满足则每日重复下单，单票建仓量数倍于计划，触顶后又被 2.2 静默吞掉。

### 2.5 ML 回测入场价违反 T+1 —— P1
ML 回测用信号日收盘价入场（因子回测已改为次日开盘），信号与成交同根 K 线，收益系统性高估。

### 2.6 模拟盘无 T+1、零交易成本 —— P1
模拟盘卖出仅校验数量、不记录买入日期，可当日反手 T+0 刷单；全程不扣佣金/印花税/滑点，与回测口径完全不一致。

### 2.7 日内回测乐观撮合 —— P1
同一根 bar 同时触及止盈与止损时优先取止盈；跳空穿越止损仍按理想价成交。胜率与收益系统性虚高。末根未跨日则持仓被丢弃，浮亏不计入指标。

### 2.8 组合日内收益率分母臆造 —— P1
日内组合收益按"估算"的占用资金计算，总收益为不可复现的估算值。

### 2.9 调参接口由 502 变为 500 —— P1
调参接口的字段错位已修正，但依赖库未实际安装，在耗时数十秒后才抛"模块未找到"错误，且取消信号未传入（已知问题）。

### 2.10 组合优化只买不卖 —— P2
优化下单只建仓不调仓，重复调用即叠加建仓直至资金不足。

### 2.11 其他
停牌估值分支每次全量排序（性能）；事件回测接口缺登录校验；成交滑点字段实际写入了限价字段；回归年化的交易日历在停盘日丢失整段截面。

---

## 三、因子表达式与行情数据

### 3.1 沪深300/中证500 候选池必崩 —— P0
板块成分股拉取的指数分支在成功路径上引用了未赋值的变量，直接报错；而多板块拉取又把异常吞掉返回空池，对外表现为"上游限流/样本不足"。说明之前反复出现的"候选池为空"至少部分根因是代码 bug，而非纯网络限流。

### 3.2 前复权基准漂移，历史 K 线被永久污染 —— P0
K 线缓存按"代码+日期"增量更新且无复权标记，每次只覆盖最近若干根；前复权价在除权后全历史重算，更早的行保留旧基准，导致除权日出现虚假跳空。长窗口因子（如 120 日动量、52 周最高距离、60 日最大回撤）系统性失真，且完全静默、缓存越久越错。

### 3.3 表达式异常逃逸为 500，非法式子却静默通过 —— P1
表达式求值中除零、类型错、溢出等异常直接逃逸成服务端错误，用户体验为 500 而非可读的 400；反向地，参数个数错误的式子在部分环境下静默通过（语义已错却不报）。

### 3.4 静默归零掩盖错误 —— P1
拼错因子名不报错、该因子静默失效；波动率除零被压成 0；类别因子参与运算失败全列变 NaN 再被填成 1.0。用户以为用了五因子实则三因子，被污染的行照常参与排序。

### 3.5 类别因子直接 500 —— P1
行业因子被注册并暴露给前端，但打分入口对字符串做数值运算直接报错，前端若勾选行业因子即 500。

### 3.6 表达式引擎与因子库口径不一致 —— P1
缺失值填充策略、方向取反、量纲三者与因子库主路径不一致：同组因子用表达式写法和用组合写法结果不可比；表达式不乘方向导致反向奖励高波动；不同量纲因子隐式权重被量纲支配。

### 3.7 生存偏差 + 前视偏差 + 流动性偏差三重叠加 —— P1
候选池取自当日快照、板块取当前成分、行业取当前分类，却直接用于历史回测；无 ST/次新/退市过滤；按成交额截断只留大票。回测收益系统性高估。

### 3.8 因子数值口径不一致（逐点 vs 向量化）—— P2
个别因子在逐点实现与向量化实现上边界处理不同（如极值相等时返回缺失 vs 返回 0），导致两条路径选出的股票不同。

### 3.9 性能与缓存浪费 —— P2
所谓向量化仍有大量 Python 循环且存在浮点累积误差；缓存键含"天数"导致同票不同天数各存一份、命中率低；新鲜度用自然日判断，长假期间恒判陈旧反而加重上游限流。

---

## 四、其他功能与优化点

- **任务取消是假取消**：取消事件先出注册表再置位，长循环内无轮询点，状态秒变"已取消"但 CPU 继续跑到底；重启后残留"运行中"任务永久僵死。
- **异步/一键运行路径的回测存档必然失败**：绕过依赖注入导致用户标识传成对象，写入数据库异常被静默吞掉，历史报告永远不生成。
- **报告删除"删文件不删记录"**：先删文件再删库，若记录不匹配（如匿名公共池）则文件已删但返回 404，记录残留。
- **回归口径偏乐观**：建仓价与因子同取当日收盘、无 T+1 延迟；t 值未做自相关校正。
- **ML 任务占用默认线程池**：与"独立线程池避免整站假死"的设计矛盾。
- **读接口泄露匿名资产**、**报告文件名同秒并发互相覆盖**、**产物列表全量加载变慢**、**路径校验在 Windows 下可改盘符逃逸**等。

---

## 五、优先级总表与修复顺序

**第一批（安全 + 数据正确性，P0）**
1. 盯盘模块加登录校验（防匿名启停/开自动交易/全量扫描）
2. 任务与中间产物加用户归属并校验 owner（防跨用户读删）
3. 板块候选池变量未定义 bug（修后"候选池为空"大半消失）
4. 前复权基准漂移（K 线缓存加复权标记，除权日整表重写）
5. 带模型的回测崩溃（补两个未定义变量）
6. 事件回测伪造成交流水（成交在确定数量后再记录）

**第二批（回测严谨性，P1）**
7. 末日强行撮合 / 重复下单 / ML 入场价 T+1 / 模拟盘 T+1+成本
8. 日内回测乐观撮合 / 收益率分母臆造
9. 表达式异常转 400、非法式子拦截、静默归零与类别因子 500
10. 生存偏差+前视+流动性三重过滤；表达式与因子库口径统一

**第三批（体验与优化，P2）**
11. 组合优化只买不卖；任务假取消；异步路径存档失败；报告 HTML 转义；读接口强制登录；性能与缓存

---

*本报告仅做验证与定位，未修改任何代码。如需针对上述任一 P0（尤其安全类 1–3、数据类 3–4、回测类 5–6）出最小修复补丁，可指定编号。*
