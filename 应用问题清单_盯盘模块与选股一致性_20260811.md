# 盯盘模块信号与「选股结果不一致」问题核查（2026-08-11）

> 只读核查，未修改任何代码。代码以 2026-08-11 当前实际状态为准（多轮对话间代码已演进，旧结论已失效）。
> 按你的偏好：只叙述现象 + 代码信号（文件/行号），不贴大段代码。

---

## 一、盯盘模块的信号到底是什么

盯盘扫描逻辑在 `backend/app/scheduler.py` 的 `_scan_signals_impl`，信号类型取决于当前引擎模式（`monitor_mode`，默认 `rule`，见 scheduler.py:53）。

### 1. 规则模式（系统默认）
- **看多类**：因子强势突破、超卖（RSI<30）、动量突破（momentum>0.1）
- **看空类**：因子弱势走弱、超买（RSI>70）、动量走弱（momentum<−0.05）
- **中性**：以上都不满足
- **减仓**：当前已持仓，且出现看空/走弱信号时，自动把信号改写为「减仓」
- 信号生成完全基于「动量 + RSI（或用户自定义因子）的 z-score / 阈值」，不依赖 ML 模型。

### 2. 模型模式（需在配置里指定 modelId）
- **看多 / 看空 / 中性**：由 ML 预测分映射（阈值 |score|>0.005，见 scheduler.py:246-251）
- **平仓**：当前已持仓且信号为「看空」时，改写为「平仓」
- 模型模式又分两种排名口径（`monitor_ranking`）：
  - `full`（默认）：对全市场排名后取 watchlist 子集分位
  - `isolated`：只对自选股内部排名

### 3. ⚠️ 全模块没有「调仓 / 换仓 / 再平衡」信号
无论是规则模式还是模型模式，信号类型都只有：看多、看空、中性、减仓（规则持仓看空）、平仓（模型持仓看空）。**不存在「调仓」语义信号**——盯盘不会主动建议"把 A 换成 B"或做再平衡，只能在已有持仓上做减仓/平仓。这解释了你"没发现调仓信号"的直觉是正确的。

---

## 二、盯盘结果为什么和选股对不上（核心问题）

这是本次核查发现的**真实功能性不一致**，并非误报。根因有 5 类：

### P0-1 默认引擎不同源（最根本）
- 盯盘默认是 `rule` 模式（动量+RSI 规则，scheduler.py:53 默认 `rule`），与选股用的「ML 模型打分 / 多因子加权」是两套完全独立的打分体系：因子集不同、公式不同、阈值不同。
- 现象：同一只股票，盯盘给「突破(看多)」，选股排行榜可能因为财务/资金流因子把它排到中后段，结论可能相反。这是结构性不一致，不是偶发。

### P0-2 候选池不同导致分位漂移
- 盯盘只扫描 `watchlist`（自选股，通常几十只）；选股扫描全市场或指定板块（成百上千只）。
- 即使同走 ML 模型模式 `full` 口径（与选股 `score_latest` 同源），一只股票在「几十只自选股内」的排名分位，和在「全市场」的排名分位天然不同，导致同一分数对应的"看多/看空"判定可能不同。

### P0-3 模型模式下盯盘写死参数，不吃调参与板块
- 盯盘模型模式调用 `ml.score_latest(cfg["modelId"], pool_size=150)`（scheduler.py:224）：**board 写死为 all、pool_size 写死 150、且不传 adjust（调参权重）**。
- 选股（`routers/selection.py:160`）调用 `ml.score_latest(modelId, board, poolSize, adjust=...)`：board / poolSize / 调参权重都是用户参数。
- 现象：你在选股里对某模型做过手动调参（featureWeights）或指定板块，盯盘仍用"全市场150只、未调参"的同一模型跑——分数和排名分位会和选股结果不一致。**调参后的模型，盯盘根本不会体现。**

### P1-4 信号语义不同（呈现形式本质不同）
- 盯盘输出「三态信号」（看多/看空/中性 + 平仓/减仓），本质是要不要买卖；
- 选股输出「分数排名列表（topN）」，本质是排行榜。
- 两边对"同一次扫描结果"的呈现维度就不一样，没有一一映射关系，肉眼对比"对不上"是必然的。

### P1-5 阈值映射不同
- 盯盘模型模式用 `|score|>0.005` 把连续分映射成三态（scheduler.py:246-251）；
- 选股只用分数排序取 topN，不做这个二元判定。
- 现象：一个 score=0.003 的股票，选股可能排进 topN 列表，但盯盘判定为「中性」不算看多——同一分数在两边"是否看多"结论不同。

---

## 三、是否存在"bug"（vs 设计使然）

| 现象 | 判定 | 说明 |
|------|------|------|
| 默认 rule 盯盘与选股 ML/多因子结论相反 | 设计缺口 | 若产品意图是"盯盘复用选股信号"，默认不同源是功能缺失；若本就定位两套体系，则需在前端明确提示用户 |
| 模型模式不传 adjust / board / poolSize | **明确功能遗漏（P0）** | 调参后的模型、指定板块的模型，盯盘完全不跟随，用户会以为"同一模型结果却不一样" |
| isolated 排名与选股全池分位不同 | 已知口径分支 | 默认是 full（与选股同源），但用户若切到 isolated 会天然不一致，易误判 |
| 无调仓信号 | 设计缺口 | 全模块无再平衡/换仓语义，只有减仓/平仓 |
| 指数被选入盯盘 | 已修复 | scheduler.py:202 已加"过滤不可交易标的（指数/ETF）"，现网不再对指数产生信号 |

---

## 四、结论与建议优先级

**盯盘信号类型**：看多 / 看空 / 中性 / 减仓（规则持仓看空）/ 平仓（模型持仓看空）——**无调仓信号**。

**与选股不一致确实存在，主因**：① 默认引擎不同源（rule vs ML/多因子）② 候选池不同（自选 vs 全市场）③ 模型模式写死参数不吃调参与板块。后两条是明确值得修的 P0。

建议修复顺序：
1. **P0**：盯盘模型模式透传 `adjust / board / poolSize`，让调参后的模型、指定板块的模型能跟随选股口径；
2. **P0**：前端在盯盘页明确标注"当前引擎（rule/model）+ 排名口径（full/isolated）"，避免与选股排行榜混淆；
3. **P1**：评估是否把盯盘默认引擎与选股打通（或用同一打分函数），消除"同股结论相反"；
4. 若需要"调仓"能力，需在信号类型中新增再平衡/换仓语义（当前完全缺失）。

> 注：之前诊断报告的"Purged Walk-Forward 失效""年化收益高估"等问题，已随代码演进修复，本次未复现，不在此列。
