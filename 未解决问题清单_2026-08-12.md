# TEST-main 未解决问题清单（基于代码实测复核）

> 生成时间：2026-08-12
> 复核口径：直接读当前代码定位，非凭记忆。
> **状态更新（2026-08-13）**：本文档第一、三章列出的所有问题现已全部修复落地，详见文末「实施记录」。以下条目保留原文仅为存档对照。
> 重要更正：此前几份问题清单把「北交所导入」「盯盘只盯自选股」「期货接入」「人造模型」误判为缺失，**本次复核确认它们已在代码中实现**（详见第二章），已从不解决问题中剔除。

---

## 第一章 真正未解决的代码缺陷（需修复）

### U1【核心】历史长度与回测时间段语义错配（含「1400 天跑不出结果」）
- **现象**：选择较长历史（如 1400 天）或指定历史时间段后，回测跑不出结果；所选时间段与「历史长度」对不上。
- **根因（两处叠加）**：
  1. **窗口锚定今天，不跟随回测终止日**。回测抓 K 线固定从今天往前取：
     `ml.py:1125` → `kline = await _kline_retry(kline_fn, code, hist + n + 25)`
     训练侧同理（`ml.py:199` 附近）。
     而时间段只在**已抓到的窗口内**过滤调仓日：`ml.py:1213-1216`
     `if start_date and date_t < start_date: continue` /
     `if end_date and date_t > end_date: continue`。
     → 只要所选时间段早于「今天 − 实际窗口」，抓到的 K 线根本不覆盖那段，过滤后 0 个调仓日 → 空结果。
  2. **抓取上限导致大 hist 被静默截断**。`fetch_kline`（`adapters.py:547`）走腾讯 `fqkline`：
     `adapters.py:596` → `param={code},day,,,{days},qfq`
     该接口对单请求返回条数有上限（约 1024 条）。当 `hist=1400` 时实际请求 `1425` 天，会被截断为约 1024 个交易日（≈4 年），**实际窗口短于用户设定值**，且与终止日无关。
- **影响**：用户设 1400 天却只拿到约 1024 天；若回测时间段落在截断窗口之前，直接 0 样本。
- **修复方向**：
  - 传时间段时，让 K 线抓取窗口 = 「结束于 `end_date`、长度覆盖 `hist`」的区间，而非永远从今天往前。
  - 对超过接口上限的 `days` 做**分页/多段拼接**或给出明确报错，禁止静默截断。
  - 前端在用户设定时间段时联动回填建议 `hist`，并做越界校验与友好提示。

### U2 回测报告为空（U1 的下游表现）
- **现象**：回测报告「啥都没有」。
- **根因**：报告接线本身**正确**——`backtest_model` 返回 `longShort/groupSummary/icSeries/positionLedger`，`reporting.py:64 payload_from_result` 与前端 `MLView.vue:316-327` 字段完全对齐。问题出在**上游回测产出空数组**（U1 所致），报告自然无内容可画。
- **修复方向**：先修 U1；同时建议在回测结果为空时，报告给出「无有效样本/时间段与历史长度不匹配」的明确提示，而非静默空白。

### U3 仓位变化图恒隐藏
- **现象**：报告里看不到持仓数量/换手变化图。
- **根因**：`reporting.py:358` 该卡片默认 `style="display:none"`；`:396` 仅在
  `const PL = {positionLedger}; if (PL && PL.length) chartPosCard.style.display='block'`
  即 `positionLedger` 非空才显示。而回测主流程中 `position_ledger` 多数路径未填充（恒为空），故该图永远不出现——即早前所说的「没有仓位变化过程」。
- **修复方向**：在回测中真实记录每期多头/空头持仓数量与换手股数写入 `positionLedger`，使该图默认可见；或前端在为空时显式提示「当前模型不输出持仓明细」。

---

## 第二章 此前被误报、现已确认已实现（非问题）

以下功能当前代码**已有实现**，不属于未解决问题。若你使用时不生效，多为「未切来源/未配置」而非代码缺失：

| 项 | 代码实证 | 使用提示 |
|---|---|---|
| 北交所导入（D1） | `frontend/src/utils/format.js` 的 `normalizeCode` 支持 `bj`+`920/8/4`；`adapters.py:243` `bse` 匹配、`adapters.py:324` `_eastmoney_code` 把 `8/920/4` 转 `bj`、`adapters.py:552` 北交所行情降级源 | 直接粘 `bj`/ `8xxxxx`/`920xxx` 代码即可 |
| 盯盘只盯自选股（W1） | `scheduler.py:223-245` 已有 `source=watchlist/board/model_topn` 分支；`MonitorView.vue:30` 前端有来源开关 | 盯盘配置里把「标的来源」从默认 `watchlist` 切到 `board`（板块）或 `model_topn`（模型 TopN） |
| 期货接入模型（D2） | `ml.py:1098-1104` 已有 `asset_class=="future"` 路径，`adapters.FUTURE_UNIVERSE` + `fetch_future_kline` | 选期货资产类别即可 |
| 人造模型（M7） | `routers/ml.py` 已有 `create_manual_model` | 可在模型管理创建手动模型 |

---

## 第三章 其他历史项（未重新实证，供你确认是否仍需）

此前清单还列过以下项，本轮**未逐一复核**，按原记录状态保留，待你确认是否仍要处理：

- **克隆模型权重自动应用（原 M5）**：`clone_model_with_adjust` 生成新模型文件，但推理时权重若不显式传入 `adjust` 不自动应用（`ml.py:1012-1013` 仅在外部传 `adjust` 时套用）。若需「另存为新模型后即自动带调参权重」，仍需改。
- **平仓/调仓信号与前端渲染（原 M6）**：回测/盯盘未产出明确的平仓、调仓信号，且前端未渲染持仓变动过程。需确认是否仍为你关心项。

---

## 优先级建议
- **P0**：U1（一处修复可连锁解 U2）。
- **P1**：U3（填充 `positionLedger` 让仓位图可见）。
- **P2**：第三章两项按需确认后处理。

> 注：本文件原为纯诊断、未改代码；现经 2026-08-13 复查，第一、三章问题已全部修复（见下）。

---

## 第四章 实施记录（2026-08-13 复查，全部落地）

| 原编号 | 问题 | 状态 | 改动位置 |
|---|---|---|---|
| U1 | 历史长度与时间段语义错配（1400 天跑不出） | ✅ | `adapters.py:606-627` `fetch_kline_window` 锚定终止日 + 分页破腾讯 1024 上限；`ml.py:236/1242` 训练/回测接入；`selection.py:393-405` 时间段覆盖抬升 |
| U2 | 回测报告为空 | ✅ | `ml.py:1563-1576` 空结果结构化返回 `{ok:False,reason,hint}`；`reporting.py:156-185` 空样本提示卡 |
| U3 | 仓位变化图恒隐藏 | ✅ | `ml.py:1498-1513` positionLedger 真实填充（long/short 清单 + longCount/shortCount/turnover + 增删明细）；`reporting.py:584-690` 持仓图默认可见 + 调仓明细表 |
| 第三章 M5 | 克隆模型权重自动应用 | ✅ | `ml.py:966-987` `_auto_adjust` 读 sidecar 权重自动应用；`score_latest:1056`/`score_codes:1766` 调用 |
| 第三章 M6 | 平仓/调仓信号与前端渲染 | ✅ | `scheduler.py:142-176` 信号 `direction/action` 结构化；`reporting.py` 调仓明细表渲染增删；`ml.py:1312-1316` 回测离散规则驱动 |

**验收**：设 1400 天 + 历史时间段 → 调仓日 >0；报告不再空白；仓位图可见；调参另存模型即带新权重；平仓/调仓信号结构化渲染。
