# 应用问题整合 · 现存缺陷与量化逻辑致命问题

> 整合范围（基于 2026-08-06 对当前代码的复核与实测）：
> - **A. 用户实测仍存在的问题**：分时线缺失、回测报告不含模型详情/调仓方式/文字图表、盯盘选股乱、无减持平仓信号、选股无法导入模拟盘、盯盘与选股打分不一致。
> - **B. 量化逻辑致命问题**：财务/资金流因子前视偏差、时序 CV 泄漏、盯盘"自动调仓"空开关、分层多空不可实盘、事件回测 open=close、生存偏差。
>
> 约束：**仅定位问题、给出证据与修复方向，未改动任何代码**。所有结论附 `文件:行号`。
>
> 先说明已复核正确的环节（避免误伤）：因子计算无前视、K 线前复权正确、事件回测 T+1 时序安全、选股打分方向取反正确、ML 标签用未来 N 日收益无泄漏。下文只列**仍存在的问题**。

---

# 第一部分 · 用户实测仍存在的问题（功能 / 界面层面）

## 1.1 行情页仍没有分时图 ❌

**现象**：行情详情页看不到当日逐笔/每分钟走势 + 均价黄线（分时图），只有日/周/月 K 线。

**证据**
- 后端数据源 `adapters.py` 只有分钟 K 线接口 `fetch_minute_kline`（`:808`，支持 1/5/15/30/60 分钟），**没有任何"分时"接口**。"分时图"指当日每分钟价 + 时间加权成交均价，与分钟 K 线（OHLC）是两回事，后端根本没这个数据通路。
- 前端行情页 `QuoteView.vue:314-318` 频度切换只有 `日K / 周K / 月K`（`klineFreq` 选项 `D/W/M`），**无分时 tab**；`loadKline`（`:140`）只调 `/kline?freq=D/W/M`。
- 行情详情卡显示了今开/昨收/最高/最低/量额，但无分时走势。

**修复方向**
1. 后端新增 `fetch_time_share(code)`：腾讯 `qt.gtimg.cn` 分时接口或东方财富 `push2his` 分时，写入 `adapters.py`。
2. 新增路由 `GET /timeshare?code=xxx`（`routers/quote.py`）。
3. 前端 `QuoteView.vue` 增加"分时"tab：调 `/timeshare`，用 ECharts 画价格 + 均价黄线双线，复用现有 `dataZoom`。

---

## 1.2 回测报告仍不含"模型详情" ❌

**现象**：报告里能看到模型 ID，但**模型类型 / 超参数 / 用了哪些因子 / 特征重要性**都没渲染；PDF 报告更是纯文字无图。

**证据**
- HTML 报告已显示模型身份：`strategy_kind="ML模型"`、`strategy_name=cfg.get("modelId") or factorLabel`（`reporting.py:87-95`）——即"策略来源=ML模型 / 策略名=mlmodel_xxx"有，但**模型核心信息缺失**：
  - 模型类型（`modelType`，`ml.py:461/569`）、超参（`bestParams`：`n_estimators/max_depth/learning_rate`，`ml.py:570,521-532`）、特征列表（`featureNames`，`ml.py:415/462`）、特征重要性（`featureImportance`，`ml.py:629`）—— meta 里**已存好**，但回测 `config` 只传 `modelId`（`ml.py:1190-1191`），报告不读这些字段 → 无法渲染。
- PDF 报告（`render_pdf`，`reporting.py:281-383`）完全无图、且只有 KPI 文字，模型详情仅一行兜底。

**根因**：模型 meta 落盘时已完整，但回测链路把 `modelId` 丢给报告后，没把 `modelType/bestParams/featureNames/featureImportance` 一并塞进 `config`。

**修复方向**
1. `routers/ml.py` 的 `/backtest` 构建 `config` 时，从 `load_model_meta` 取出上述四项一并写入。
2. `reporting.py`「回测概要」块新增：模型类型 / 关键超参 / 特征数 / Top 特征(重要性)，HTML 与 PDF 同步加。

---

## 1.3 回测报告仍缺"调仓方式"文字说明与结果解读文字 ❌

**现象**：报告只列数值参数，没有"本次回测是怎么调仓的"文字说明，也没有对策略与结果的结论性解读文字。

**证据**
- 回测 `config`（`selection.py:359-368` 的 `BacktestBody`）只有 `groups/n/startDate/endDate/modelId` 等**数值参数**，无"调仓方式/频率/选股规则"描述字段。
- 报告渲染的 config 字段（`reporting.py:78-101`）同样只有：持有期(n)、候选池、板块、成本——**无调仓逻辑文字**。
- 实际调仓逻辑在 `run_backtest`（`selection.py:483/541-543`）：每 `n` 日重新按因子值全市场排序、分 `groups` 组、头部组做多/尾部组做空（line 580 注释"调仓间隔 n 日→年化采样数=252/n"）——**这段逻辑完全没进报告**。
- 报告全程无"结果解读文字"：当前结构 = KPI 卡片 + 参数表 + 分组收益表 + IC 统计 + 3 张图，**没有任何结论性描述**（如"本策略年化 Y%、最大回撤 Z%、IC 均值 W，有效性中等"）。PDF 更纯数字。

**修复方向**
1. 报告「回测概要」增加"调仓方式"文字块，例如：`分层多空回测 · 每 {n} 交易日按因子值全市场排序分 {groups} 组 · 做多第1组/做空第{groups}组 · 交易成本已计入`。
2. 增加"结果解读"段：据 `sharpe/maxDrawdown/meanIC/IC胜率` 给定性评价；补充调仓净值路径、各组累计曲线、回撤区间标注等图表。

---

## 1.4 盯盘调度中选股会选到大盘指数（不符合逻辑）❌ P0

**现象**：开启盯盘后，扫描产生的信号里包含 `sh000001`（上证指数）等大盘/指数标的，并可能给出"突破"等买入倾向标签。指数不是可交易个股，对它出交易信号在逻辑上不成立。

**证据**
- 默认自选池混入指数：`db.py:9` `DEFAULT_CODES = ["sh000001","sh600519","sz000001","sz300750","sh601318"]`；`init_db()`（`db.py:124-128`）watchlist 为空时写入这 5 个，其中 `sh000001` 是上证指数。
- 盯盘直接扫描 watchlist：`scheduler.py:182` `SELECT code FROM watchlist`，**不过滤指数**。
- 规则模式对指数照常打分：`scheduler.py:215-236` 的 `one(code)` 对任意 code 拉 K 线、算 `momentum/rsi`，`m>0.1` 即打"突破"——指数同样触发。
- 加入自选也无指数校验：`routers/quote.py:66-83` `add_watchlist` 只去重。
- 指数还能被"买入"进模拟盘：`place_order`（`routers/portfolio.py:82-89`）只校验"行情能返回价格"，`sh000001` 有指数点位会被当可成交标的建仓。

**修复方向**：`DEFAULT_CODES` 移除指数或初始化过滤；`add_watchlist` 与 `_scan_signals_impl` 入口加指数/ETF 黑名单校验；`place_order` 对指数代码显式拒绝。

---

## 1.5 模型中也没有减持、平仓的信号 ❌ P0

**现象**：无论"模型模式"还是"规则模式"，盯盘信号都只有**做多/看多**语义，没有任何"减持""平仓""看空"类信号，模型无法表达卖出意图。

**证据**
- 模型模式只有二态信号：`scheduler.py:206-210` `signal = "ML看多" if score>0 else None`——只有"看多"或"无"，无减仓/平仓/看空类别。
- 规则模式也是入场偏向：`scheduler.py:229-236` 只生成 `超卖/超买/突破` 三类标签，都是"是否该买"的描述。
- 盯盘从不看现有持仓：`_scan_signals_impl` 全程只读取 watchlist，**不读取 `positions` 表**，即便已持仓也不会生成卖出判断。

**影响**：形成"只有买入、没有退出"的单边交易体系。与 1.4、1.6、2.3 联动，交易闭环完全断开。

**修复方向**：信号扩展为三态/多类（`看多/中性/看空` 或 `建仓/持有/减仓/平仓`）；规则模式增加与持仓联动的退出信号（持仓且 `rsi>70` 或 `momentum` 转负 → 减仓/平仓）；`_scan_signals_impl` 同时读取 `positions` 生成 exit 信号。

---

## 1.6 选股没办法"直接"导入模拟盘中交易 ❌ P1

**现状（有入口，但断点多）**：后端 `routers/selection.py:898-947` 的 `/select/apply` 支持 `action="buy"`，前端 `SelectView.vue:280-281` 也有"一键买入模拟盘(等权)"按钮——技术上能下单。但用户感知"无法直接导入"，根因是导入过程丢掉选股核心信息、且与盯盘脱节。

**断点明细**
1. **仅等权，丢弃选股权重/排名**（`selection.py:927`）：`per_code_cash = body.totalCash/len(body.codes)`，`composite_score` 算出的因子权重、排名、`score` 全部未用。
2. **必须手动填 totalCash**（`SelectView.vue:153`）：不填则 `toast('请输入买入总资金')` 返回。
3. **买入的持仓不回写 watchlist**（`selection.py:924-945` 只调 `place_order`，不写 watchlist）：而盯盘扫描源是 watchlist（`scheduler.py:182`），**买了也不被盯盘跟踪**。
4. **无"按选股权重/排名建仓"的组合导入**；已保存的 selection artifact 只有 `codes`，无"导入为模拟盘组合"入口。
5. **若选股池含指数**（默认 watchlist 的 `sh000001`）会被无差别买入——`place_order` 不拦截指数（见 1.4）。

**修复方向**：导入时尊重选股结果（用 `score/rank` 派生权重、支持按权重建仓）；`buy` 成功后自动把成交 code 同步进 watchlist（可选开关）；提供"导入为组合"入口；买入前校验代码可交易性。

---

## 1.7 盯盘和选股打分的股票、方法都不一样 ❌ P1

**现象**：同一只股票，在"盯盘"里得到的信号/打分，和在"选股"里得到的排名/打分经常不一致，两套体系结论不可比。

**证据**
1. **扫描股票池完全不同**：盯盘扫 watchlist（默认 5 只、含 `sh000001`，`scheduler.py:182`）；选股扫全市场 `fetch_market_list_multi(board, poolSize)`（默认 `poolSize=200`，`selection.py:204-208`）。两者集合基本不重叠。
2. **打分方法完全不同**：盯盘规则模式 = `momentum(60)+rsi(60)` 阈值（`scheduler.py:224-236`）；选股 = `composite_score` 用**用户任意勾选因子+权重+方向**（`selection.py:330-332`）。
3. **模型模式排名语境不同**：盯盘 `ml.score_codes(modelId, watchlist_codes)`（`scheduler.py:201`）——**对给定 code 孤立打分**；选股 `ml.score_latest(modelId, board, poolSize)`（`selection.py:155`）——**全池排名**。同一模型、同一只股票，两种口径结论天然不同。

**修复方向**：盯盘与选股共用同一打分函数/因子配置（选股选定因子方案后可"一键设为盯盘信号方案"）；盯盘模型模式改用 `score_latest` 排名口径或对 watchlist 结果做分位标准化；提供"把当前选股结果写入盯盘 watchlist"入口。

---

# 第二部分 · 量化逻辑致命问题（研究 / 回测严谨性层面）

> 下列问题会直接摧毁"回测可信度"或让"选股→盯盘→交易"链路名存实亡，属致命级。

## 2.1 财务 / 资金流因子存在前视偏差（Point-in-Time 违规）❌ P0

**现象**：用 ROE、PE、PB、主力净流入、北向持股等基本面/资金流因子训练或回测时，回测与交叉验证绩效被系统性高估——模型在历史某天"看到"的其实是那天之后才发布的财报/资金数据。

**证据**
- 数据集构建（`ml.py:214-223`）把快照特征固定为**当前最新值**套用到每一个历史截面：
  ```python
  if use_snapshot:
      r = row_by_code.get(code)          # 来自当前行情快照
      sv = [snapshot_factor_value(r, k) for k in snapshot_keys]
  ```
  即 2020-06-01 的样本，喂的是 2024 年的 ROE/PE——典型 point-in-time 违规。
- 推理侧 `score_latest/score_codes` 同样用"当前快照"（`ml.py:867`），与训练一致，故"训练/推理自洽"；但**回测（`backtest_model`）在历史重放，用的却是未来基本面 → 历史绩效失真**。
- 作者已知情：注释/docstring 写"**含前视风险，仅探索用**"（`ml.py:145,214,996`），但 **UI 不向用户提示**，含基本面因子的模型回测曲线会被当可信结果展示。

**影响**：含基本面/资金流因子的模型，其 IC、OOS Sharpe、分层收益均被夸大，实盘显著不及回测——量化研究最致命的一类错误（look-ahead bias）。

**修复方向**：基本面因子必须做时点对齐（按财报披露日取"截至该截面已公告"的财务值，point-in-time 财务库）；资金流同理对齐到截面日；UI 在模型含快照因子时显式弹窗警告"含前视风险，回测绩效不可信"。

---

## 2.2 时序交叉验证的"purge gap"实际失效，训练/测试时间重叠 ❌ P0

**现象**：ML 的时序 Walk-Forward CV 号称"中间留 gap 防泄漏"，但因 gap 单位是**样本序号**而非**交易日**，候选池较大时 gap 远小于一个交易日，训练集与测试集落于**同一交易日**，OOS Sharpe 被高估。

**证据**
- 样本排序：`ml.py:238` `sorted(..., key=lambda k:(meta_dates[k], meta_codes[k]))`——按 (日期,代码) 升序，每交易日约 `poolSize`（默认 200）条样本。
- 切分：`purged_walk_forward_split`（`ml.py:307-326`）的 `gap=5` 是**样本序号**间隔（`test_start = train_end + gap`）。
- `poolSize≈200` 时，`gap=5` 条样本 << 一个交易日的 200 条 → `train_end` 与 `test_start` 处于**同一交易日**（仅相邻几只股票），训练尾与测试头共享同一市场状态/未来窗口 → 时序隔离形同虚设。

**影响**：CV 的 OOS Sharpe、IC 是被污染的"伪样本外"指标，模型选择（optuna 以 OOS Sharpe 为目标，`ml.py:485`）可能选中过拟合折。与 2.1 叠加，含基本面因子的模型评估几乎完全失真。

**修复方向**：purge gap 必须按**交易日**计算——切分前取去重日期列表，训练区结束日与测试区起始日之间至少间隔 `n`(持仓周期)+若干交易日；或对样本按 `date` 分组后再切块，确保整日不被 train/test 拆分。

---

## 2.3 盯盘"自动调仓"是空开关，永远不成交易 ❌ P0

**现象**：调度器文档与配置都暗示"可选自动调仓"，但翻遍 `scheduler.py` 与全后端，`place_order` 只被选股/组合优化调用，**盯盘信号生成后从不被转换成任何下单动作**。

**证据**
- `scheduler.py:5` docstring："重算 watchlist 因子打分，生成信号（可选自动调仓，默认关闭）"。
- `scheduler.py:17` `from .routers import portfolio as pf` 导入了组合模块，但全文**无任何 `pf.` 调用**（`grep place_order` 仅命中 `selection.py:941`、`portfolio_opt.py:95`）。
- `_scan_signals_impl`（`scheduler.py:178-243`）只把信号写入 `_last_signals` 并落 `scheduler_runs` 日志；无论 `monitor_mode=rule/model`，**都不读 `positions`、也不调用下单**。

**影响**：用户开启盯盘、甚至以为"自动调仓"已启用，实则只是看板多一列信号，无任何实际交易动作——属虚假功能/空开关。与 1.4、1.5 联动，交易闭环完全断开。

**修复方向**：二选一——要么**删除"自动调仓"文案**避免误导；要么**真正实现**：在 `_scan_signals_impl` 末尾读 `positions`，对持仓按信号生成减仓/平仓单、对 watchlist 中"看多"且未持仓的生成建仓单，调 `pf.place_order`（需带 uid；后台线程需显式用户上下文）。执行前加风险闸（单次下单比例上限、总仓位上限，并与 1.4 指数校验联动）。

---

## 2.4 分层回测头条是"多空组合"，A 股不可实盘；且回测同日成交 ❌ P1

**现象**：分层回测默认头条指标 `cumulativeReturn/longShort` 是**做多 top 组 + 做空 bottom 组**的多空组合收益；A 股散户无法做空个股，该曲线不可直接实盘。同时分层回测在**信号根 K 线当日 close 成交**，与事件回测 T+1 不一致，含轻微同日成交前视。

**证据**
- 多空构造：`selection.py:547-559`
  ```python
  net_ls = ls_ret - 2.0 * cost_rate
  cum *= (1.0 + net_ls)          # 头条累计收益 = 多空组合
  net_top = top_ret - cost_rate
  cum_top *= (1.0 + net_top)     # 仅多头 top 组（可实盘部分）放次级字段
  ```
  bottom group（做空腿）在 A 股需融券，普通账户不可用。
- 同日成交：`selection.py:529` `fret = closes[i+n]/closes[i]-1` 与 `selection.py:506` 用 `i=date_maps[code].get(date_t)`——因子在 `close[date_t]` 算出、并以 `close[date_t]` 买入、`close[date_t+n]` 卖出，信号与成交同在 `date_t` 这根 K 线。事件回测（`backtest_event.py`）正确做了 T+1，两层回测口径不一致。
- 报告（`reporting.py`）把多空收益放更显眼位置，易让用户把多空曲线当策略可实盘收益。

**修复方向**：回测增加 `long_only`（仅多头）开关，默认以可实盘的 long-only top 组为头条指标；多空组合明确标注"研究用，需做空"。分层回测成交改 T+1 开盘（与事件引擎一致）。

---

## 2.5 事件回测用 `close` 冒充 `open` 撮合，无真实开盘价 ❌ P1

**现象**：事件驱动回测撮合价标注"次日开盘"，但实际 `open` 字段被设为**当日 close**（kline 数组未返回 open），成交落在次日 **close** 而非 open。

**证据**
- `backtest_event.py:149-151`
  ```python
  # kline_to_arrays 仅返回 close/high/low/volume，撮合价用 close 代 open
  "open": float(arr["close"][idx]),
  ```
- `_match_orders`（`backtest_event.py:176`）`price = s["open"]` 即等于次日 close。

**影响**：无法刻画次日高开/低开的滑点与跳空，回测精度下降；对突破/均值回归类策略，开盘价恰是最关键成交点，误差被放大。属真实度缺口（非致命前视），但会让事件回测与实盘偏差偏大。

**修复方向**：让 `kline_to_arrays` 一并返回 `open` 字段（腾讯/东财日 K 本含开收），撮合价用真实 `open[date_t+1]`。

---

## 2.6 生存偏差（历史文档已提，本轮复核确认仍成立）❌ P1

**证据**：`fetch_market_list_multi` 返回**当前上市**股票池，回测只测幸存者，退市股不在池内 → 历史收益系统性高估（`selection.py:632` 已有 `survivorshipBiasWarning` 提示，但默认候选池仍是当前名单）。属已知但仍是量化逻辑硬伤。

**修复方向**：接入含退市股的历史成分数据源，或用 as-of 时点的上市股票池；至少在报告中显式披露。

---

# 第三部分 · 整合：贯穿根因、优先级总表与修复顺序

## 3.1 贯穿根因

- **根因 A（数据准入缺失）**：全链路（默认 watchlist、加自选、买入、盯盘扫描）都不校验"代码是否可交易个股"，导致大盘指数一路畅通 → 触发 1.4、1.6(指数无差别买入)。
- **根因 B（信号语义单边）**：信号体系只有做多/看多，无减持/平仓/看空，且不读持仓 → 触发 1.5、2.3(即便有信号也不执行)。
- **根因 C（两套割裂的股票池与打分体系）**：盯盘扫 watchlist、选股扫全市场，且打分函数/排名口径不同 → 触发 1.7，并放大 1.4。
- **根因 D（量化研究严谨性缺失）**：快照因子前视 + CV 泄漏 + 多空不可实盘/同日成交/生存偏差 → 触发 2.1~2.6，直接摧毁回测可信度。

## 3.2 优先级总表

| # | 问题 | 严重度 | 当前状态 | 核心证据 |
|---|------|--------|----------|----------|
| 1.4 | 盯盘选股选到大盘指数 | P0 | ❌ 仍存 | `db.py:9,127` / `scheduler.py:182` |
| 1.5 | 模型无减持/平仓信号（单边做多） | P0 | ❌ 仍存 | `scheduler.py:206-210` |
| 2.1 | 财务/资金流因子前视偏差 | P0 | ❌ 仍存 | `ml.py:214-223,996` |
| 2.2 | CV purge gap 按样本序号、训练测试同日重叠 | P0 | ❌ 仍存 | `ml.py:238,307-326` |
| 2.3 | 盯盘"自动调仓"空开关，永不成交易 | P0 | ❌ 仍存 | `scheduler.py:5,17`（无 `pf.` 调用） |
| 1.6 | 选股无法直接导入模拟盘（断点） | P1 | ❌ 仍存 | `selection.py:924-945` / `place_order` 不拦截指数 |
| 1.7 | 盯盘与选股打分的股票/方法不一样 | P1 | ❌ 仍存 | `scheduler.py:182` vs `selection.py:204-208`；`score_codes` vs `score_latest` |
| 1.1 | 行情页无分时图 | P1 | ❌ 仍存 | `adapters.py` 无分时接口 / `QuoteView.vue:314-318` |
| 1.2 | 回测报告不含模型类型/超参/特征/重要性 | P1 | ❌ 仍存 | `reporting.py` 不渲染 meta / `ml.py:1190` 只传 modelId |
| 1.3 | 回测报告缺调仓方式文字 + 结果解读文字 | P2 | ❌ 仍存 | `selection.py:359-368` 无描述字段 / `reporting.py` 无结论文字 |
| 2.4 | 分层回测多空不可实盘 + 同日成交 | P1 | ❌ 仍存 | `selection.py:547-559,506,529` |
| 2.5 | 事件回测 open=close 冒充开盘 | P1 | ❌ 仍存 | `backtest_event.py:149-151,176` |
| 2.6 | 生存偏差 | P1 | ❌ 仍存（已知） | `selection.py:632` |

## 3.3 建议修复顺序（按"致命程度 × 收益/成本"）

1. **先修根因 A（指数准入）**：成本最低、收益高，顺带解决"选到大盘"与"指数被无差别买入"（1.4、1.6 部分）。
2. **再修根因 B（信号三态 + 读持仓）**：补齐减持/平仓退出信号，使交易闭环完整（1.5）；并落地或明确去除 2.3 的"自动调仓"。
3. **修量化严谨性（2.1→2.2→2.4/2.5→2.6）**：前视偏差与 CV 泄漏直接摧毁模型评估可信度，优先；其次对齐回测口径与实盘、消除生存偏差。
4. **最后修体验与报告（1.1/1.2/1.3/1.7）**：分时图、报告模型详情/调仓文字/解读、盯盘选股统一打分——属体验与可用性，不致命但影响日常使用。

---

*本文档整合自《应用问题清单_分时图与回测报告内容.md》《应用问题清单_盯盘选股逻辑.md》《应用问题清单_量化逻辑致命问题.md》三篇诊断，覆盖用户实测现存问题与量化逻辑致命问题两类。全程仅诊断、未改动任何代码。*
