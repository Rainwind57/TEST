# 应用排查：人造模型跑不了回测

日期：2026-08-18
范围：量化平台 ML 模块 —— 人造/手动模型（`modelType=manual`，如 `manual_20260818_161349`）在 ML 页点「回测」后表现异常。
约束：仅诊断，未改动任何业务代码。

## 一、现象

用户在 ML 页对「人造模型」点击回测按钮后，出现两类"跑不了"的观感：

1. **设了长回测区间但跑出来几乎是空的 / 只有最近一小段**：无论回测卡片里把历史长度或时间段设多长，回测结果都只覆盖最近约 60 个交易日，与预期的长周期回测严重不符。
2. **回测结果"看着不对"**：手工设了权重的因子（如市盈率、市净率、ROE、北向持股等）在结果里似乎完全没起作用，曲线与用户设定的因子权重对不上。

实测（直接调用后端回测逻辑，人造模型 + 历史长度 500 天）：
- 日志明确打印：起点被自动设为"快照生效日"（最近 60 个交易日），更早历史截面因前视偏差被跳过。
- 实际只产生 11 次调仓（对应最后 60 天窗口），而非 500 天历史应有的上百次。
- 回测卡片返回一条提示：模型含快照因子，起点已自动设为最近 60 个交易日；如需完整历史，请改用不含快照因子的模型或明确指定起始日。

## 二、根因（主因，必现）

人造模型的"因子清单"在创建时被**固定写死成全部因子的全集**（全部技术因子 + 全部非分类快照因子，约 57 个），而不是用户真正设了权重的那几个因子。

因为这份清单里必然包含快照类因子（市盈率、市净率、ROE、成交额、北向持股等），回测逻辑一旦检测到模型"含快照因子"，就会自动启动两套"防前视"保护：

- **起点被改写**：未显式指定回测起始日时，系统把回测起点强行改成"最近 60 个交易日"。用户设的历史长度、时间段参数全部被无视。
- **历史截面被跳过**：每一个早于该起点的调仓日，其截面样本都被跳过（因为快照因子用的是"当前值"，回溯到历史日期会产生前视偏差）。

结果：人造模型回测**永远只回测最近 60 个交易日**，长区间回测诉求落空 —— 这就是"跑不出来 / 跑出来的不对"的本质。

## 三、为什么训练模型能跑、人造模型跑不了

- 训练模型的因子清单是用户当时勾选的**子集**，很多人只勾技术因子、不含快照因子，就不触发上述前视压缩，长回测正常。
- 人造模型的因子清单是代码写死的全集，**必然含全部快照因子**，因此必然触发压缩。两者走同一套回测逻辑，差异只在于"因子清单里有没有快照因子"——而人造模型永远有。

## 四、放大问题的次因

1. **快照因子缺失值被兜底为 0**：回测侧需要为每只股票批量拉取财务 / 资金流 / 北向等数据来填充快照因子，这些数据接口偶发失败。失败时缺失值被直接填成 0。于是对人造模型而言，用户手工设权重的市盈率、市净率、ROE、北向持股等基本面因子在回测中几乎全是 0，权重形同失效，结果自然与设定不符却又不报错。
2. **数据抓取层不稳定（叠加）**：实测日志中大量新浪 K 线请求返回 456（限流/封禁）、腾讯降速；人造模型又比纯技术模型多拉 21 类快照数据，整体更慢、更易样本不足报错。
3. **样本内警告误报**：回测结果对人造模型附带"本回测为样本内…"的提示，但手工加权模型本无训练过程，该警告系误报，增加困惑。

## 五、修复方向（仅方向，未改代码）

- **P0**：人造模型回测不应被"快照前视"逻辑绑架。应以"用户实际设了非零权重的因子"为准来判断是否需要快照前视，而不是按写死的全集因子清单判断。
- **P1**：创建人造模型时，因子清单只记录用户实际使用的因子；或在回测时按非零权重因子裁剪，避免"含快照全集"误触发前视压缩。
- **P2**：快照因子缺失值应区分"前视不可用"与"数据缺失"，而非一律填 0；并在回测卡片明确提示"该模型含快照因子，回测区间自动限定为最近 N 个交易日"。
- **P2**：人造模型跳过"样本内"警告（其无训练区间概念）。

## 六、临时验证 / 复现

- 检查人造模型 JSON 的 `featureNames` 字段：必为全部因子全集（技术 + 快照），而非用户设权重的少数因子。
- 在 ML 页对该模型点回测、历史长度填大值：观察返回提示是否出现"起点已自动设为最近 60 个交易日"，以及调仓次数是否远小于长历史应有的数量。
- 对照：训练模型若因子清单不含快照因子，则回测区间按所设历史长度正常展开。
