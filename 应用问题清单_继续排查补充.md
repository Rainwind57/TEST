# 应用问题清单 · 继续排查补充（2026-08-07 新增）

> 本篇为在《应用问题整合_现存缺陷与量化逻辑致命问题.md》等前序文档之外，**继续深挖**得到的新 bug。覆盖三路并行审查：API/功能路由层、量化核心模块（risk/组合优化/因子/日内/事件回测）、ML 全链路。
>
> **本篇不再重复**前序文档已报告的问题（盯盘选大盘指数、模型信号仅看多无减持平仓、盯盘"自动调仓"空开关、财务/资金流因子前视的已知段落、CV 旧 `purged_walk_forward_split` 死代码、分层回测多空组合同日成交、`backtest_event` 的 `open=close` 冒充开盘、`adapters` 无连接池/kline 封顶/无分时/上游限流误报样本不足、`db` 默认 watchlist 含 `sh000001`、板块单选、报告缺模型/调仓方式文字等）。仅在其"更深关联"处做必要补充标注。

---

## ⚠️ 代码演进修正（2026-08-07 用户复核触发）

用户在 2026-08-07 复核时指出两件事（减仓平仓信号、分时线），经重新读代码确认：**代码自前序文档诊断后已显著演进，部分旧结论已过时**，在此更正，并补充新细节问题。

### A. 减仓/平仓信号 —— 已加入（修正"无减持平仓信号"旧结论）
- `scheduler.py:208-253` ML 模式已生成**三态信号**：全池排名口径 `pct>=0.75→看多 / <=0.25→看空 / 否则中性`（:229-234）；`score_codes` 口径 `score>0.005→看多 / <-0.005→看空 / 否则中性`（:243-248）。
- 持仓联动退出：持仓中遇「看空」→ 转「平仓」（:235-236, :249-250）；规则模式遇「看空/走弱」→ 转「减仓」（:295-296, :318-319）；对不在 watchlist 的持仓也做超买/走弱退出检查（:344-351）。
- **自动调仓已落地**（修正"空开关"旧结论）：`_execute_auto_trade`（:365-470）真正读写 `positions`/`portfolio_state` 并记 `trades`，含事务与现金校验。需 `db.get_setting("auto_trade")=="1"` 显式开启（:358），**默认关闭**。
- **"选大盘"问题已修复**：`codes=[c for c in all_codes if db.is_tradable(c)]`（:200）过滤指数/ETF，只对可交易个股生成信号与下单。

### B. 减仓/平仓信号的新细节问题（仍建议修）
- **"减仓"实为全平（语义与实现不一致）**：`:370` 注释与 `:438` "有卖出信号的持仓全平"——名为「减仓」的卖出单执行的是 `DELETE FROM positions`（清仓），并非卖一部分留一部分。用户若预期"减仓=降仓位"，实际会整笔清掉。
- **`score_codes` 路径打分口径仍与选股不一致**：ML 模式非 `full` ranking 时用 `score>0.005/-0.005` 绝对阈值（:243），而选股 `score_latest` 用全市场分位；同一模型对同一只股票在盯盘与选股下结论可能不同（之前"盯盘选股打分不一致"在 `full` 路径已修，`score_codes` 路径仍在）。
- **自动调仓默认关闭无提示**：UI 若未显式开 `auto_trade`，`_execute_auto_trade` 永不执行，用户可能以为"盯盘会自动交易"而实际只产生信号不下单。

### C. 分时线 —— 前端代码已存在，根因在后端数据接口（回应"依旧没添加"）
- 前端 `QuoteView.vue` **已有分时图**：下拉含「分时」选项（:373），`loadTimeshare()` 调 `GET /api/timeshare`（:149），`timeshareOption` 渲染价格+均价黄线（:255-390），空白时显示"暂无分时数据"（:390）。
- 用户实测"看不到分时线"的真实根因在后端 `fetch_time_share`（`adapters.py:1258-1297`），详见**第六部分 · quote.py/data.py 下"timeshare 跨日 + 均价跨日累计"条目**：
  1. `scale=1&datalen=240` 取"最近 240 根 1 分钟 K 线"，A 股一日恰 240 根 → 必然混入**昨日尾盘**；日期 `[:5]` 截断使昨日/今日同 HH:MM 在图上重叠；
  2. `cum_amount` 跨日累计 → 均价黄线严重失真（分时均价须当日归零）；
  3. 上游异常 `arr=[]` → 返 200 空数组，前端 `:390` 显示空白当正常，`quote.py` 的 502 分支永远走不到。
- 结论：**不是"没加分时图"，而是分时数据接口有缺陷导致显示空白/失真**。修复见第六部分。

---

## 〇、P0 致命问题速览（建议立即修）

| # | 问题 | 位置 | 后果 |
|---|------|------|------|
| 1 | 回测用未来价量筛样本（删负尾） | `ml.py:1094-1100` | 收益/Sharpe/胜率系统性虚高（数据造假） |
| 2 | 日内回测同 bar 成交 + 当根反手 | `intraday.py:78-101` | 绕开 T+1，回测失真 |
| 3 | 事件回测无现金校验、仓位上限未用 | `backtest_event.py:216-218` | 隐式无限杠杆 |
| 4 | ML 规则表达式走 `eval`，存在 RCE | `ml.py:1397-1426` | 用户可控输入逃逸沙箱 |
| 5 | `/api/ml/optimize` 100% 报 502 | `ml.py:106` | 调参功能完全不可用 |
| 6 | 风险模型 `ddof` 用反 | `risk.py:210`（同 `routers/risk.py:68`） | 特质方差放大 ~13 倍，归因数字全错 |
| 7 | 风险模型快照因子前视 | `risk.py:42-51` | 用今天估值预测历史收益，污染归因 |
| 8 | 报告接口越权 + HTML 导出 100% 失败 | `reports.py:119-124,42-58` + `client.js` | 任意用户读删他人报告；导出链路全挂 |
| 9 | 组合优化 `/apply` 绕过鉴权下单 | `portfolio_opt.py:63-95` | 匿名请求即可批量建仓 |
| 10 | 登录接口零防护可撞库 | `auth.py:123` + `main.py:46` | 无锁定/无验证码/非常量比较/排除限流 |

---

## 一、安全与鉴权

### P0
- **`reports.py:119-124` 越权读/删他人报告**：`_get_run(run_id, uid)` 收了 `uid` 却不使用，`list_backtest_runs(200, user_id=None)` 拉全量；`delete_report` → `db.delete_backtest_run` 在 `user_id is None` 分支为无条件 DELETE。`require_user_id` 只验"登录了"没验"是本人"，鉴权形同虚设。
- **`reports.py` HTML 导出 100% 失败（前后端契约冲突）**：`export_backtest`/`download_report`/`export_select_report` 及 reportlab 缺失时的 PDF 降级全部返回 `text/html`；前端 `client.js` 的 `saveBlob()` 把含 `text/` 的响应当错误并弹"下载失败"。HTML 是默认导出格式 → 整条导出链路不可用。修：错误统一 JSON，前端只对 `application/json` 判错。
- **`portfolio_opt.py:63-95` `/apply` 绕过 `require_user_id` 直接下单**：`place_order` 的 `uid=Depends(require_user_id)` 只在路由分发时生效；Python 内部直接调用时 `uid` 拿到 Depends 对象且函数体不用它 → 匿名请求即可批量建仓。修：抽 `_execute_order` 纯逻辑，路由各自挂鉴权。
- **`ml.py:1397-1426` 规则表达式走 `eval` 存在 RCE**：`eval(expr, {"f": f, "__builtins__": {}})`，`rule` 来自用户输入。清空 `__builtins__` 不构成沙箱（可经 `f.__class__.__mro__[...].__subclasses__()` 逃逸）；`_parse_rule` 只拦"连续 2+ 中文"，纯英文 payload 直通。修：改 `ast.parse` + 白名单节点（Compare/BoolOp/Num/Subscript），禁用 `eval`。

### P1
- **鉴权不一致**：`/evaluate /train /score /backtest /models/{mid}/params` 无 `Depends(require_user_id)`，而 `/optimize /import /manual`/DELETE 有 → 匿名可触发重 CPU 任务。
- **`routers/jobs.py` 全系列无鉴权**：submit/get/cancel 均无 `Depends`，可枚举 job_id 读他人结果、`nTrials` 无上限、可取消他人任务。
- **`routers/optimize.py:58-61` 保存策略归属 `uid=0`**：接口拿到 `uid` 却不传 `save_best_as_strategy`，保存返回 200，但 `list_strategies(uid)` 查不到、`run_strategy` 因 `user_id != uid` 返 403。
- **`quote.py:36-54` `/stock/exists` 可绕过的空校验 + `add_watchlist` 从不调它**：三源全挂时 `exists=True`；直接 `POST /api/watchlist {"code":"sh999999"}` 即可把不存在代码写库，后续拉数静默丢票。
- **`auth.py:123` + `main.py:46` 登录零防护**：`_hash_password(...) != row["password"]` 非常量时间比较、无失败锁定、无验证码；`_exempt_paths` 把登录排除在限流外 → 可无限撞库。修：登录单独 5 次/分钟/IP 锁定，`hmac.compare_digest`。
- **`artifacts.py` 4 端点全无鉴权**：任意用户可列举/读取/删除他人中间结果。
- **`data.py:64-71` `/data/index-constituents` 未知指数返回 200 空列表**：与"该指数确实无成分"不可区分；网络失败也返空缓存。修：未支持指数返 400，取数失败返 502。

### P2
- **`quote.py:28` `days` 无校验**：`days=0` 返回全量历史、`days=-5` 丢最早 5 条；`data.py:90` `max(60,min(days,500))` 把用户请求的 10 天静默改成 60 天。

---

## 二、回测引擎（量化致命）

### 事件回测 `backtest_event.py`
- **B1 · P0 · `:216-218` 无现金校验 + `max_position_pct` 从未使用**：全仓 grep 确认 `max_position_pct` 只在 config 定义、`routers/selection.py:731` 赋值，引擎内零引用；配合固定 `qty`（`selection.py:757`），现金可无限为负 → 隐式无限杠杆，`cumulativeReturn` 完全失真。修：撮合前按 `min(qty, cash可承受量, 总资产*max_position_pct/price)` 截断。
- **B2 · P1 · `:212-213` 滑点重复计入 + 印花税摊到买单**：`exec_price = price ± slip`（滑点第 1 次），`cost = round_trip_cost_rate(...)*(qty*price)/2` 中 `round_trip=2*comm+stamp+2*slip`，/2 得 `comm+stamp/2+slip`（滑点第 2 次 + 印花一半摊给买）。买多收、卖少收。修：单边成本买=comm、卖=comm+stamp；滑点只留 `exec_price` 一处。
- **B3 · P1 · `:223,231-233` `bought_dates` 按"股"存字符串 + `list.pop(0)` O(qty²)**：买 10000 股塞 10000 个字符串，卖出 `pop(0)` 每笔 O(qty)；叠加 `:196` 每次撮合全量扫描 → 多股票长周期回测卡死/OOM。改存 `[(date,qty),...]` 批次。
- **B4 · P2 · `:241-245` 停牌股市值按 0 计**：`idx is None: continue` 让停牌日市值凭空消失，净值假暴跌+次日假暴涨，`max_drawdown`/`sharpe` 污染。应沿用最后有效收盘价。
- **B5 · P2 · `:118-138,178-181` 最后交易日挂单永不撮合；涨跌停直接 `continue` 丢单**（未进 `still_pending`，与"未成交递延次日"承诺不符）。

### 日内回测 `intraday.py`
- **I1 · P0 · `:78-101` 信号根 K 线当根成交 + 同 bar 反手**：`mom` 由 `bar["close"]` 算出随即以同一 `close` 建仓（收盘价确定瞬间无法以此价成交）；且平仓判断在建仓前，同一根 bar 可"同价卖→同价买"绕开 T+1。修：信号在 bar `i` 生成，成交用 `kline[i+1]["open"]`。
- **I2 · P1 · `:86-89` 止盈止损只看 close 不看 high/low**：盘中最高已触 +2% 但收盘回落判未触发，止损同理 → 回测系统性偏乐观。必须用 `bar["high"]/bar["low"]` 触价判定。
- **I3 · P1 · 分钟数据未复权（配 `adapters.py:829-836`）**：新浪分钟线为**不复权**价，日线链路走前复权；除权日产生数十个百分点假跳空，直接触发全部止损。修：按复权因子还原或明示 `adjust:"none"` 并禁跨除权日回测。
- **I4 · P2 · `:173` 组合收益率分母错误**：`totalReturn = totalPnl / sum(entry_price*shares)`，分母是所有交易累计投入（同一笔资金反复计），交易越频收益率越被摊低。应以峰值占用资金或初始资金为分母。
- **`routers/intraday.py:28-39` 参数零校验（P2）**：`stopLoss` 填正数 `0.01` 时 `收益 0<=0.01` 恒成立，每笔次日首根即平，返 200 的错误回测。

---

## 三、ML 全链路（量化致命 + 功能）

### P0（见速览表）
- `ml.py:1094-1100` 回测用未来价量筛样本（t 日决策读 t+n 收盘/成交，剔除跌停/停牌极端亏损样本 → 删负尾虚高）。修：跌停价顺延持有、停牌期冻结，均不得剔除。
- `ml.py:106` `/api/ml/optimize` 100% 502（`OptimizeMlBody` 无 `selectedFactors` 字段 → AttributeError 被裸 except 吞成"数据集构建失败"，误导排查）。
- `ml.py:1397-1426` 规则走 `eval` RCE（见一-P0）。

### P1
- **`ml.py:379-380` gap 与持有期 n 完全解耦 → 标签泄漏**：`evaluate_dataset(..., gap=5)` 签名拿不到 `n`；标签是 `closes[i+n]/closes[i]-1`，purge 至少需 `gap_days ≥ n`。实测 n=10 时训练标签覆盖到 D0069 与测试期重叠 5 天。修：`gap_days = max(gap, n)` 且把 n 传入。
- **`ml.py:348-362` 第 0 折丢弃 + OOS 仅覆盖 16%**：`train_end_date_idx = k*0` → k=0 训练集空被 `continue`，300 日/5 折只得 4 折；每折测试窗 4%，合计 OOS 仅 16%，**最后 43 日（最近段）从未进测试集**，`oosSharpe` 建立在这 16% 上。修：折起点 `(k+1)*fold`，测试窗取下一整折滚动到末尾。
- **`ml.py:538/554/598` Optuna 同折调参又报该折为 OOS**：30 次 trial 在同一 test_idx 取最大值，`oosSharpe`=选择偏差后的样本内指标，与 docstring"杜绝全样本调参后宣称高收益"自相矛盾。修：调参用 IS 内再切 valid，留未参与寻优的 holdout 报 OOS。
- **`routers/ml.py:303` ML 回测默认 `hist=180` → 仅约 7 个调仓日**：`dist_52w_high/low` 需 240 根热身，`range(60, len-n, n)` 中 t<240 截面全被因子缺失跳过，外推年化收益/Sharpe/Calmar/最大回撤全部无意义且不报错。修：改 `ML_DEFAULT_HIST(1024)`，`rebalanceCount<20` 时显式警告。
- **`ml.py:1038-1044,215-223` 快照因子前视**：回测侧 `snapshot_vals` 对所有调仓日复用"今天"的 ROE/PE/主力净流入；而推理 `score_latest/score_codes` 取当日快照 → 线下 IC 高、上线即衰减。修：财务/资金流按 report_date 滞后一个披露期对齐，`/evaluate /train` 响应也回写 `snapshotWarning`。
- **`ml.py:1265-1292` `score_codes` 缺 `_enrich_pool_extra`**：含财务/资金流因子的模型在盯盘路径上 `snapshot_factor_value` 返 None → `return` 丢弃每一只 → `return []`，调度器永远收不到信号，无日志无异常。修：复用 `_enrich_pool_extra`；补齐失败应抛错而非返空。
- **`ml.py:961,1333` 回测重排特征列 + 导入不校验顺序**：`tech_keys=[k for k in feature_names if k not in snap_set]` 后再 `fvals=tech+snap`，而 `score_latest:904` 按 `feature_names` 原序；外部模型若交错命名 → 因子错位喂入无报错。修：回测也按原序取值；导入校验并落盘规范化顺序。
- **`ml.py:422-424,441-443` IC/分层收益跨日期混池**：把不同日期样本混排求 pearson 当 IC，时序方差被当截面预测力，IC 虚高；`evaluate` 与 `backtest` 的 IC 口径差一个量级（后者按日算）。修：复用按日聚合算 meanIC/ICIR/分层收益。
- **`ml.py:1397-1400` 规则解析裸子串替换 → 静默失效**：`momentum120>0.1`→`f[2]120>0.1`、`volatility60<0.3`→`f[8]60<0.3`、`eps>1`→`f[41]s>1`，短 key 先于长 key 命中前缀；eval 抛错后逐样本 `keep.append(i)` → **规则等于没写，界面却显示已生效**。修：按 key 长度降序 + `\b` 词边界正则；解析失败必须抛错。
- **`ml.py:1466` `feature_importances_` 定义成方法非属性** → `load_model_meta:690` `imp[i]` 抛 TypeError 被 `:694` 吞；`import_model_file:1357` 同写法无 try → 导入此类 bundle 直接 500。
- **`ml.py:1489/1497` `create_manual_model` bundle 存全量 `MANUAL_FEATURES`、meta 只存 `list(valid.keys())`** → `adjust:212` 按 meta 校验，未赋权因子无法调参，前端展示与真实入模特征不符。
- **`train_final_model` 无超参入参** → `/optimize` 产出的 `bestParams` 无处可传，调参闭环断裂。
- **`ml.py:1459` `_ManualModel.predict` 当前 batch z-score**：盯盘单只股票时 `sd=0→1, Z=0`，得分恒等于 threshold。

### P2
- **`ml.py:157` 候选池幸存者偏差未覆盖训练路径**：`build_dataset` 用当日在市列表回溯历史，训练/评估无声（警告只在回测结果出现）。
- **`ml.py:307-326` 旧 `purged_walk_forward_split` 死代码仍导出**：按样本序号 gap、同日 train/test 必重叠，应删除避免被误调用复活。

---

## 四、风险模型与组合优化

### `risk.py`
- **R1 · P0 · `:210`（同 `routers/risk.py:68`）`np.var` 的 `ddof` 用反**：`np.var(resid, ddof=max(1, len(resid)-k-1))`，分母 `N-ddof` 实际变 `k+1`；N=100,k=6 时除 7 而非 93，**特质方差放大 ~13 倍**，specificRisk/factorRiskPct 全错。修：`ddof=k+1`。
- **R2 · P0 · `:42-51` 快照因子前视**：`turnover/ep/bp/size` 取自当前 quote，却在每个历史截面复用 → 用今天估值预测半年前收益，污染 `betas→sigma_f→` 全部归因。
- **R3 · P1 · `:62-73` 截面回归按数组下标对齐不同长度股票**：停牌/上市晚的股票同一 `t` 对应不同日期，跨日期收益混进同一截面（同病 `estimate_specific_variances:199`）。修：先按 date 求交集建统一日历。
- **R4 · P1 · `:207` vs `:77` 标准化口径不一致**：`build_style_panel` 对截面 zscore，`estimate_specific_variances` 对时序 zscore，再相乘求残差，尺度不同残差含义错。
- **R5 · P1 · `:17,100-111` 无行业因子、无协方差收缩**：行业收益全落"特质风险"→风险被低估；`np.cov` 裸估计，无 Ledoit-Wolf、无 PSD 截断；routers 只取 60 日 K 线估 6×6 矩阵，可能非正定 → `port_exposure@sigma_f@port_exposure` 出负方差被 `max(0,·)` 吞。
- **`:43-48` 用户权重静默丢弃**：`codes_valid` 过滤后长度与 `weights` 不等 → 无声退等权，前端归因与真实组合无关且无提示。修：按 `codes.index(c)` 取权重、过滤后重归一、响应回传 `droppedCodes`。
- **`:75` 持仓 <10 只必 422 且文案误导**：要求 `len(rows)>=10` 才回归，模拟盘 3~8 只永远不可用，提示却写"需≥3只"。
- **`:99` 权重用成本价、收益只用当日涨跌（P2）**：`mv=qty*avg_cost` 应为 `qty*price`；`stock_returns` 当日涨跌 vs `factor_returns` 历史平均，期数口径不一致。

### `portfolio_opt.py` / `routers/portfolio_opt.py`
- **`:21-22` `_normalize` 抹掉空头腿**：`w=np.clip(w,0,None)` + `s<0` 时 `w_raw/s` 整体翻符号，`long_only=False` 多空组合实际不可用。修：`long_only=False` 时按 `sum(|w|)` 归一并跳过 clip。
- **`:63-65`（同 `max_sharpe:84`/`risk_parity:103`）约束不可行静默回退**：`n*max_weight<1` 时 `sum(w)==1` 必不可行，`w.value is None` → 返等权 0.2 **反而违反用户 0.1 上限**，调用方无察觉。修：检查 `prob.status` 抛错或带 `fallback=True` 标记。
- **`routers:130` 对齐取最旧段**：`closes={c:[k["close"] for k in series[c][:min_len]]}`，K 线升序 `[:min_len]` 取最早段，各股长度不同 → 收益矩阵按日期错位相关（`routers/risk.py:134` 用正确的 `[-min_len:]` 自相矛盾）。修：按 date 取交集。
- **`routers:142 + portfolio_opt.py:49` 小样本协方差奇异**：样本门槛仅 `len>=20`，N>T 时 `np.cov` 秩亏 → cvxpy `quad_form` 抛 `DCPError` 被吞回退等权；`mu` 纯历史均值年化，**ML 预测完全未接入**。修：加 Ledoit-Wolf 或 `psd_wrap`，要求 `T>=2N`，`/estimate` 增加可选 ML mu 来源。
- **`routers:78-82` 总资产估算漏算存量持仓**：不在 `body.codes` 里的持仓 `price=0` → `market_value` 低估 → `total` 偏小 → 每只少买；且 `total` 含市值但资金只有 `cash`，订单撞"资金不足"被 `:97` 异常吞进 `applied[].error`，HTTP 200 前端显示"已建仓"。修：对 `set(codes)|set(持仓)` 一次性取价；失败项返 `partial:true`。

---

## 五、因子与表达式引擎

### `factor.py`
- **行情失败静默变空（P1）**：`try: quotes=fetch_tencent_quotes(...); except: quotes={}`，腾讯挂掉时 `pctChg/pe/pb/mktCap/turnover` 全 None、name 退化成 code，HTTP 200，前端"有表无数"无法区分数据源故障。修：加 `dataSourceError` 或 502。
- **K 线串行拉取（P1）**：`for code in code_list: await fetch_kline(code,260)`，async 路由逐只 await，50 只=50 次串行 HTTP（含重试退避）；同项目 `risk.py:36` 已用 `asyncio.gather+Semaphore` 此处没用。
- **单因子回归同 bar 成交 + 伪 IC（P1）**：`factor["calc"](kline,i)` 用 `close[i]`，`fret=closes[i+n]/closes[i]-1` 同用 `close[i]` 起算 → 看到收盘价同时以该价成交；`xs/ys` 跨股票跨日期混池求 pearson 当 `ic/rankIc`，被全市场同涨同跌主导把 beta 暴露误报成因子有效性，且重叠窗口使有效样本远小于 `sampleSize`，`r2` 显著性虚高。修：因子取 `i`、收益取 `close[i+1+n]/close[i+1]`；IC 按日截面相关求均值+ICIR。

### `factor_expr.py`
- **F1 · P1 · `:145-151` 只处理 NaN，inf 放行**：`operator.truediv` 除零得 `±inf` 逃过 `np.where(isnan)` 进入排序，**inf 股票恒排第一**；两操作数皆 `ast.Constant`（如 `1/0`）是纯 Python 除法抛 `ZeroDivisionError` 未捕获 → 500。修：`np.nan_to_num(...,posinf=0,neginf=0)` + 安全除。
- **F2 · P1 · `:58-66` `_rank` 把 NaN 排最高分**：`np.argsort` 将 NaN 置尾得 `rank=1.0`（最优），缺失数据股票被选出；且与 `factors.py:34` `rank_of` 并列处理不同，同一因子两条路径打分不一致。
- **F3 · P2 · `:20-28` `_ALLOWED_NODES` 白名单定义后从未引用**：`validate_expression` 只跑黑名单，错误在求值期而非校验期暴露（用户拿 500 非 400）；`ast.Call` 未校验 `keywords`。修：真正启用白名单。
- **F4 · P2 · `:121` NaN/缺失一律填 0**：0 非中性值（rsi=0 极端超卖、pe=0 异常），缺失股被赋有偏极端分。应剔除或填截面中位数。

### `factors.py`
- **N1 · P2 · `:704-706` `bucket_index` 并列全进最高组**：`pos=sum(1 for v in sorted if v<=value); idx=int(pos*groups/n)`，`<=` 使全等时 `pos=n`→`idx=groups` 被 clamp 到 `groups-1`，**所有股票进同一组**，分层回测该日多空为空不报错。修：`np.searchsorted(side='left')` 或并列取平均秩。
- 已核对无误：`volatility/rsi/amplitude/obv_trend/vol_corr/volume_ratio` 系列窗口边界与 `numpy_factors.py` 逐点版完全一致，无前视；`macd` 全序列回填正确。

---

## 六、报告、行情与策略接口

### `reports.py`
- 越权 + HTML 导出 100% 失败（P0，见一）。
- **`:119-124` `_get_run` 只扫最近 200 条** → 老报告（第 201 条起）永久 404 且删不掉（记录残留）。修：`db.get_backtest_run(run_id, user_id)` 走 `WHERE id=? AND user_id=?`。
- **`:100` `ext` 判定写错**：`"excel" in media_type`，实际 media_type 是 `spreadsheetml.sheet` 不含 "excel"，xlsx 被命名成 `.pdf`（前端自带 filename 掩盖）。

### `quote.py` / `data.py`
- **`adapters.py:1258-1297`（quote.py:91-98 透出）分时图跨日 + 均价跨日累计**：A 股一日恰好 240 根 1 分钟 K 线，"最近 240 根"必含昨日尾盘；日期 `[:5]` 截断后昨日 14:00 与今日 14:00 图上重叠；`cum_amount` 跨日累计使均价黄线严重失真（分时均价须当日归零）。上游异常时 `arr=[]` 返 200 空数组，前端显示空白图当正常。（**用户 2026-08-07 复核确认：前端分时 tab 代码已存在 `QuoteView.vue:373,149,255-390`，但因本接口缺陷显示空白/失真，详见文首 C 节——并非"没加分时图"**）。修：按 `day[:10]==最新交易日` 过滤后再累计；空结果显式报错。
- **`strategies.py:60-81` `except TypeError` 抓不住 Pydantic `ValidationError`** → 旧配置字段不匹配时直接 500+堆栈（P1）。修：`except (TypeError, ValueError, ValidationError)`。
- **`strategies.py:135` `/api/backtest-runs` 登录后永远空**：存档入口（`selection.py:672`/`ml.py:1241`）调 `store_backtest_report` 不传 user_id，全落 `user_id=0`；登录用户查 `uid=5`→0 条，匿名 `uid=0`→可见所有人（P1）。修：存档透传真实 uid，列表口径统一。

---

## 七、任务调度与参数寻优

### `jobs.py` / `optimize.py`
- **取消按钮是空开关（P1）**：`routers/jobs.py:71` 用 `loop.run_in_executor(None, optimize.optimize_backtest, ...)`，`Task.cancel()` 无法中断线程；点了取消状态立刻变 cancelled，Optuna 仍后台跑满 CPU 直到自然结束。修：传 `threading.Event`，在 trial 回调检查并 `raise TrialPruned`。
- **重计算占满默认线程池拖垮全站（P1）**：`run_in_executor(None,...)` 用的是 asyncio 默认 executor，与 FastAPI 同步路由（`/watchlist`/`/portfolio`/`/auth/*` 全是 `def`）同一批线程；几个 optimize 任务即耗尽线程，整站假死。修：单独 `ThreadPoolExecutor(max_workers=2)` 或 ProcessPool。
- **`optimize.py:98-99` IS/OOS 两段非互斥**：`endDate<=mid` 与 `startDate>=mid` 都含 `mid_date` 当天；OOS 起点应取 `mid_date` 下一交易日。
- **`optimize.py:91` `study.best_trial` 全失败时抛异常但 `_objective` 返 `-1e9` 恒 COMPLETE** → 把 `Sharpe=-1e9` 的垃圾参数当最优回写策略表。修：过滤 `value<=-1e8` 并在全失败时报错。
- **`optimize.py:52-53` 产物落盘异常全吞（P2）**：`except Exception: pass`，artifact 保存失败无日志无字段。

---

## 八、修复优先级建议

**第一批（P0，致命或安全）**：〇-表 1~10。
1. 回测未来价量筛样本（数据造假）
2. 日内同 bar 成交+反手（绕 T+1）
3. 事件回测无现金校验（无限杠杆）
4. ML rule `eval` RCE
5. `/api/ml/optimize` 全挂
6. 风险 `ddof` 反 + 前视（归因全错）
7. 报告越权 + 导出失效
8. 组合优化无鉴权下单
9. 登录撞库防护
10. 任务取消空开关 + 线程池耗尽

**第二批（P1，严重失真/不可用）**：CV gap 与 n 解耦、OOS 仅 16%、Optuna 同折报 OOS、ML 回测 hist=180 仅 7 调仓日、score_codes 静默空、特征列重排错位、规则解析静默失效、滑点翻倍、bought_dates O(qty²)、分钟未复权、组合优化静默回退等权/协方差错位/权重丢弃、单因子伪 IC、串行拉取、timeshare 跨日、backtest-runs 永远空、ValidationError→500、保存策略 uid=0、鉴权不一致。

**第三批（P2，稳健性）**：NaN 填 0 / inf 放行 / 白名单未用、bucket_index 并列、停牌市值 0、未成交递延缺失、ext 判定、days 无校验、产物落盘吞异常、IS/OOS 重叠、best_trial 全失败回写。

> 全程未修改任何代码，仅定位与记录。如需针对任一条（尤其 P0 的回测数据造假、RCE、无限杠杆、归因 ddof）出最小修复补丁，可继续。
