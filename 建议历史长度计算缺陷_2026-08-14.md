# 建议历史长度计算缺陷（为什么三年算出 6203 日）

日期：2026-08-14
关联问题：U1（历史长度与时间段错配，同源缺陷）

---

## 1. 现象

在 ML 训练面板设置：

- 训练起始日 `2017/01/14`
- 训练结束日 `2020/01/14`（约三年，自然日跨度 ≈ 1096）
- 当前历史长度 `1500`

页面提示：

> 建议历史长度 ≥ **6203** 日（当前 1500 不足，请增大历史长度）

三年自然日 ≈ 1096 日，加上缓冲约 260 日也仅 ≈ 1356 日，**远不到 6203**。该数字明显错误，且导致历史长度被自动回填成一个荒诞的大值、提交按钮被禁用。

---

## 2. 根因（代码实证）

文件：`frontend/src/views/MLView.vue`，`suggestedHist` computed（第 59–72 行）：

```js
const suggestedHist = computed(() => {
  const today = new Date(); today.setHours(0, 0, 0, 0)
  let need = 0
  for (const [s, e] of [[btStart.value, btEnd.value], [trainStart.value, trainEnd.value]]) {
    if (!s && !e) continue
    const from = s ? new Date(s + 'T00:00:00') : today
    const to   = e ? new Date(e + 'T00:00:00') : today
    const backToFrom = Math.max(0, Math.floor((today - from) / 86400000))
    const extraEnd   = to < today ? Math.floor((today - to) / 86400000) : 0
    // 60 缓冲 + 因子最长回看 240 + extraEnd
    need = Math.max(need, backToFrom + 60 + 240 + extraEnd)
  }
  return Math.max(0, Math.ceil(need))
})
```

逻辑把 `trainStart`、`trainEnd` **都相对于今天（2026-08-14）**取距离，再相加：

| 项 | 计算 | 天数 |
|---|---|---|
| 今天 − 2017/01/14 | `(today − from)/86400000` | ≈ 3500 |
| 今天 − 2020/01/14 | `(today − to)/86400000` | ≈ 2403 |
| 缓冲 | 60 + 240 | 300 |
| **合计** | 3500 + 2403 + 300 | **≈ 6203** |

**问题在 `extraEnd` 这一行**：训练结束日早于今天时，它又加了一遍"今天到训练结束日"的距离。结果是把 2017→2026 和 2020→2026 两段距离叠在一起，而不是按所选的**训练区间（2017→2020）**来算。

---

## 3. 正确口径

合理的建议值应当覆盖**所选训练区间** + 因子回看缓冲：

```
正确建议值 = (trainEnd − trainStart) / 86400000 + 缓冲(≈260)
          = 1096 + 260
          ≈ 1356 日
```

最多也就是从今天退回训练起始日（覆盖到区间起点之前的全部样本）：

```
(today − trainStart) / 86400000 + 260 ≈ 3500 + 260 = 3760 日
```

**绝不应把 `today−from` 与 `today−to` 两段同时相加。**

---

## 4. 影响

- 历史长度被 `watch(suggestedHist)`（第 76–78 行）自动回填成约 6203 的荒诞值，用户被迫"增大历史长度"；但后端 `hist` 仍**从今天往前抓**（U1 根因），实际根本覆盖不到 2017–2020 区间。
- `histInsufficient`（第 73 行）误判为 `true`，禁用"评估(CV)/训练并落盘"按钮，用户难以提交。
- 与 **U1（时间段与历史长度错配）同源**：前端与后端对"历史长度"的语义理解不一致，前端以为"覆盖所选区间"，后端实际"从今天往前 N 天"。

---

## 5. 修复建议（优先级 P0）与实施状态

前端 `suggestedHist` 已按此改为基于所选区间跨度：

```js
const spanDays = Math.max(0, Math.floor((to - from) / 86400000))
const need = Math.ceil(spanDays + 260)        // 区间跨度 + 缓冲
// 仅填起/止或留空时：from/to 缺省取今天，等价 (today - from)/86400000 + 260
```

- ✅ 已删除 `extraEnd` 这段重复加法。
- ✅ 自动回填 `watch` 沿用修正后口径，不再回填离谱大值。
- ✅ 后端 `hist` 窗口跟随结束日已确认存在（`ml.py` 的 `days_to_cover` + `fetch_kline_window`），并同步修复了后端双端日期超额抓取、仅 end 日期 gap 翻倍两处缺陷（详见《回测日期错配根因_2026-08-14.md》第 6 节）。

## 6. 本文件状态

- 诊断与修复均已落地（2026-08-14 下午）：`frontend/src/views/MLView.vue`（suggestedHist + 提示文案）、`backend/app/ml.py`（days_to_cover + 快照起点透明化）、`backend/app/reporting.py`、`backend/app/routers/reports.py`（报告渲染 `snapshotStartNote`）。
- 与项目根目录已有文档的关系：
  - 补充并具体化了《未解决问题清单_2026-08-12.md》中 U1 的**前端侧**根因。
  - 是《机器学习模块优化设计方案.md》U1 设计的前端实现细节之一。
