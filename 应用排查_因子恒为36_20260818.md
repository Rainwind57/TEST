# 应用排查记录：机器学习呈现因子恒为 36 个

日期：2026-08-18
范围：机器学习模块 / 因子选择 → 训练 → 模型特征呈现链路
结论：**不是"平台只支持 36 个因子"的硬编码限制，也不是子集筛选失效；而是两层真实问题叠加——① 你训练出的模型缺失 `.joblib` 训练产物（只剩元数据），UI 显示的 36 来自元数据里的默认全量技术因子；② 训练时未真正应用子集（默认"全选 36"），属 UX 设计陷阱。后端对因子子集的支持经端到端实测是正常工作的。**（对应 P1/P2/P3 修复已于 2026-08-18 落地，见第五节）

---

## 一、现象

用户在机器学习模块勾选因子后训练，最终模型"呈现的因子数"始终为 **36 个**，怀疑平台漏因子、有硬编码上限，或勾选没生效。

---

## 二、关键证据（实测，非猜测）

### 1. 后端完全支持因子子集（端到端生产路径实测通过）
复刻前端提交链路 `前端 evalConfig → 后端 EvalBody → jobs → build_dataset → train_final_model`：
- 选中 2 个因子（momentum / rsi）→ 落盘模型 `featureNames` 长度 = **2** ✓
- 选中 3 个 → 长度 = **3** ✓

→ **后端子集筛选正常工作**，选择只要传到后端就一定会生效。因子池技术因子共 36 个、快照因子约 20 个（合计 56 个，非 36 上限）。

### 2. 你的训练模型缺失 `.joblib` 训练产物（核心问题）
检查 `ml_models/` 目录：
- 你训练的 4 个模型（`mlmodel_20260818_094652 / 102215 / 114630 / 122831`）：**只有 `.json` 元数据，没有 `.joblib`**。
- 排查期间新建的 2 个测试模型（`mlmodel_20260818_135802 / 135952`）：`.json` + `.joblib` **都在**。

→ 你那 4 个模型**实际不可用**：打分/回测需要加载 `.joblib`，缺失即无法产出信号；UI 展示的"因子数 = 36"取自 JSON 元数据里的默认全量技术因子列表，**背后并没有真实模型**。

### 3. 你的模型元数据本身就是"全部 36 个技术因子"
读取你那 4 个模型的 `.json`：`featureNames` 实测为 momentum5 / momentum10 / momentum / … / vol_change5 共 **36 个技术因子**。说明训练时**没有应用任何子集**，直接用了默认值（全部 36）。

---

## 三、为什么"勾选了还是 36"——两层原因

**第一层（致命）：模型包丢失，UI 只能回退显示元数据。**
你的模型 `.joblib` 不在了（疑似被某次文件清理/重启删除旧的大文件），前端/后端读不到真实特征，只能拿 JSON 里的 36 个因子名展示。无论你当时选了几个，现在看到的都是这 36。

**第二层（UX 陷阱）：训练默认 = 全选 36，且无任何提示。**
- 因子面板默认"什么都不勾 = 使用全部 36 个技术因子"（代码注释即"未传则默认使用全部"）。
- 面板头部显示 `因子选择 (X/56)`，但 X=0 时训练仍会用满 36（不勾快照时），**没有任何"将使用 N 个因子训练"的提示**。
- 若你点过"全选"且"含全部快照因子"未勾，则选中的是全部技术因子 = 恰好 36。

→ 极易在"没主动收窄 + 无提示"的情况下训练出 36 因子的模型，让人误以为"勾选没用"。

---

## 四、影响

- **你的 4 个训练模型目前不可用**：缺 `.joblib`，打分/回测会失败或无信号（与之前发现的"盯盘扫不出信号"无直接关系，但是独立的功能性故障）。
- **UI 显示的因子数不可信**：现在看到的 36 是元数据默认值，不代表真实模型特征。
- 重新训练（带子集）可正常产出对应因子数的模型并落盘 `.joblib`，但需注意旧模型包丢失问题是否会复发。

---

## 五、修复建议（2026-08-18 已全部落地）

### P1（功能性，优先）：模型包 `.joblib` 丢失 — ✅ 已落地
- 排查 `ml_models/` 是否被某清理逻辑（如 `safe-delete`、重启脚本、定时任务）删除旧 `.joblib`：**全仓 grep 未发现任何删除 `ml_models/` 文件的清理逻辑**，丢失大概率来自人工/外部操作，非平台内定时任务。
- 训练落盘增加"写 JSON 后校验 `.joblib` 存在"的断言，缺失则明确报错而非静默成功。
  - 新增 `ml.py::_ensure_artifact_exists(path)`：落盘后校验产物存在且非空，缺失即抛 `RuntimeError`。
  - 覆盖 `train_final_model`、`create_manual_model`、`clone_model_with_adjust`、`import_model_file` 四个落盘入口。

### P2（UX，防再犯）：因子选择无提示 — ✅ 已落地
- 训练按钮旁实时显示"将使用 **N** 个因子训练"（N = 勾选数，0 时提示"未选=全部技术因子"）。
  - 新增 `MLView.vue::effectiveTrainFactors` 计算属性，按技术/快照拆分统计；快照因子在开关未勾时不计入并提示"需勾选开关"。

### P3（可发现性）：快照因子 — ✅ 已落地
- "含全部快照因子"开关后的约 20 个基本面/资金流因子在**不勾开关时也可见**（面板已默认展示全部 56 个因子），并新增：
  - 面板顶部提示"技术因子 N 个 + 快照因子 M 个"；
  - 开关未勾时快照因子分组整体置灰并打标"快照，开关未勾不生效"，避免强化"只有 36 个"错觉。

---

## 六、给你的验证方法

1. 现在重新训练一个模型（先主动勾 3~5 个因子）→ 训练完成后到 `ml_models/` 看是否同时生成 `.json` + `.joblib`；模型详情应显示你勾选的因子数（非 36）。
2. 若重新训练后 `.joblib` 仍会"过一段时间消失"，即可确认是文件清理在删 `ml_models/`，需按第五节 P1 处理。

---

## 七、关联说明

- 与之前记录的盯盘扫描崩溃 bug、持仓行内买卖缺失**无关联**。
- 本排查最初"只诊断不改"，2026-08-18 已按第五节 P1/P2/P3 落地代码修复（后端 `ml.py` 落盘校验 + 前端 `MLView.vue` 因子提示/快照可见性），并已通过后端 pytest（27 passed）与前端 `npm run build` 验证。
- 之前 08-11～08-17 的 6 份旧诊断文档已过时，与本问题无关。
