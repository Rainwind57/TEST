# 应用改进方向（基于 08-11～08-17 诊断）

> 本文档汇总「机器学习模块」「盯盘模块与选股一致性」两轮诊断中已确认的问题，并给出可落地的改进方向，按优先级 P0 / P1 / P2 排列。
> 文档只描述**现象**与**改进方向**，不贴完整代码；仅保留少量定位线索。所有改进均为建议，未改动任何业务代码。

---

## 一、机器学习模块改进方向

### P0（高优先，直接消除“对不上 / 看不懂”）

**1. 前端回显“实际数据起止日 / 实际生效历史长度”**
- 现象回顾：用户设置的时间段与历史长度经常“对不上”。底层存在多处对“历史长度”的静默处理——历史天数被钳制到下限之上、K 线按最近 N 天拉取、快照类因子的起始日被前移，而前端界面从不回显真实生效值，用户无法知道模型到底用了哪段数据。
- 改进方向：后端在回测 / 训练响应中返回 `data_start`、`data_end`、`effective_hist_days` 等字段；前端结果页明确展示“实际数据区间”，并在发生钳制时给出提示。
- 预期效果：用户一眼看到真实数据范围，消除“时间段 / 历史长度对不上”的困惑。

**2. 回测报告增加“仓位变化过程”图**
- 现象回顾：现有报告已有分组收益、多空对冲、IC 三张图，但回测引擎并不计算逐日持仓序列，因此没有“仓位变化过程”曲线，用户看不到调仓节奏与暴露变化。
- 改进方向：在回测引擎中记录逐日多头 / 空头 / 净持仓比例（或信号强度），报告中新增一张仓位走势图。
- 预期效果：直观呈现持仓随时间的变化，便于判断策略稳定性。

### P1（中优先）

**3. 手动调参初始值不应全为 1**
- 现象回顾：调参面板因子权重初始值在无已存权重时统一取 1，导致“调参起点”并不反映模型真实状态。
- 改进方向：初始值优先取模型已保存的因子权重；无权重时给中性默认值并在界面上标明“未调参 / 默认”。
- 预期效果：调参起点真实可信，避免误以为“已合理初始化”。

**4. 调参后支持“另存为新模型”**
- 现象回顾：人工调参后结果覆盖原记录，调参面板没有“保存为新模型”的入口，无法保留基线模型做对比。
- 改进方向：调参面板增加“另存为新模型”按钮，生成新的模型 id 并落盘。
- 预期效果：可保留多个调参版本，便于横向对比。

**5. 明确 / 补齐平仓、调仓信号**
- 现象回顾：机器学习回测只计算分组多空收益，不产出离散的逐日买卖信号；全仓模式也没有“调仓”这一信号类型，用户在前端找不到平仓 / 调仓信号。
- 改进方向（二选一）：
  - a) 引擎可选输出逐日离散信号（多 / 空 / 平 / 调仓）；
  - b) 在报告与界面明确说明“本模式产出分组 / 多空收益，而非逐日买卖信号”。
- 预期效果：消除“看不到平仓 / 调仓信号”的疑惑。

### P2（低优先，健壮性与偏差）

**6. 快照因子前视与生存偏差**
- 现象回顾：快照类因子存在前视风险，且对退市 / 停牌股票的过滤不够充分，可能引入生存偏差。
- 改进方向：因子计算严格对齐交易日刻度；引入退市 / 停牌过滤，保证回测样本与真实可得标的一致。

---

## 二、盯盘模块与选股一致性改进方向

### P0（高优先）

**1. 盯盘“模型模式”透传调参 / 板块 / 候选池参数**
- 现象回顾：盯盘的模型模式在评分时只传入模型 id 与固定候选池大小，没有把方向（direction）、板块筛选、调参参数、自定义候选池规模透传过去。这是导致“盯盘信号与选股结果不一致”的核心根因。
- 改进方向：将选股 / 模型的完整配置（方向、板块、调参、候选池规模）透传到盯盘评分链路，使盯盘与选股同源同参。
- 预期效果：盯盘信号与选股结果保持一致。

**2. 打分选股增加 direction 下拉**
- 现象回顾：打分选股入口不传方向参数，无法限定“只做多 / 只做空”，与“只做多模型”等设定无法对齐。
- 改进方向：选股入口增加方向下拉，并将所选方向透传到引擎。
- 预期效果：选股结果可受方向约束，与模型设定一致。

### P1（中优先）

**3. 盯盘候选池来源可配置**
- 现象回顾：盯盘默认候选池（如自选）与选股（全市场）不同源，范围不一致会直接造成结果差异。
- 改进方向：盯盘模型模式支持配置候选池来源（自选 / 全市场 / 指定板块）。
- 预期效果：用户可手动对齐盯盘与选股的股票范围。

**4. 默认引擎差异说明**
- 现象回顾：盯盘默认是规则模式（动量 + RSI），与选股使用的 ML 引擎不同源，二者天然可能给出不同结论。
- 改进方向：在文档 / 界面明确说明差异，并提供“与选股同一引擎”的开关选项。
- 预期效果：避免用户把两种不同机制的产出直接对比。

**5. 信号类型语义统一**
- 现象回顾：各模块信号类型不统一，全模块都没有“调仓”这一信号类型，用户不易理解每个模式输出的是什么。
- 改进方向：统一各模式的信号语义（看多 / 看空 / 中性 / 平仓 / 调仓），或在界面说明各模式信号含义。
- 预期效果：信号含义清晰、可跨模块对照。

---

## 三、已确认无需改动（避免重复劳动）

以下项目在代码核查中已确认**正确或已修复**，无需作为缺陷处理：

- **Purged Walk-Forward** 已演进为按交易日隔离（gap_days），并非旧版整段切断。
- **年化换算** 已使用 `ppy = 252 / n` 修正。
- **指数被选入** 已加过滤。
- **long_only（只做多）全链路已生效**：前端方向下拉 → 任务透传 → 引擎裁剪空头侧并做归零保护，设为只做多模型完全可用。
- **寻找模型列表无筛选、打分不传 direction** 属于“功能缺失”而非“bug”，对应上文改进项 2。

---

## 四、建议落地顺序

1. **P0 × 3**：数据区间回显、仓位变化图、盯盘透传参数 —— 直接消除最大的“对不上 / 看不懂 / 不一致”问题。
2. **P1 × 4**：调参初始值、另存新模型、候选池配置、信号说明。
3. **P2 × 1**：快照因子前视与生存偏差。

> 说明：以上均为改进建议，当前业务代码未作任何修改。若需要，可针对 P0 三项给出最小改动方案。
