# 待解决事项（2026-08-19）

> 来源：核查《应用排查_快照因子无法使用_20260819.md》《应用实跑排查_量化全功能极端数据_20260819.md》两份文档后，列出尚未解决或需人工确认的事项。

## 一、代码层尚未解决

### 1. 规则选股路径与 ML 路径对非法因子键行为未对齐（P0 残留）
- **现状**：ML 路径已从"静默丢弃"改为"告警 + 回显"（`backend/app/ml.py:294-305`）；规则选股路径仍显式 400（`backend/app/routers/selection.py:190-191`）。
- **影响**：同一非法键（如 `pe_ttm`），规则选股直接报错，ML 训练只是告警后忽略，两条路径行为相反，易致用户困惑。
- **待办**：决定是否统一。若改为告警，改动点仅在 `selection.py:189-191` 一处，将 `raise HTTPException(400)` 改为过滤未知键并 `logger.warning`。

### 2. 下单全 422 "暂无行情数据"（P1，待 Web 实测确认）
- **现状**：探针独立进程直调路由函数，所有"需要行情"的下单（含正常买入、无持仓卖出、资金不足）均返回 422 "暂无行情数据，请稍后重试"。curl 实测腾讯/新浪行情接口均正常返回价格 → 网络可达。
- **怀疑**：探针环境缺失行情客户端运行时初始化，疑似误报。
- **待办**：在 Web / UI 实际下单一次确认。若 UI 也 422，则为真 bug——错误信息把"无持仓 / 资金不足"等业务原因也掩盖成"暂无行情数据"，需修正错误码与提示。

### 3. 参数寻优在异步上下文事件循环冲突（P2，待 Web 实测确认）
- **现状**：探针在 `async` 主函数里调同步的 `optimize_backtest` 报 `Cannot run the event loop while another loop is running`；同步方式直调则成功。判定属探针调用方式误用，非应用 bug。
- **风险**：`optimize_backtest` 内部若用 `asyncio.run` / `loop.run_until_complete`，在 FastAPI 异步路由（已有 running loop）中调用可能复现冲突。
- **待办**：前端实际触发一次参数寻优，确认 Web 路径不报此错。

### 4. ML 回测成功不返回 `ok` 字段（P3，脆弱契约）
- **现状**：成功回测无 `ok` 字段，仅失败置 `ok:false`；前端用 `=== false` 判断，当前不误判。
- **待办**：建议成功时统一返回 `ok:true`，消除脆弱契约。

### 5. 规则回测 `hist=3000` 年化指标为 None（P3）
- **现状**：超长窗口回测可跑出 979 次调仓，但年化收益率为 `None`。
- **待办**：确认极端长窗口下年化指标计算逻辑，避免指标缺失。

## 二、数据源层尚未验证

### 6. 北向持股因子接口实测（快照因子根因 2）
- **现状**：`fetch_north_holding` 已改为双源兜底（`adapters.py:1065-1115`：主源 `RPT_MUTUAL_HOLDSTOCKNORTH_STA` + 回退 `RPT_MUTUAL_HOLDSTOCKS_DETAILS`）。
- **待办**：仍需实测确认双源中至少一个能取到 `north_holding_pct` 真实值。若两源均空，说明 reportName / 字段已变更或需额外参数，需重查东财接口。

### 7. 行情快照字段映射未覆盖（快照因子次要问题）
- **现状**：`pct_chg`、`mkt_cap`、`circ_mkt_cap` 在本机实测返回 None，疑似快照字段映射未覆盖。
- **待办**：核对实时报价快照字段名与映射，补齐缺失字段。

## 三、已解决（供对照，无需再处理）

| 项 | 位置 | 状态 |
|---|---|---|
| 财务因子市场标识拼错（`600519.1` → `600519.SH`） | `adapters.py:1034-1038` `fetch_finance_summary` | 已修 |
| ROA 字段 ZZCJLL 误用 | `adapters.py:1049` | 已修 |
| 北向持股单一接口失效 | `adapters.py:1065-1115` | 已改双源兜底 |
| ML 路径非法键静默丢弃 | `ml.py:294-305` + `routers/ml.py:53-58` | 已修（告警+回显） |
| 快照总开关 UX 陷阱 | `ml.py` snapshot_ignored_keys 回显 | 已加告警旁路 |

## 优先级建议

1. **P1 下单 422**：影响核心交易功能，最优先 Web 实测。
2. **P0 残留**：行为对齐，改动小，可快速决策。
3. **数据源实测（北向）**：影响北向因子可用性。
4. **P2 / P3**：均为边缘场景，可缓。
