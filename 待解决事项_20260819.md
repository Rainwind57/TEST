# 待解决事项（2026-08-19）

> 来源：核查《应用排查_快照因子无法使用_20260819.md》《应用实跑排查_量化全功能极端数据_20260819.md》两份文档后，列出尚未解决或需人工确认的事项。

## 一、代码层尚未解决

### 1. 规则选股路径与 ML 路径对非法因子键行为未对齐（P0 残留）✅ 已修
- **已修复**：规则选股路径改为与 ML 路径一致——过滤未知键 + `logger.warning`，不再 400（`backend/app/routers/selection.py:189-195`）。
- **改动**：`raise HTTPException(400)` 改为 `logger.warning` + `body.factors = [s for s in body.factors if s.key not in unknown]`。

### 2. 下单全 422 "暂无行情数据"（P1）✅ 判定为探针误报
- **核查**：`portfolio.py` 中无"暂无行情数据，请稍后重试"文案。实际行情相关 422 仅两处：`portfolio.py:96`（未获取到报价）和 `:98`（价格为 0）。
- **初始化结论**：行情 HTTP 客户端懒创建（`adapters.py:41-49`，按 event loop id 建 `httpx.AsyncClient`），与 `main.py` startup 无关（startup 仅 `db.init_db` + `auth.ensure_secret_persisted`）。探针直调函数不会因缺初始化而 422。
- **判定**：探针误报（文案与代码不符）。
- **已修（价格吞 0 隐患）**：`_parse_tencent`（`adapters.py:139-163`）、`_parse_sina`（`adapters.py:193-201`）、`_parse_eastmoney`（`adapters.py:235-246`）原本 `f(x) or 0.0` 会把 "0.00"/空解析成 0 → 触发 `portfolio.py:98` 422。已改为显式区分「解析失败 None」与「真 0」，仅解析失败时兜底 0.0。

### 3. 参数寻优在异步上下文事件循环冲突（P2）✅ 代码已防护
- **核查**：`optimize.py:87-99` 已内置防护——`optimize_backtest` 检测到当前线程有 running loop 时，改走独立线程执行 `_optimize_backtest_sync`，避免 `Cannot run the event loop while another loop is running`。FastAPI 异步路由场景已被覆盖，无需再 Web 实测。

### 4. ML 回测成功不返回 `ok` 字段（P3）✅ 已在代码中正确
- **核实**：成功分支在 `backend/app/ml.py:1976` 已 `result["ok"] = True`；失败分支在 `ml.py:1766-1782` 置 `ok: false`。文档 P3 "成功回测无 ok 字段"描述不准确——代码已正确返回 `ok:true`。

### 5. 规则回测 `hist=3000` 年化指标为 None（P3）✅ 当前代码已修复
- **核查**：`annualized_return`（`factors.py:719-736`）当前实现不会返回 None。`cum<=0` 时走约化收益分支 `(cum-1)/years`（`factors.py:730-731`），溢出分支也有兜底。
- **根因**：文档 P3 的 None 症状对应旧版负底数分数次幂产生复数 → JSON 序列化为 null 的经典 bug。当前源码注释（`factors.py:728-729`）明确描述并已修复。
- **结论**：代码无需改动。若线上仍报 None，需确认部署的是否为旧版（未含此修复）。

## 二、数据源层尚未验证

### 6. 北向持股因子接口实测（快照因子根因 2）
- **现状**：`fetch_north_holding` 已改为双源兜底（`adapters.py:1065-1115`：主源 `RPT_MUTUAL_HOLDSTOCKNORTH_STA` + 回退 `RPT_MUTUAL_HOLDSTOCKS_DETAILS`）。
- **待办**：仍需实测确认双源中至少一个能取到 `north_holding_pct` 真实值。若两源均空，说明 reportName / 字段已变更或需额外参数，需重查东财接口。

### 7. 行情快照字段映射未覆盖（快照因子次要问题）✅ 判定非映射不匹配
- **核查**：`_SNAPSHOT_FIELD_MAP`（`factors.py:587-597`）映射 `pct_chg→pctChg`、`mkt_cap→mktCap`、`circ_mkt_cap→circMktCap`；市场行情 parser `_parse_sina_market_row`（`adapters.py:283/289/290`）与 `_parse_eastmoney_market_row`（`adapters.py:353/358/359`）均返回同名驼峰字段，逐字一致。
- **根因**：字段名映射无问题。None 真正来源：
  1. **自定义代码池**：`selection.py:210-211` 传 `codes` 时 `pool=[{"code","name"}]`，无行情字段 → 三因子恒 None（本机实测最可能场景）。
  2. **上游字段缺失**：新浪 `mktcap/nmc/changepercent` 或东财 `f20/f21/f3` 缺失/停牌时 parser 显式返回 None。
- **备注**：快照因子真正数据源是 `fetch_market_list`（市场全量），非单股 `fetch_*_quotes`。单股接口 `_parse_tencent` 无 `pctChg`、`_parse_sina`/`_parse_eastmoney` 三者全缺——若未来想从单股接口补快照，那才是实际缺口。

## 三、已解决（供对照，无需再处理）

| 项 | 位置 | 状态 |
|---|---|---|
| 财务因子市场标识拼错（`600519.1` → `600519.SH`） | `adapters.py:1034-1038` `fetch_finance_summary` | 已修 |
| ROA 字段 ZZCJLL 误用 | `adapters.py:1049` | 已修 |
| 北向持股单一接口失效 | `adapters.py:1065-1115` | 已改双源兜底 |
| ML 路径非法键静默丢弃 | `ml.py:294-305` + `routers/ml.py:53-58` | 已修（告警+回显） |
| 快照总开关 UX 陷阱 | `ml.py` snapshot_ignored_keys 回显 | 已加告警旁路 |

## 优先级建议

仅剩一项待实测：**北向持股双源实测**——需确认 `fetch_north_holding` 双源至少一个能取到真实 `north_holding_pct`。

其余所有项均已修复或在代码中正确（详见上表）。
