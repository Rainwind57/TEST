# 模拟盘买入卖出优化建议（解除自选股限制）

> 日期：2026-08-13 ｜ 依据：对 `backend/app/routers/portfolio.py`、`backend/app/db.py`、`frontend/src/views/PortfolioView.vue`、`frontend/src/stores/watchlist.js` 的逐行复核（非记忆）。
> 目标：让模拟盘的下单标的不再被"自选股"绑架，可交易任意可交易个股/板块股。

---

## 0. 现状核实（代码实证）

| 层 | 是否限制自选股 | 实证 |
|---|---|---|
| 后端 `routers/portfolio.py:78-140` | **否** | `place_order` 接收任意 `code`，仅校验 `db.is_tradable(code)`（`db.py:11-25` 只排除指数/ETF），行情用 `adapters.fetch_tencent_quotes([code])` 独立获取，全程无 watchlist 校验 |
| 前端 `PortfolioView.vue:92-97` | **是（根因）** | 下单代码 `<select>` 直接绑定 `watchlist.codes`，无自由输入；非自选股无法选、无报价 |

**结论：限制 100% 在前端 UI，后端逻辑本就支持任意可交易个股。** 修复主要改前端，后端仅做少量加固。

---

## 1. 限制点定位（4 处）

1. `PortfolioView.vue:92-97` 下单 `<select v-model="orderCode">` 选项来自 `watchlist.codes` → 列表外股票无法选择。
2. `PortfolioView.vue:19` `orderPrice = watchlist.quotes[orderCode.value]?.price` → 非自选股无报价，预计金额恒为 `--`。
3. `PortfolioView.vue:23` `if (!orderCode.value) { toast('请先添加自选股'); return }` → 提示误导，把"需填代码"说成"需加自选"。
4. `PortfolioView.vue:58/65` 轮询只 `watchlist.refreshQuotes()` → 列表外股票在 UI 永远无实时价。

---

## 2. 优化建议

### P0 — 解耦下单标的与自选股（核心修复）
- 把 `<select>` 换成「自由输入框 + 代码联想」：`@input` 时调用 `/quote?codes=xxx`（或新增 `/api/quote/validate`）取名称/可交易性，下拉展示候选（可复用现有股票搜索/板块接口）。
- 新增 `orderQuote` ref，下单前取价用 `orderQuote.price` 替代 `watchlist.quotes[...]`。
- 校验提示改为「请输入股票代码」；真错误交给后端 `is_tradable`（指数/ETF 拦截）+ `422 暂无行情`。

### P0 — 按需单票行情
- 新增 store action `fetchQuote(code)`：`api.get('/quote', {params:{codes:code}})` 写入局部 `orderQuote`；代码变更即拉取，摆脱 6s watchlist 轮询依赖。
- 后端 `/quote`（`routers/quote.py`）已通用，无需改；可选加 `/api/quote/validate` 返回 `{tradable, name}` 供前端预校验与名称回填。

### P1 — 板块 / 全市场选股入口
- 下单面板复用 `BacktestView`/`SelectView` 已有的行业板块下拉（`adapters.BOARD_MATCHERS` / `fetch_sector_map`），支持从板块挑股直接下单，呼应此前 W1 的板块源能力。

### P1 — 自选转为"便捷"而非"门槛"
- 保留「加入自选」按钮（一键监控），但下单不再要求已在自选；下单成功后提示"是否加入自选"。

### P2 — 代码归一与边界
- 输入框经 `format.js normalizeCode` 归一（支持 sh/sz/bj、8xxxxx/920xxx），与行情/导入一致。
- 可选：模拟盘放开 ETF（`db.is_tradable` 当前排除 ETF），若用户想练习 ETF 交易。

---

## 3. 前端改造草图（PortfolioView.vue）

```vue
<!-- 原：<select v-model="orderCode"> 绑定 watchlist.codes -->
<!-- 改：自由输入 + 联想 -->
<input
  v-model="orderCode"
  @input="onCodeInput"
  @blur="onCodeBlur"
  placeholder="输入代码，如 sh600519 / bj830799"
/>
<span v-if="orderQuote" class="order-estimate">
  现价 {{ fmtNum(orderQuote.price) }} ｜ 预计 {{ fmtMoney(orderQuote.price * orderQty) }}
</span>

<script setup>
const orderQuote = ref(null)
async function onCodeInput() {
  const code = normalizeCode(orderCode.value)
  if (!code) { orderQuote.value = null; return }
  const data = await api.get('/quote', { params: { codes: code } })
  orderQuote.value = data[code] || null
}
async function placeOrder() {
  if (!orderCode.value) { toast('请输入股票代码'); return }
  if (!orderQuote.value?.price) { toast('暂无行情，请确认代码'); return }
  if (orderQty.value % 100 !== 0) { toast('数量需为100的整数倍'); return }
  await portfolio.order(normalizeCode(orderCode.value), orderSide.value, Number(orderQty.value))
  toast(orderSide.value === 'buy' ? '买入成功' : '卖出成功')
}
</script>
```

后端无需改动即可支撑；若加 `/api/quote/validate`：

```python
# routers/quote.py 新增
@router.get("/validate")
async def validate_code(code: str):
    c = code.strip().lower()
    name = (await adapters.fetch_tencent_quotes([c])).get(c, {}).get("name")
    return {"code": c, "name": name, "tradable": db.is_tradable(c)}
```

---

## 4. 验收标准

| 能力 | 验收 |
|---|---|
| 任意个股下单 | 不入自选，直接输入 `sh600519` → 有实时价、可下单、成交入库 |
| 非法标的拦截 | 输入指数 `sh000001` → 后端 400「非交易标的（指数/ETF）」 |
| 板块选股 | 选行业板块某股 → 直接下单成功 |
| 报价解耦 | 非自选股也能看到现价与预计金额，不再 `--` |

> 本建议原为纯诊断，现已按 P0+P1+P2 全部落地实现（见第 5 节「实施记录」）。

---

## 5. 实施记录（全部落地）

| 项 | 状态 | 改动位置 |
|---|---|---|
| P0 下单标的解耦自选股 | ✅ | `PortfolioView.vue:14-42` 自由输入 + `orderQuote` 独立行情（`onCodeInput` 调 `/quote`），非 `<select>` 绑定 `watchlist.codes` |
| P0 按需单票行情 | ✅ | `PortfolioView.vue:29-42` `fetchQuote` 逻辑；`routers/quote.py:29-43` 新增 `/quote/validate` 返回 `{code,name,tradable}` |
| P1 板块选股入口 | ✅ | `PortfolioView.vue` 保留 `selectedBoard`/`boardStocks` 板块挑股下拉直接下单 |
| P1 自选转便捷 | ✅ | 下单不再要求已入自选；提示改为「请输入股票代码」；datalist 仅作联想建议 |
| P2 代码归一 | ✅ | `PortfolioView.vue:32/50/76/85/139` 下单全程 `normalizeCode`（sh/sz/bj、8xxxxx/920xxx） |

**关键实现细节**
- 下单价格取 `orderQuote.value?.price`（`:24`），不再读 `watchlist.quotes[orderCode]`；`watchlist.quotes` 仅用于 datalist 展示名称。
- 非法标的由后端 `db.is_tradable`（`db.py:18-25`，排除指数/ETF）+ 行情缺失拦截，前端仅提示「暂无行情，请确认代码」。
- ETF 仍按 `db.py` 现状排除，未放开（属 P2 可选项，未启用）。

**验收**：不入自选直接输 `sh600519` → 有实时价、可下单、成交入库；指数 `sh000001` → 后端拦截。
