# 应用优化建议（图表拆分 / 调参因子一致性 / 其他）

> 本文档仅做**优化建议与问题定位**，不修改任何代码。
> 针对用户反馈：① K 线图太凌乱应拆成几个图；② 调参因子与人造模型所选因子不一致；③ 其他可优化点。
> 文中均以 `文件:行号` 标注代码级证据。

---

## 一、K 线图太凌乱，应拆分为多图（重点，P1）

**定位文件**：`frontend/src/views/QuoteView.vue`

当前行情详情页只有**一张** K 线图（`QuoteView.vue:52-90` 的 `klineOption`），把 5 类信息全部叠在同一绘图区：

- 蜡烛图（K线）`78-79`
- MA5 / MA10 / MA20 三条均线 `80-85`
- 成交量柱 `86-87`，且用了**右轴 `yAxisIndex:1`（`min:0`）叠在价格图上方**（`:71`、`:86`）

结果：成交量柱与价格蜡烛共用同一纵向区间、视觉互相穿插，均线三色 + 蜡烛红绿 + 量柱红绿全挤在一起，用户第一眼确实"凌乱、看不清"。

**建议拆分为多子图（grid）**，共享同一 x 轴与 dataZoom：

1. **主图（上方）**：蜡烛 + MA 均线，独立 y 轴（价格）；
2. **成交量子图（中间）**：独立的 grid、独立 y 轴，柱体不再压在蜡烛上；
3. **指标子图（可选，下方）**：MACD / KDJ / BOLL 等副图指标，作为可勾选的折叠面板。

> 注：第③点里提到"没有分时图""缩放太弱"之前已单列；本处聚焦"同一张图信息过载"。
> 当前 `dataZoom` 其实已存在（`:73-76`，这是好的），只需把单 grid 改成多 grid 即可彻底解凌乱。

**其他 K 线体验短板（同一文件，顺带优化）**：
- `loadKline` 硬编码 `days: 500`（`:47`），无"日/周/月"周期切换；
- 无长周期均线（MA60/MA120/MA250），只有短周期 MA5/10/20；
- 无任何副图技术指标（MACD/KDJ/BOLL），用户无法叠加常用指标判断。

---

## 二、调参因子 ≠ 人造模型所选因子（重点，P0 根因）

**现象**：你在「新建人造模型」时勾选了一组因子（如动量20、波动率20、RSI14），点「调参」后却看到一堆**不一样**的因子名。

**根因（代码级）**：人造模型的因子名清单与调参面板读到的清单，来源与内容都不一致。

### 2.1 人造时展示的是「中文 label + key」，调参时只剩「key」

- 人造创建页数据来自 `GET /ml/manual/features` → 后端 `manual_feature_options()`（`ml.py:1303-1306`）返回 `{key, label, group}`；
- 前端创建 UI 显示中文 `f.label`（动量20）并附 `f.key`（`MLView.vue:372-376`）；
- 调参面板 `openAdjust` → `GET /ml/models/{mid}/params` → `load_model_meta` 返回 `featureNames`（`ml.py:600-650`）；
- 前端调参 UI 遍历 `adjustMeta.featureNames`，**直接渲染裸 `key`（`momentum_20`）**，无中文标签（`MLView.vue:427-430`）。

→ 同一因子，创建时叫"动量20"，调参时叫"momentum_20"，用户体感"因子不一样"。

### 2.2 调参面板把「全部因子」都列出来，而非你真正选的那几个

- 后端 `MANUAL_FEATURES = list(FACTORS.keys())`（`ml.py:1300`）——**是全部 ~24 个技术因子**；
- `create_manual_model` 把 `feature_names = MANUAL_FEATURES`（全部 24）连同模型落盘（`ml.py:1342`、`:1362`）；
- 你创建时只给其中几个填了非零权重（`featureWeights: valid` 才只存你选的，`ml.py:1363`），但 `featureNames` 字段存的是全部 24；
- 调参 `openAdjust` 用 `featureNames` 初始化权重（`MLView.vue:64-65`：`for f of featureNames: featureWeights[f]=1`），于是面板**把全部 24 个因子（含你 0 权重的）全列出来**。

→ 你"人造时选了 3 个"，调参却"看到 24 个"，自然觉得不一致。

### 2.3 更深的结构性限制（值得一并优化）

- **人造模型因子池被锁死为技术因子**：`MANUAL_FEATURES = list(FACTORS.keys())`（`:1300`）**不含** `SNAPSHOT_FACTORS`（PE/PB/ROE/北向/资金流等 18 个快照因子）。所以用户想做"动量 + 低 PE"这类**技术+财务**的混合人造模型，根本选不到财务因子。
- **训练流程完全没有"因子选择"**：`MLView` 的 `evalConfig()`（`MLView.vue:89-97`）只有板块/样本量/历史长度/CV 等，**没有任何"选哪些因子训练"的入口**——模型默认吃 `build_dataset` 的全部特征。于是训练出的模型，调参时显示的 `featureNames` 是一堆内部特征名（技术 + 快照），你"从没主动选过"，也造成"调参因子 ≠ 我选的因子"。

**建议修复方向**：
1. 调参面板复用 `featureLabels` 映射（`ml.py:1391` 已有 `{key,label}`），把裸 key 显示成中文 label；
2. 人造模型落盘时，`featureNames` 只存用户实际赋权（非零）的因子，而非全部 `MANUAL_FEATURES`；
3. 调参面板对 0 权重因子做折叠/置灰，突出用户真正参与的因子；
4. 放开人造模型因子池，把 `SNAPSHOT_FACTORS` 也并入可选项；
5. 训练页增加"因子多选"入口，让用户清楚知道模型用了哪些因子。

---

## 三、其他可优化点（P1/P2，附证据）

### 3.1 训练不支持选因子 → 模型"黑盒"，可解释性差（P1）
如上 2.3，`evalConfig`（`MLView.vue:89-97`）无因子入参，训练吃全部特征。建议增加因子多选，并在模型元信息里记录"用到的因子清单"，调参/报告时一目了然。

### 3.2 人造模型无法用财务/资金流因子（P1）
`MANUAL_FEATURES = list(FACTORS.keys())`（`ml.py:1300`），排除全部快照因子。量化里"技术+基本面"混合是刚需，建议把 `SNAPSHOT_FACTORS` 纳入人造因子池（注意 `build_dataset` 也要能算这些特征的截面值）。

### 3.3 调参面板对训练模型不友好（P2）
训练模型（GBDT/LightGBM）的 `featureNames` 可能多达 40+ 个内部名（`build_dataset` 技术 + 快照）。当前面板（`MLView.vue:417-437`）无搜索、无分组、无中文标签、无折叠——调参时滚到眼瞎。建议：分组（技术/财务/资金流）、搜索过滤、显示 `featureImportance` 排序、默认仅展开 TopN。

### 3.4 K 线硬编码 500 天、无周期切换（P2）
`QuoteView.vue:47` `days:500` 写死，无日/周/月切换，也无历史长度输入。结合之前"行情历史被压在 90~1023 天"的清单，这里至少该放一个"天数"输入并支持到 1023+。

### 3.5 回测报告图表依赖外网 CDN（P2，呼应前篇）
`reporting.py:101` 的 HTML 报告图表走 `cdn.jsdelivr.net`，国内常加载不出 → 下载的报告只剩表格。建议把 ECharts 资源本地化（随包分发或 inline），并给 PDF 报告补图（`render_pdf` 当前纯文字）。

### 3.6 行情页无"多股对比 / 叠加"能力（P2）
`QuoteView.vue` 每次只渲染 `store.activeCode` 一只票（`:45`、`:101`），自选列表只能看表格数值，无法在同一图叠加多只或看相对强弱。对"盯盘/对比"场景偏弱。

### 3.7 人造模型规则说明 `rule` 未被校验/利用（P3）
`manualRule`（`MLView.vue:212`）仅作为备注存进 meta（`ml.py:1364`），不参与任何推理或回测逻辑。若要做"规则+打分"混合信号，目前 `rule` 是摆设。

---

## 四、优先级汇总

| 级别 | 项 | 文件:行 | 一句话 |
|---|---|---|---|
| P0 | 调参因子 ≠ 人造所选因子 | `ml.py:1300,1342,1362` / `MLView.vue:64-65,427` | 裸 key + 全量 24 因子，与创建时中文 label/子集不一致 |
| P1 | K 线拆多子图 | `QuoteView.vue:52-90` | 量柱叠在蜡烛上、均线三色拥挤 |
| P1 | 训练无因子选择 | `MLView.vue:89-97` | 模型吃全部特征，用户不知用了啥 |
| P1 | 人造模型缺财务因子 | `ml.py:1300` | `MANUAL_FEATURES` 仅技术因子 |
| P2 | 调参面板 UX（无标签/搜索/分组） | `MLView.vue:417-437` | 40+ 因子滚到眼瞎 |
| P2 | K 线硬编码 500 天/无周期 | `QuoteView.vue:47` | 无日周月切换 |
| P2 | 报告图表依赖 CDN | `reporting.py:101` | 国内加载不出 |
| P2 | 行情无多股对比/叠加 | `QuoteView.vue:45,101` | 只能单股 |
| P3 | `rule` 字段未利用 | `MLView.vue:212` / `ml.py:1364` | 规则说明成摆设 |

---

## 五、与历史诊断的关联

- 呼应《应用问题清单_图表与报告.md》：K 线"不能放大缩小"已在新版 `QuoteView.vue` 加了 `dataZoom`（`:73-76`），但"凌乱需拆分"是更细的问题，本文补全；
- 呼应《应用问题清单_调参与板块多选.md》：该篇聚焦"调参 network error / 板块单选"，本文聚焦"调参因子内容本身与创建时不一致"，角度互补；
- 呼应《应用功能验证_机器学习与盯盘回测.md》：训练默认 `hist` 太小、因子不可选，导致模型效果与可解释性双低——本文 2.3/3.1 给出根因级优化方向。
