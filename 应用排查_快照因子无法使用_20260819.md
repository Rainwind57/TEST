# 应用排查：快照因子无法使用（2026-08-19）

## 现象
在 ML 训练 / 评估 / 回测，以及规则选股中勾选"快照因子"（ROE、净利率、营收同比、北向持股等）后，这些因子在数据集里**恒为 0 或缺失**，模型实际学不到它们——表现为"快照因子选了也没用 / 无效"。

## 根因

### 根因 1（确定性 Bug）：财务类快照因子数据源拼接错误，永远查不到数据
快照因子里的财务子类（ROE、毛利率、净利率、资产负债率、ROA、营收同比、净利同比、EPS、每股净资产，共 9 个）由财务接口从东方财富 F10 拉取。
该接口在拼接股票代码过滤条件时，把内部的市场标识（sh 映射为 "1"、sz 映射为 "0"）直接拼入，得到形如 `600519.1` 的过滤串；而该接口要求的是带交易所后缀的代码 `600519.SH`（市场标识应映射为 SH / SZ）。于是过滤条件永远匹配不到任何记录，接口返回空，这一批财务因子在数据集里被兜底填充为 0.0。
实测佐证：用正确后缀 `600519.SH` 直接请求该接口返回真实财务数据；而经应用代码请求返回空，9 个财务因子全部落为 0。

### 根因 2（接口失效）：北向持股因子取不到数据
北向持股比例因子从东方财富沪深港通持股明细接口拉取。实测两种过滤写法均返回 0 条记录，该因子恒为空。属接口 reportName / 字段可能已变更或需额外参数，当前实现无法取到值。

### 根因 3（设计层 UX 陷阱）：总开关未勾则快照因子被整体忽略
前端 ML 训练面板存在"含全部快照因子"总开关（useSnapshot）。该开关未勾选时，即便在因子清单里勾了 PE/PB/ROE 等快照因子，后端也不会把它们纳入训练。前端虽有"快照，开关未勾不生效"的提示，但极易被忽略。

## 受影响因子清单
- **行情快照类（可用）**：`pe`、`pb`、`turnover`、`amount` —— 来自实时报价快照，实测能正常取值。
- **资金流类（可用）**：`main_net_pct`（主力净流入占比）—— 来自资金流接口，实测有值。
- **财务类（不可用）**：`roe`、`net_margin`、`revenue_yoy`、`profit_yoy`、`gross_margin`、`debt_ratio`、`eps`、`bps`、`roa` —— 因根因 1 全部恒为 0。
- **北向类（不可用）**：`north_holding_pct` —— 因根因 2 恒为空。
- **其余行情字段（本环境取不到）**：`pct_chg`、`mkt_cap`、`circ_mkt_cap` 在本机实测返回 None，疑似快照字段映射未覆盖，属次要问题。

## 影响范围
凡在 ML 模型、规则选股中用到了上述 9 个财务因子或北向因子的场景，因子值恒为 0 / 缺失，"等于没选"。这会直接让依赖基本面 / 北向的选股、训练、回测失效，且无报错提示，极具迷惑性。
