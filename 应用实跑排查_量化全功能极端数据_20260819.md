# 应用实跑排查 · 量化全功能极端数据 · 2026-08-19

> 目的：把所有量化功能用**极端数据/参数**实际跑一遍，确认有无 bug。方式：绕过 HTTP 鉴权直接用后端模块驱动全部功能链路；极端参数触发边界；未修改任何业务代码。

## 覆盖的功能与极端参数（问题定位背景）

| 功能 | 极端参数 |
|---|---|
| 规则选股 | 池 2000、权重全 0、非法因子 `pe_ttm`、单只代码、极窄日期区间 |
| 规则回测 | `hist=5`（极小）、`hist=3000`（超长）、池 2000、期货、非法因子 |
| 因子回归 | `hist=1`、单因子、全非法因子、池 2000 + n=100 |
| ML 评估 | `hist=1`、`hist=3000`、子集含非法键 `pe_ttm`、合法子集 |
| ML 训练/选股/回测 | 训练小模型→选股（池 2000 / 期货）→回测（训练模型 `hist=5/3000/期货`、人造模型 纯技术 vs 含快照） |
| 参数寻优 | `hist=3000` + 小池、groups=1、n=1、nTrials=20 |
| 盯盘扫描 | 空自选股 / 含自选股 |
| 批量买入分配 | 现金 0 / 负 / 极大 / 空代码、perPositionPct=0、maxPositions=0 / 9999、单只有效 |
| 持仓下单 | 非法 side、数量 0、非 100 倍、负数量、非交易标的（指数）、无持仓卖出、资金不足、正常买/卖、自动交易信号 |

---

## 发现的问题

### P0 · 因子子集静默丢弃非法键（老问题，本次重现）
- **现象**：`selectedFactors=["momentum","volatility","pe_ttm"]` 只产出 **2 个特征**，`pe_ttm` 被**静默吞掉**；用确属合法的 3 键（`momentum/volatility/rsi`）才正确得 3 特征。
- **根因**：后端因子键名与直观命名不一致（市盈率真实键是 `pe` 而非 `pe_ttm`，量比键是 `volume_ratio5` 而非 `volume_rate`）；ML 路径对未命中因子字典的键**直接丢弃、无任何告警**。
- **关联**：与「因子恒为 36」同源（前端按习惯名传参 → 整键落空）。且**规则选股路径对未知因子显式 400，ML 路径却静默丢弃**，两条路径行为相反，易致用户困惑。
- **影响**：用户在 ML 因子面板勾选/输入的因子若用错键名，模型会"悄悄少因子"却不报错，结果对不上设定却无从排查。

### P1 · 下单全 422 "暂无行情数据"（疑似探针环境误报，需在 Web 实确认）
- **现象**：所有"需要行情"的下单（含**正常买入**、无持仓卖出、资金不足）均返回 **422 "暂无行情数据，请稍后重试"**；而非法 side / 数量等入口校验均已正确 400 拦截。
- **排查**：直接用 `curl` 实测腾讯、新浪行情接口均正常返回贵州茅台实时价 → 数据源网络可达，非网络问题。下单依赖运行时初始化的行情客户端；本次是独立进程直调路由函数，可能缺失该初始化 → 疑似探针环境误报。
- **待确认**：需在 Web / UI 实测下单。若 UI 下单也 422，则为真 bug——错误信息把"无持仓 / 资金不足"等业务原因也掩盖成"暂无行情数据"，误导排查。

### P2 · 参数寻优在异步上下文会报"事件循环"冲突（疑似探针误用，Web 待确认）
- **现象**：探针在 `async` 主函数里直接调同步的 `optimize_backtest`，报 `Cannot run the event loop while another loop is running`；但用**同步方式**直接调则完全成功。
- **判定**：属探针调用方式误用（异步上下文调了会自建事件循环的同步函数），**非应用 bug**。
- **风险提示**：`optimize_backtest` 内部若使用 `asyncio.run` / `loop.run_until_complete`，则在 **FastAPI 异步路由**（已有 running loop）中调用可能复现该冲突。需在前端实际触发一次参数寻优确认 Web 路径不报此错。

### P3 · ML 回测成功不返回 `ok` 字段（非新）
- 成功回测无 `ok` 字段、仅失败置 `ok:false`；前端用 `=== false` 判断，当前不会被误判，但属脆弱契约。

### P3 · 规则回测 `hist=3000` 年化指标为 None
- 超长窗口回测能跑出 979 次调仓，但年化收益率为 `None`（指标完整性在极端长窗口下存疑，不影响主流程）。

---

## 脚手架观察（非应用 bug）
- 探针日志部分用例重复执行（`ML评估-hist=3000` 出现两次），疑似异步事件循环重入或探针进程重复启动；重复执行均成功、无副作用，属测试脚本自身问题，非应用 bug。

---

## 结论（仅问题层面）
- **残留真实问题**：因子子集静默丢弃非法键（P0）——与「因子恒为 36」同源的老问题，依然存在。
- **两处待 Web 实测确认**：① 下单在 UI 是否也全 422；② 参数寻优在 Web 异步路由是否报事件循环冲突。
