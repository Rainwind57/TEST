# TEST-main 量化平台 · 代码审查报告（Bug 清单 + 优化建议 + 竞品对比）

> 审查对象：`C:\Users\asus\Downloads\TEST-main\TEST-main`（FastAPI + Vue 3 量化研究平台，后端版本 3.3.0）
> 审查方式：全量源码通读（后端 15 个模块 + 16 个路由，前端 14 个视图）+ 关键 bug 用真实代码实证复现
> 审查日期：2026-07-30

---

## 〇、结论先行

你反馈的「**跑完因子之后没办法有模型给我回测，很多选项都无继续的方式**」根因不是单一问题，而是**三层断链**：

| 层级 | 问题 | 后果 |
|---|---|---|
| ① 训练层（P0 Bug） | `numpy_factors.py` 的 `macd_series` 只计算最后一个点，其余全是 NaN。ML 数据集构建时每个样本都含 MACD 特征 → **所有样本被跳过 → 必然报"有效样本不足"** | 「训练并落盘」按钮**无论如何都训不出模型**（实证：250 根 K 线仅 1 个有效点，185 个候选样本点全部丢弃） |
| ② 接口层（设计缺口） | 即使训出模型，后端也**没有任何使用模型的接口**：`/api/ml/` 只有 evaluate / train / models / delete，没有 predict / score / backtest；选股和分层回测也不接受"模型"作为信号源 | 模型只能躺在 `ml_models/` 目录里，唯一的操作是「删除」 |
| ③ 前端层（流程断头） | ML 页模型列表只有「删除」按钮；异步任务队列（jobs）只支持 3 种任务，ML 训练和参数寻优**只能同步调用**，前端 axios 60 秒必超时 | 页面上"很多选项无继续的方式" |

类似的"断头路"还有：**自定义组合因子**创建后在选股页无处可勾选、**组合优化**要手工粘贴 mu/cov 且与持仓完全脱节、**regression 类策略**能保存不能一键运行。详见 [第三章](#三流程断点地图死胡同功能清单)。

**另外 2 个必须知道的"沉默错误"**（界面不报错，但结果是错的）：

- **参数寻优的 IS/OOS 验证是假的**：`_split_hist(180)` 返回 `(90, 90)`，IS 和 OOS 取的都是"最近 90 天"，窗口完全重叠 → OOS 指标 = IS 指标，所谓"防过拟合"不成立（已实证）。
- **ML 的"时序交叉验证"名存实亡**：数据集按股票拼接、按索引切分，训练集里混着比测试集更晚的日期（实证某 fold 86% 样本来自"未来"）；且缩尾/标准化在切分前对全样本拟合，存在预处理泄漏。报告的 OOS IC 不可信。

---

## 一、Bug 清单（按严重程度分级）

### P0 — 致命：核心功能不可用

| # | 位置 | 现象 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | `numpy_factors.py:77-92` `macd_series` | ML 训练/评估 100% 报"有效样本不足"；选 MACD 做分层回测/多因子回归必报"有效截面样本不足" | 函数只给 `out[-1]` 赋值，历史点全 NaN，`series_at` 全返回 None，所有样本被跳过 | 改为全序列 EMA→DIF→DEA→柱（**已交付修复版**，与逐点版最大偏差 3.9e-16） |
| 2 | `routers/ml.py`（全文件） | 训练好的模型无法用于选股/回测——用户主诉 | 无 predict/score/backtest 接口；`/api/select` 不接受模型因子；策略 kind 无 `ml` 类型 | 新增 `/api/ml/score`（截面打分选股）+ `/api/ml/backtest`（ML 信号分层回测）（**已交付补丁**） |
| 3 | `ml.py:222` `train_final_model` | 旧模型无法复现推理 | 落盘只有 model+feature_names，**没存缩尾分位数/均值/标准差**，推理无法复现预处理 | 预处理参数随 joblib 一起落盘 + sidecar 元数据 JSON（已交付） |

### P1 — 严重：结果错误或功能实质失效

| # | 位置 | 现象 | 根因 | 修复建议 |
|---|---|---|---|---|
| 4 | `optimize.py:14-17` `_split_hist` | 寻优的 OOS 验证是假的 | `hist=180 → (90,90)`，IS/OOS 都取"最近 90 天"，完全重叠（已实证） | K 线一次性取足全长，按日期切两段互不重叠的窗口分别回测 |
| 5 | `ml.py:44-72` `build_dataset` | "时序 CV"无效，OOS IC 虚高 | 样本按股票拼接，切分按索引 → 训练集含未来日期（实证 fold4 有 86/100 个"未来"样本）；同日截面同时进训练/测试 | 按 (date, code) 排序后再切分（已交付修复，实证切分后 0 个未来样本） |
| 6 | `ml.py:130` `evaluate_dataset` | 预处理泄漏 | `standardize(winsorize(...))` 在全数据集上拟合后再 CV | 每折在训练集内 fit 预处理再应用测试集（已交付修复） |
| 7 | `numpy_factors.py:47-55` `volatility_series` | 波动率因子含 1 天未来数据（前视偏差） | 窗口取 `rets[i-n+1:i+1]`，`rets[j]` 是第 j+1 日收益 → 纳入了第 i+1 日收益；与 `factors.py` 逐点版不一致（实证偏差 ~1e-3） | 窗口改为 `rets[i-n:i]`（已交付修复，对齐偏差 1.7e-18） |
| 8 | `routers/jobs.py:22` + 前端 | 大池训练/寻优/回测必超时 | 异步任务只支持 select/backtest/factor-regression；ML、optimize 只能同步调；前端 axios 统一 60s；前端回测页也没接 jobs | jobs 扩展 `ml-train`/`ml-backtest`/`optimize` 三种 kind，前端统一改「提交→轮询」（补丁 B） |
| 9 | `adapters.py:260-292` `fetch_kline` | 回测在用陈旧数据自己不知道 | 磁盘缓存只按**条数**判断，不看最后日期 → 新交易日数据永远不补充；且返回全量缓存（hist 参数被静默放大） | 增加末日日期新鲜度校验（容差 3 天，补丁 D）；返回值裁剪到请求的 days |
| 10 | 全局设计 | 幸存者偏差 | 回测候选池 = 当下按成交额排序的快照，历史回测用当下成分股 → 系统性高估收益 | 至少加文字披露；进阶：接入含退市股的历史成分数据源 |
| 11 | `routers/strategies.py:40-61` | 横向越权（IDOR） | 删除策略/自定义因子/回测存档、运行策略均不校验 user_id 归属，知道 id 就能删别人的 | 操作前查库比对 `user_id`（补丁 C） |
| 12 | `StrategiesView` + `selection.py:165-168` | 自定义组合因子是死功能 | 前端因子目录不含 `uf:` 条目，选股页勾选不到；且成分因子未勾选时值全 None → 得分恒 0 | 目录接口混入 uf 条目；后端按 uf 成分自动补取因子值 |

### P2 — 中等：结果有偏或体验受损

| # | 位置 | 问题 |
|---|---|---|
| 13 | `ml.py:180` | `oosSharpe` 对单元素列表求夏普 → **恒为 0**（已实证）；修复版改为按日聚合多空序列，实证可得真实值 2.85 |
| 14 | `selection.py:346-349` | 基准序列与多空序列长度不齐时 alpha/beta 静默置 0；应按日期交集对齐 |
| 15 | `selection.py:297-298` | 成本模型：多空双腿只扣一份往返成本；且每期默认全仓换手，与真实换手无关 |
| 16 | `optimize.py:118` + `routers/optimize.py` | 寻优回写策略丢失 user_id（归匿名账户）；`nTrials` 无上限可被刷爆 |
| 17 | `routers/risk.py:42-54` + `risk.py:60-64` | 风险归因方法缺陷：用"买入以来收益"做单截面回归，5 因子对个位数持仓自由度不足；`factor_covariance` 用截面暴露协方差冒充因子收益时序协方差 |
| 18 | `intraday.py:35,81-90` | `maxTrades` 实为全程上限而非注释的"单日最大"；按"每笔收益 × 960 期/年"年化无意义；无成本、无 T+1 约束（A 股日内不能卖出当日买入） |
| 19 | `db.py` / `auth.py` | SQLite 未开 WAL，并发写 + APScheduler 易 `database is locked`；登录无限流可爆破；`JWT_SECRET` 每次重启随机生成 → 重启后全员掉线 |
| 20 | `routers/selection.py:50` / `adapters.py` | 行情列表默认 `sortField="f6"` 是东财字段名，新浪接口不识别 → 排序静默失效；README 宣称"限流重试即可"但代码无任何重试 |
| 21 | `PortfolioOptView.vue` + `routers/portfolio_opt.py` | 组合优化是孤岛：手工粘贴 mu/cov，与持仓/自选/因子无联动，结果也无法一键应用到模拟盘；`max_sharpe` 归一化后 `maxWeight` 约束失真；cvxpy 求解异常未捕获直接 500 |

### P3 — 轻微 / 工程卫生

- ML 页板块选项缺「北交所」（回测页有）；北交所代码在东财 secid 映射可能不正确（`adapters.py:14` 仅按 sh/sz 二分）。
- `RiskView.vue:37` 贡献条形图正绿负红，与全站"涨红跌绿"约定相反。
- `BacktestView.vue:127-129` "最高组累计"曲线是否显示竟取决于有无基准（条件写错）。
- `StrategiesView.vue:98` `s.config?.factors?.length + '因子'` 在无 factors 时显示 "undefined因子"。
- `reports.py` 导出 HTML 未转义 `factorLabel`（自注入 XSS，低危）。
- 模拟盘无交易成本、无 T+1、下单读改写无并发保护、`/portfolio/reset` 无鉴权任何人可清空。
- 依赖卫生：`requirements.txt` 有 lightgbm 但代码未用；`ml.py` import pandas 未用；`main.py` 的 `@app.on_event("startup")` 已被 FastAPI 弃用（应改 lifespan）。
- 无任何测试 / CI / 日志体系（出错只进 `job.error`，线上无法回溯）。

---

## 二、重点 Bug 实证记录（可复现）

| Bug | 实证方法 | 结果 |
|---|---|---|
| macd_series 只有 1 个点 | 250 根合成 K 线计算全序列 | 非 NaN 点 = **1**（应为 ~216）；模拟 build_dataset 内层循环：保留 0 / 跳过 185 |
| volatility_series 前视 | 与 `factor_volatility` 逐点对比 | i=120 时 0.008447 vs 0.007820，窗口含第 121 日收益 |
| IS/OOS 重叠 | 调用 `_split_hist(180/200/240)` | (90,90)/(100,100)/(120,120)，完全重叠 |
| oosSharpe 恒 0 | `_sharpe_periodic([0.023])` | 返回 0.0（单元素 std=0） |
| CV 混入未来 | 3 股 × 100 日拼接后切分 | fold4 训练集 86% 样本日期晚于测试集 |
| 修复版正确性 | 合成数据全流程测试 | macd 217/250 有效、与逐点版偏差 3.9e-16；volatility 偏差 1.7e-18；CV 未来样本 = 0；OOS Sharpe = 2.851（非 0）；模型落盘/加载/打分/元数据全通 |

---

## 三、流程断点地图（"死胡同"功能清单）

```
因子计算(/factor)        → 只能"看"，不能"用"（无"用该因子回测/选股"跳转）
自定义组合因子(策略中心)  → 创建后选股页勾选不到，得分恒 0   【断头】
机器学习(/ml)            → 训练必失败(P0#1)；训成也无打分/回测入口 【断头×2】
参数寻优(/optimize)      → IS/OOS 同窗(P1#4)；同步调用必超时(P1#8)；回写策略丢 user_id
组合优化(/portfolio-opt) → 手工输入 mu/cov，与持仓/自选零联动，结果不可落地 【断头】
分钟回测(/intraday)      → 无成本无 T+1，结果不可信；与策略中心无联动
风险归因(/risk)          → 方法学缺陷(P2#17)，个位数持仓跑 5 因子回归
策略中心(regression 类)  → 能保存，一键运行报"暂不支持" 【断头】
异步任务(jobs)           → 前端没有任何页面真正在用（回测页走同步接口） 【断头】
```

---

## 四、优化建议（按主题）

### 4.1 研究闭环（最高优先）
1. **打通模型使用侧**（补丁已给）：`/api/ml/score` 截面打分 → 列表可「加入自选 / 一键买入模拟盘」；`/api/ml/backtest` 响应结构与 `/api/select/backtest` 完全一致，前端图表零成本复用。
2. **策略统一抽象**：kind 扩展为 `select / backtest / regression / ml`，任意页面「保存为策略」，策略中心一键重跑任意 kind。
3. **页面间跳转**：因子页 →「用此因子回测」；回测结果 →「保存为策略 / 导出报告 / 应用到模拟盘」；寻优结果 →「回写策略」。

### 4.2 数据与缓存
4. K 线缓存加**末日新鲜度校验**（补丁 D），返回裁剪到请求长度；接入交易日历（替换 `scheduler.py:56` 的"仅排周末"）。
5. 数据源加**指数退避重试**与失败降级（腾讯→东财互为备份）；修复 `sortField="f6"` 字段错配。
6. 披露并缓解幸存者偏差；财务/北向/资金流数据（`data.py` 已有接口）接入因子库与 ML 特征。

### 4.3 性能
7. 长任务全部接入 jobs 异步队列（补丁 B），前端轮询 + 进度条；axios 超时分级（普通 60s / 任务提交 10s）。
8. `fetch_market_list` 全市场快照缓存复用于选股/回测/打分三处，避免重复分页拉取。
9. `_objective` 每次新建事件循环（`optimize.py:36-38`）改为 `asyncio.to_thread` 或全程 async。

### 4.4 安全与多用户
10. 全部按 id 操作的接口加归属校验（补丁 C）；`save-strategy` 透传 user_id；登录加限流；`JWT_SECRET` 固定从环境变量读取并写进部署文档；`/portfolio/reset` 加鉴权或确认码。

### 4.5 方法论（量化严谨性）
11. 回测口径升级：基准按日期交集对齐；成本按实际换手率计提、多空双腿双份；涨跌停/停牌不可成交处理；T+1 约束（分钟回测同）。
12. 风险归因改为时序法：因子收益用 Fama-MacBeth 时序回归估计，协方差用因子收益时序估计；持仓少于 ~30 只时明确提示"样本不足，仅供参考"。
13. 分钟回测年化按 bar 收益而非按笔；`maxTrades` 改为真正的"每日上限"。

### 4.6 工程化
14. 补 pytest 冒烟测试（本报告的 6 个实证脚本就是现成的测试用例）+ GitHub Actions；加 logging 体系；`on_event` 改 lifespan；清理 lightgbm/pandas 死依赖。

---

## 五、与市面竞品的详细对比

### 5.1 竞品全景（2026 年现状）

| 平台 | 定位 | 数据 | 回测引擎 | ML/AI | 实盘 | 费用 | 对比本项目的启示 |
|---|---|---|---|---|---|---|---|
| **聚宽 JoinQuant** | 研究+回测+模拟+实盘一体，社区最大 | 20 年 Tick 级，基本面/资金/舆情全 | 事件驱动，日/分钟/Tick | 有（AI 因子、ML 课堂） | 对接券商实盘 | 基础免费+数据付费 | 本项目形态最接近它，但**数据维度、实盘通道、社区**是数量级差距 |
| **米筐 RiceQuant** | 专业研究，回测速度快 | 全球化市场（含港股通） | 向量化+事件驱动 | 因子分析+组合优化工具链完整 | 期货实盘强 | 专业版 ¥999/月 | 其**因子分析→组合优化工具链闭环**正是本项目缺的那一环 |
| **掘金量化 MyQuant** | 机构级，实盘落地最强 | 10 年 Tick/分钟 | 纳秒级事件驱动 | 一般 | C++/Python，毫秒级，股/期/期权全 | 按券商/版本 | 走实盘路线才需要对标，研究阶段不必 |
| **优矿 Uqer** | 数据与因子库见长（通联数据） | 400+ 内置因子，风险模型内置 | 日/分钟 | 因子研究+风险模型+组合优化内置 | 弱 | 个人免费+专业版 | **400+ 因子库 vs 本项目 32 个**；风险模型内置 vs 本项目简化版且有缺陷 |
| **BigQuant** | AI 量化，无代码拖拽 | 全市场 | 有 | **StockRanker/AutoML 一键训练+回测** | 对接券商 | 基础免费 | 其「AI 模型 → 回测 → 策略」无缝衔接，正是本项目断裂的地方 |
| **恒生 PTrade / 迅投 QMT** | 券商系实盘终端 | 券商通道 | Tick 级，<1ms | 模板策略 | **真实盘**（10 万门槛） | 开户免费 | 互补而非竞争：本平台可作为其**上游研究工具** |
| **同花顺 i问财 / MindGo** | 自然语言选股+入门量化 | 全市场 | 基础 | NLP 选股 | T+0 模拟 | 免费 | 交互门槛低值得借鉴（自然语言→因子组合） |
| **Qlib（微软开源）** | AI 量化研究框架 | 自带数据工具 | 有（含高频） | **完整 ML 工作流：数据→模型→回测→分析**，内置 LGBM/深度模型库 | 无 | 免费开源 | **本项目 ML 模块的直接对标**：Qlib 的 dataset 按日期组织、handler 预处理随折拟合，正是本项目踩坑处的标准答案 |
| **Backtrader / vn.py** | 本地回测/实盘框架 | 自备 | 事件驱动 | 无 | vn.py 实盘强 | 免费开源 | 事件驱动架构更真实，但无 UI；本项目差异化在**开箱即用的 Web 界面** |

### 5.2 能力差距矩阵（本项目 vs 头部竞品）

| 能力 | 本项目现状 | 聚宽/米筐/优矿 | 差距评级 |
|---|---|---|---|
| 行情数据维度 | 量价+PE/PB/市值快照+资金流/北向/财务（未入因子） | 基本面/行业/概念/舆情/Level2，含退市股 | ★★★★☆ 大 |
| 因子库 | 32 个写死 + 组合式（但断头） | 400+，可表达式自定义 | ★★★★☆ 大 |
| 回测真实性 | 向量化分层，成本粗糙，幸存者偏差，无涨跌停/T+1 | 事件驱动，完备成本/撮合/停牌处理 | ★★★★☆ 大 |
| ML 闭环 | **训练必失败 + 无使用入口** | BigQuant/Qlib 一键训练→回测→分析 | ★★★★★ 致命 |
| 参数寻优 | Optuna 有，但 IS/OOS 同窗失效 | Walk-Forward 滚动寻优 | ★★★☆☆ 中 |
| 组合优化 | cvxpy 求解器有，但孤岛手工输入 | 与因子/风险模型联动 | ★★★☆☆ 中 |
| 风险模型 | 简化版且有方法缺陷 | Barra 级内置 | ★★★☆☆ 中 |
| 模拟盘/调度 | 有（单机共享，无成本） | 模拟+实盘+云托管 | ★★★☆☆ 中 |
| 报告导出 | HTML/Excel 已有 | 研报级 PDF+归因 | ★★☆☆☆ 小 |
| Web UI 易用性 | **完整 Vue 界面（差异化优势）** | Jupyter 为主 | 本项目局部领先 |
| 多用户 | JWT 有，但越权漏洞 | 完整 SaaS | ★★★☆☆ 中 |

### 5.3 定位建议

不要正面硬刚聚宽的数据和实盘。本项目的合理生态位是：**「本地私有、开箱即用、Web 化的个人量化研究台」**——对标 Qlib 的研究工作流 + 比 Qlib 友好得多的界面。优先做三件事即可形成差异化可用产品：

1. **修通 ML 闭环**（补丁已交付）——这是连 Qlib 用户都要写代码才能做的事，你做成按钮就是优势；
2. **回测口径专业化**（成本/基准对齐/T+1/披露偏差）——研究工具的信任基石；
3. **报告与策略沉淀**（已有雏形，补齐 regression/ml 两类）——形成"研究资产"。

---

## 六、修复路线图与已交付补丁

### 6.1 已交付的修复补丁（本目录 `修复补丁/`，已通过合成数据实测）

| 文件 | 内容 | 验证结果 |
|---|---|---|
| `修复补丁/numpy_factors.py` | 直接替换 `backend/app/numpy_factors.py`。修复 macd 全序列化 + volatility 前视偏差 | macd 217/250 有效点、与逐点版偏差 3.9e-16；volatility 偏差 1.7e-18 |
| `修复补丁/ml.py` | 直接替换 `backend/app/ml.py`。数据集按日期排序、每折内预处理、oosSharpe 修复、预处理参数随模型落盘、新增 `score_latest` / `backtest_model` | CV 未来样本 0；OOS Sharpe 2.851；落盘/加载/打分/元数据全通过 |
| `修复补丁/闭环接入补丁.py` | 合并用代码片段：`routers/ml.py` 新接口、jobs 异步扩展、IDOR 修复、缓存新鲜度、前端 MLView 按钮与超时处理 | 按注释位置合并即可 |

> 应用顺序建议：先替换 `numpy_factors.py`（立刻解封 ML 训练和 MACD 回测）→ 替换 `ml.py` → 合并 `闭环接入补丁.py` 的 A/B/C/D/E 各段 → 重启后端。

### 6.2 分阶段路线

- **第 1 周（P0/P1 止血）**：应用 3 个补丁文件；修复 IS/OOS 窗口、IDOR、缓存新鲜度；前端接 jobs 异步。
- **第 2-3 周（闭环与口径）**：策略 kind 统一 + 页面跳转；成本/基准/T+1 口径；uf 因子接入选股；组合优化与持仓联动。
- **第 4 周+（对标增强）**：财务/资金流因子入库；风险归因时序化；测试与日志体系；交易日历。

---

## 附录 · 审查方法与复现

- 全文通读：`backend/app/{main,db,adapters,factors,numpy_factors,ml,optimize,portfolio_opt,risk,intraday,scheduler,auth,jobs}.py` + `routers/` 全部 16 个路由 + `frontend/src/views/` 全部 14 个视图。
- 实证脚本（已按项目要求清理）：合成 K 线 250 根验证因子数值；3 股 × 100 日验证 CV 切分；60 股 × 120 日 × 21 因子合成数据集验证 ML 全流程（训练→落盘→加载→打分）。
- 竞品信息：各平台官网及 2026 年公开资料整理。
