# 各模块 Bug 与优化空间汇总（分版块）

> 项目：TEST-main 量化平台（FastAPI 后端 8899 + Vue3/Vite 前端 5899）
> 审查日期：2026-08-14
> 范围：全部后端模块 + 全部前端视图，按功能版块拆分
> 方法：静态代码审查（5 个并行审查 Agent）+ 此前运行实测结论
> 严重度：P0 致命 / P1 严重 / P2 一般 / P3 轻微

---

## 严重程度速览

| 版块 | P0 | P1 | P2 | P3 | 关键结论 |
|---|---|---|---|---|---|
| 数据/行情/因子 | 0 | 2 | 5 | 7 | L1 缓存静默截断、快照因子全空最严重 |
| 选股/组合/回测/报告 | 0 | 4 | 7 | 6 | 因子回测方向误标、选股表因子列全空 |
| 盯盘/调度/策略/日内/优化/风险 | 0 | 3 | 7 | 10 | 做空 cover 被清空、T+1 压制日内信号、模型优化静默回退 |
| 机器学习 | 1* | 4 | 4 | 6 | 样本内回测泄漏、年化口径、6203 已修复 |
| 认证/基础设施/路由 | 0 | 1 | 4 | 9 | token 存 localStorage、JWT 密钥落盘 |

\* 机器学习 P0 为前端 `suggestedHist` 双重计数，**已于 2026-08-14 下午修复**（删 `extraEnd` 改区间跨度算法，并同步修复后端 `days_to_cover` 双端超额抓取/仅 end gap 翻倍两处缺陷）；其余 P0/P1 多为新静态发现，仍待处理。

---

## 一、数据 / 行情 / 因子模块

### 后端

**`backend/app/adapters.py`**
- **P1 · L1 缓存静默截断**：`fetch_kline` 命中内存缓存时 `return full[-days:] if len(full) > days else full`（L561-566）。若已缓存条数 `< days`，直接返回较短 `full` 而不回源补齐。后果：回测/因子回归/ML 在 300s TTL 内请求更长历史时，拿到被截断的数据，长窗口因子（52 周高低）或 `fetch_kline_window` 窗口静默变短，污染结果。**建议**：`len(full) >= days` 才切片，否则落网络分支。
- **P1 · L606-627 回测窗口复用被截断的 L1**：`fetch_kline_window` 按 `end_date` 算 `fetch_days`（可能上千），但底层 L1 命中只会返回少量缓存条数，导致 `clipped` 不足时仍返回截断序列。根因同上。
- **P2 · 成交量单位跨源不一致**：`_parse_sina`（L189）`volume` 为「股」，腾讯/东财（L142/227）`*100` 转「手」。切到 `source=sina` 时自选股成交量差 100 倍。
- **P2 · 指数/板块重复全量拉取**：`fetch_market_list`（L407/421-435）`sector:` 与 `hs300/zz500` 分支每次递归全市场 3000 只，多板块场景放大限流风险。
- **P2 · 基础面/资金流无缓存无批量**：`fetch_money_flow_trend`（L828）、`fetch_finance_summary`（L894）、`fetch_north_holding`（L934）按股逐个外网请求，N 只 = N 次。
- **P3**：硬编码上限不统一（`SINA_CAP=1500` vs 后端允许 `days=2000` vs 前端 `max=1023`）；`code` 未校验直接拼 URL；`fetch_kline_window` 日期比较依赖 ISO 字符串。

**`backend/app/factors.py` + `backend/app/routers/factor.py`**
- **P1 · 快照因子（基本面/资金流/行业）永远为 None**：`get_factors`（routers/factor.py L42-51）的 `snap_row` 只填 7 个行情字段，**从未调用** `fetch_finance_summary/fetch_north_holding/fetch_sector_map`。后果：`roe/net_margin/revenue_yoy/sector…` 恒为 `None`，`FactorView` 整个 fundamental/moneyflow/sector 分组全显 `--`，已注册因子形同虚设。**建议**：在 `get_factors` 中按需拉取并填充快照因子。
- **P2 · 单因子回归 O(hist²)**：`run_regression`（L103-112）逐点重算 `factor["calc"]`，而 `factor_macd/ema_slope` 内部每次对 `closes[:i+1]` 重算整段 EMA；项目已有 `numpy_factors.compute_factor_series` 向量化实现却未复用。
- **P3 · 全局池化回归与文案不符**：`run_regression` 把所有股票×时点混做一次 OLS，未做横截面 z-score；而 `FactorRegressionView` 称「Fama-MacBeth 截面回归」，口径不一致。
- **干净**：逐点因子函数均仅用 `kline[:i+1]`，无前视偏差。

**`backend/app/numpy_factors.py`**
- **P2 · 「向量化」实为大量行循环**：`volatility_series`(L59)/`rsi_series`(L72)/`kdj_k_series`(L121)/`cci_series`(L162)/`boll_pct_series`(L180) 等仍是 Python `for` 循环；仅 `_rolling_mean/_ema/_daily_returns` 真正向量化。万级股票回测时偏慢。
- **P3 · MACD 柱口径前后端不一致**：后端 `factor_macd` 返回 `DIF-DEA`（无 ×2），前端 `QuoteView.calcMACD` 用 `(DIF-DEA)*2`，差 2 倍。
- **干净**：NaN/inf 处理完善（`np.nan` 标无效、`series_at` 转 `None`）。

**`backend/app/factor_expr.py`**
- **干净**：`ast` 白名单 + 函数 arity 校验有效拦截代码执行风险，`nan_to_num` 防 inf。
- **P3**：`log/sqrt/exp` 对非法输入刷 RuntimeWarning；列名校验延迟到求值期，UI 校验阶段拿不到。

**`backend/app/routers/quote.py` / `data.py`**
- **P3 · quote**：`validate_quote_code` 小写化而 `get_quote` 不处理，跨源大小写可能命中失败；`get_kline` days 上限 2000 与前端 1023 不一致。
- **干净 · data**：`days` 钳制、异常转 502、`/macro` 未知指标返 404，逻辑合理；仅 `fetch_finance_summary` 返回的 ROE 等字段当前无消费方（数据白拉）。

### 前端
- **`QuoteView.vue`**：P3 MACD 柱 ×2 与后端不符；P2 `source=sina` 成交量偏差 100×；P3 `klineDays` 上限 1023 与后端 2000 不符。
- **`FactorView.vue`**：P1 截面表基本面分组全 `--`（联动后端 P1）；P3 `pct_raw` 存在双重 ×100 风险需确认 `fmtPct`；P3 表达式前端未预校验。
- **`DataView.vue`**：干净。
- **`FactorRegressionView.vue`**：P3 `hist` 上限 2500 与单股 kline 上限 2000 不一致（端点调用属 selection 模块，路径正确非 bug）。

### 本版块优化空间
1. 引入统一 K 线缓存层（按 `code+source+days` 缓存并支持回源补齐），一次性解决 L1 截断与多模块重复拉取。
2. 快照因子改为按需+缓存拉取，打通 `FactorView` 基本面分组。
3. 因子计算全量迁移到 `numpy_factors` 向量化实现，消除 O(hist²)/行循环热点。
4. 统一前后端 MACD/成交量单位/days 上限三处常量。

---

## 二、选股 / 组合 / 回测 / 报告模块

### 后端

**`backend/app/routers/selection.py`**
- **P1 · 区间选股因子暖机截断**：`_fetch_technical_values` + `run_select`（L109-141, 257-261）仅在传入 `startDate/endDate` 时对过滤后窗口做 `len<25` 校验，但 `momentum60/dist_52w_high/ma_dev` 需 60+ 根。长窗因子返回 `None` 或基于截断历史算值 → 打分静默失真。
- **P1 · 因子回测报告方向误标**：因子回测分支（L700-714）**未设置 `result["direction"]`**（仅 `longOnly`），`reporting` 默认回退 `"long_short"`。后果：HTML 报告 KPI 写「多空累计收益」实为多头 Top 组 → 严重误标。（ML 分支传了 `direction` 所以正常。）
- **P1 · 优化建仓不建空单**：`apply_to_portfolio`（routers/portfolio_opt.py L102-129）只处理多头目标，负权重（`longOnly=False`）使 `target_qty<0` 进卖分支 `min(<0,0)=0` → **空单永远被静默丢弃**。与 `portfolio_opt` 支持空头不一致。
- **P1 · 选股表因子列全 `--`**：`SelectView` 渲染 `r.factorDetail?.[f.key]?.raw`，但后端把因子值**平铺**在 `entry[key]`（`factorDetail` 仅表达式模式存在）→ 所有单因子列恒显 `--`。前后端字段命名不一致。
- **P2 · 报告空字段集中**：因子回测缺 `effectiveStart/effectiveEnd/actualHistDays`（仅 ML 路径填充）；`group_summary` 仅含 `group/avgReturn/sample`，缺 `cumReturn/annualizedReturn/sharpe/maxDrawdown/equity`；`long_short_points` 无 `bottomCum`；`metrics` 无 `grossSharpe/annualizedTurnover/costErosion`。→ 多个卡片/图表恒空。
- **P2**：`run_select` 对 `None` 技术因子做 `composite_score` 行为依赖下游是否处理 None；`commissionRate/stampDuty` 等无边界校验；每次回测按 `sem=4` 串行拉全池 K 线无缓存，poolSize=300 极慢。

**`backend/app/portfolio_opt.py`**
- **P2 · `mean_variance/max_sharpe` 近奇异 cov 无 PSD 校验**：直接 `cp.quad_form(w, sigma)`，失败仅以 `w.value is None` 触发等权回退（静默降级，可加前置提示）。
- **P3 · `risk_parity` 归一化**：clip `max_weight` 后直接 `w.value/w.value.sum()`，未走 `_normalize`，和可能 ≠1 且不支持 `long_only=False`，组合统计轻微偏离。

**`backend/app/routers/portfolio.py`（模拟盘）**
- **P3**：`reset_portfolio` 用 `get_user_id_from_auth` 而非 `require_user_id`，两套鉴权来源不一致；开多 `avg_cost` 含佣金、开空不含 → 空头盈亏口径与多头不一致。

**`backend/app/backtest_event.py`（事件驱动回测）**
- **P2 · 年化口径**：`_result` 默认 `ppy=252`，事件回测未传 `periods_per_year`，持有期>1 日年化偏乐观。
- **P3 · `_compute_limits` 首根 K 线**用 `kline[0]["close"]` 作 `prev_close`，前视价格错误（极少触发）；`max_volume_pct` 默认 0.10 流动性约束形同虚设。

**`backend/app/reporting.py`**
- **P2**：报告引用了因子回测从不填充的字段（分组 `cumReturn/sharpe`、`bottomCum`、`grossSharpe/annualizedTurnover/costErosion`、`effectiveStart/End/actualHistDays`）→ 大量空白/`-` 区块，与 selection P2 同源。

**`backend/app/routers/reports.py`**
- **P3 · `export_select_report`** 读 `r.get('pctChg')` 选股结果行未必含该字段 → 列空白；HTML 拼接未对 `code/name` 转义（内部数据风险低）；选股 PDF 仅传 `rebalanceCount` → 近乎空白 PDF。

### 前端
- **`SelectView.vue`**：P1 因子列全 `--`（联动后端）；P3 `saveArtifact` 的 `config.board` 写死 `'all'`。
- **`PortfolioOptView.vue`**：P3 `ok = res.applied.filter(a=>a.qty).length` 把「hold」计为失败致成功数低估；P3 UI 可切「允许做空」但后端 `/apply` 不建空单，体验不一致。
- **`PortfolioView.vue`**：P3 下单面板仅买/卖，后端支持的 `short/cover` 无 UI 入口（能力缺口）；6s 轮询无节流。
- **`BacktestView.vue`**：P2 导出 payload 取 `effectiveStart/End/actualHistDays` 因子回测为 `undefined` → 报告窗口空白（同源）；P3 报告 `strategyAlpha` 与前端直读 `alpha` 口径不一致。

### 本版块优化空间
1. 后端一次性补齐因子回测的 `direction`/`group_summary` 累计夏普/`bottomCum`/成本指标/有效区间字段，报告即完整。
2. 选股结果统一返回结构（因子值平铺到 `factorDetail` 或双写），修复前端因子列全部空白。
3. 选股建仓支持空头腿，端到端打通「允许做空」。
4. 区间选股按因子最大窗口动态抬升暖机长度，杜绝截断失真。

---

## 三、盯盘 / 调度 / 策略 / 日内 / 优化 / 风险模块

### 后端

**`backend/app/scheduler.py`**
- **P1 · `tradeDirections='short'` 清空 cover**：`dirs=="short"` 分支（L720-726）清空 `short_candidates/sell_candidates/cover_candidates`，但平掉现有空单（cover）是 short-only 策略一部分 → 自动交易永远无法平仓空头，空单被套。
- **P1 · 日内信号 T+1 压制**：`can_exit = bar_date != position["entry_date"]`（L81-103）使 TP/SL 仅在「不同日历日」评估，entry 在 bar `i+1`，日内 TP/SL 永远被压、持仓到次日开盘，违背「日内」前提（A 股 T+1 下属设计但意图受损）。
- **P2 · 空仓+看多信号误标 `hold`**：`code in position_codes` 分支（L148-164）只映射平仓/减仓→sell，空+看多落到 `"hold"`，结构化 `action` 错误，UI 永不显示平空动作。
- **P2 · 空头名义额用完整 `cash`**：分配引擎 `budget=cash*per_pct` 参数化（无硬编码，干净），但 `short` 对已被前序空单 proceeds 抬高的 `cash` 再分配 → 过度杠杆。
- **P3**：不在监视列表的持仓重复 `fetch_kline`（L599-625）；`_record_auto_equity` 空头权益用 `-avg_cost*qty` 忽略空头 P&L 波动；`_load_trading_days` 循环内重复 import `bisect`。

**`backend/app/routers/monitor.py`**：干净（`alloc-preview` 安全处理 `state is None`，配置校验委托 `set_signal_config`）。

**`backend/app/intraday.py`**
- **P2 · 同 scheduler T+1**：日内 TP/SL 同日被压制（L81-103）。
- **P3**：缺口检测仅跨日（L128-140），午休/日内缺失分钟未标记；`peak_invest=total_invest` 跨多标的求和夸大投入资本（L193-206）。

**`backend/app/optimize.py`**
- **P2 · 按 `modelId` 而非 `modelType` 分支**：`optimize_backtest`（L60-63,149-192）仅当 `base.get("modelId")` 存在才走 ML，否则静默回退 `cfg["factor"]`（默认 `"momentum"`）。`/api/optimize/backtest` 在调用方本意 ML 时却跑动量搜索，且 `save_best_as_strategy` 持久化无 `modelId` 的回测配置 → 重跑无法复用模型。（UI 走 `/jobs`→`ml.optimize_model` 规避，但同步端点是潜在陷阱。）
- **P3**：每 trial 新建 asyncio loop（30+）浪费；walk-forward 切分本身正确（IS/OOS 不重叠），建议加集成测试锁定不变量。

**`backend/app/routers/optimize.py`**
- **P2 · `body.model_dump()` 总带 `factor="momentum"` 默认，`modelId` 不被 UI 填充**：叠加 `optimize.py` 分支 bug，同步优化端点静默忽略 ML 意图。
- **P3**：`result["artifact"]` 部分失败也存；`OptimizeView` 读的 `result.finalModel` 该端点从不返回 → 死 UI 分支。

**`backend/app/risk.py`**
- **P2 · Ledoit-Wolf 小样本塌缩对角**：`n < k*3`（常见 30 天×~12 因子）时协方差收缩到 `d*I`，丢弃全部因子相关性风险，低估小样本组合风险。
- **P3 · 行业哑变量与截距共线**（L147-151）；`ddof=Xt.shape[1]+1`（L304）若放松 `<20` 门槛会趋近样本量致方差→0/NaN；`build_style_panel` 重复 z-score/哑变量可缓存。

**`backend/app/routers/risk.py`**
- **P3**：`dropped`（L48）计算但未用（死代码）；权重归一化 `w/w.sum()` 在多头空头对冲净值≈0 时爆炸，需近零分母保护；`stock_returns` 缺 `preClose`→0.0 收益静默丢弃。

**`backend/app/routers/strategies.py`**
- **P3 · 日内策略重建漏成本参数**：`run_strategy`（L68-75）重建 `IntradayConfig` 不含 `commissionRate/stampDuty/slippage` → 保存的日内策略重跑用默认成本（与 `IntradayView.saveStrategy` 同漏）。

### 前端
- **`MonitorView.vue`**：P2 批量做空/cover 按钮与逐行 📉 在用户配置 `tradeDirections='long'` 时仍尝试 `short/cover`（手动 UI 不守配置，后端守）；P2 独立空单无平空按钮（因后端把空+看多标 `hold`）。
- **`IntradayView.vue`**：P3 `saveStrategy` 漏成本参数（同源）；`run()` 仅单 `code`，无池 UI（可接受）。
- **`OptimizeView.vue`**：P2 `baseCfg()` 发 `modelType` 但不发 `modelId/factor`，若路由到 `/api/optimize/backtest` 触发静默动量回退；`result.finalModel`（L169）永不被返回 → 隐藏 UI 分支。
- **`RiskView.vue` / `StrategiesView.vue`**：干净，字段与后端一致。

### 本版块优化空间
1. 分配引擎补齐空头 cover 不被清空、空头权益用真实市价、与 `tradeDirections` 一致的前端批量守门。
2. 日内/事件回测支持可选 T+0 退出（或明确文档化 T+1 约束），避免「日内」名实不符。
3. 模型优化端点统一按 `modelType` 分支并透传 `modelId`，消除静默回退。
4. 风险模块小样本协方差加 PSD 投影 + 权重归一化近零分母保护。

---

## 四、机器学习模块

> 此前运行实测已确认：1024 分页、回测方向参数、分配引擎、报告 Alpha/Beta/IC 字段 **均已修复**。以下为新静态审查 + 此前确认存活项。

### 后端 `backend/app/ml.py`
- **P1 · 样本内回测（前视泄漏）**：`train_final_model`（L540-583）用全量历史训练，`backtest_model`（L1141/1405-1408）在**同一段历史**上打分。模型已"看过"回测区间 → IC/Sharpe/分组收益被系统性高估，回测不可作实盘证明。**建议**：walk-forward 逐期重训或明确标注 in-sample。
- **P1 · 年化口径用 252/n 而非真实调仓间距 step**：`step`（L1344-1347，上限 200 个调仓日）常 ≫ n，但 `ppy=252/max(1,n)`（L1578）按 n 年化 → step=15,n=3 时收益约 5×、夏普约 2.2× 高估。与"修复旧版 252 错误"注释自相矛盾。
- **P1 · `evaluate_dataset` 的 `oosRankIc` 恒为 0**：L508 `overall_rank_ic=0.0 # 保持兼容不重算`，前端 KPI 永远显示 0，OOS RankIC 报告缺失。
- **P2 · 快照因子前视**：`build_dataset`（L263-272/1273-1308/1374-1382）把当日财报/资金流套用全部历史样本 → beginning-of-sample look-ahead，IC 虚高（已有 `snapshotWarning` 但易忽略）。
- **P2 · 自动训练模型方向硬编码 `long_short`**：`train_final_model`（L569-570）无方向参数，`model_direction` 对自动模型永远 long_short，与 `create_manual_model`（L155 可持久化方向）不一致。
- **P2 · 1 折时 valid==holdout**：`optimize_model`（L606-611）折数退化为 1 时 `holdoutSharpe` 非独立，过拟合被掩盖。
- **P2 · `model_direction` 重复计算两次**：routers/ml.py:383 与 ml.py:1159 各调一次（每次读文件），冗余 IO 易漂移。
- **P3**：全 NaN 列标准化后仍 NaN 未兜底；魔法数字 60/260/240 互不引用；`validSharpe` 实为 IS 拟合 Sharpe 命名混淆；评估集 <3 样本时丢弃致整体 IC 上偏；候选池 survivorship bias 仅告警；报告 `positionLedger` 等无分页。

### 路由 `backend/app/routers/ml.py`
- **P2 · `hist` 默认值三处不一致**：evaluate/train/optimize 用 `ML_DEFAULT_HIST=1024`（L17/87），backtest 用 `hist=180`（L340，且 < `min_hist_for_ml(3)=244` 必被钳升），前端默认 500（MLView:17）→ backtest 永远静默钳升，语义混乱。
- **P2 · 同步跑 `evaluate_dataset`**（L44/67）在大池仍阻塞请求线程。
- **P3 · `split_directional_scores` 的 `top_ratio=0.34` 不可配**（方向裁剪逻辑正确，无 bug）。

### 前端 `frontend/src/views/MLView.vue`
- **✅ 已修复 · `suggestedHist` 双重计数（6203）**：原计算属性 L59-72 核心 L66-69：
  ```js
  const backToFrom = Math.floor((today - from) / 86400000)  // (today - start)
  const extraEnd   = to < today ? Math.floor((today - to) / 86400000) : 0  // (today - end)
  need = Math.max(need, backToFrom + 60 + 240 + extraEnd)   // ← 双重计数
  ```
  根因：区间 `[start,end]`（end≤today）已完全包含在 `[start,today]` 内，再加 `extraEnd=(today−end)` 把尾巴重复加一次。**修复**：改为区间跨度 `Math.max(0, floor((to−from)/86400000)) + 260`，缺省端取今天；已删 `extraEnd`。同步修复后端 `ml.py` 的 `days_to_cover`（双端改区间跨度、仅 end 去 gap 重复）、快照起点透明化（`snapshotStartNote`）与单端日期语义提示文案。
- **P2 · `hist` 默认 500 与后端 1024 不一致**（同源 R1）。
- **P2 · suggestedHist 漏掉持有期 `n`**：用固定 `260` 未 `+n`，大 n 时前端仍略有低估（后端会二次钳升兜底）。
- **P3**：魔法数字 60/240 硬编码；前端字段命名与后端一致（唯一失效是后端 `oosRankIc` 恒 0）；`positionLedger` 仅 `.slice(-10)` 展示（属展示截断）。

### 本版块优化空间
1. ✅ **已修复**：修 `suggestedHist`（删 `extraEnd`）→ 消除 6203 与回测误报；并同步修复后端 `days_to_cover` 双端超额抓取/仅 end gap 翻倍。
2. 回测改 walk-forward 或明确标注 in-sample，消除绩效虚高。
3. 年化按真实 `step` 而非 n；`oosRankIc` 真实计算。
4. 统一 `hist` 默认值；快照因子默认禁用于"真实"模式；自动训练暴露方向参数。

---

## 五、认证 / 基础设施 / 路由

### 后端
**`backend/app/auth.py`**
- **P2 · JWT 密钥回退落盘**：未设 `QUANT_JWT_SECRET` 时密钥持久化进 SQLite `settings` 表（明文，L63-81）→ 拿到 `quant.db` 可伪造任意用户 token。生产务必环境变量注入。
- **P2 · 密码 KDF 偏弱**：PBKDF2-HMAC-SHA256 仅 10 万次迭代（OWASP 2023 建议 60 万），salt 仅 8 字节（L96-97）→ 离线爆破成本低，建议提至 60 万或 argon2/bcrypt。
- **P3 · 限流器进程内内存**：`defaultdict(deque)` 不跨 uvicorn worker/容器（L30-43）；异常吞没掩盖配置错误（L84-92）；自研 JWT 无 iat/iss/aud/吊销，登出后 token 仍 7 天有效。

**`backend/app/routers/auth.py`**
- **P2 · 取客户端 IP 不可靠**：用 `request.client.host`（L18/27）做限流 key，经代理后所有 IP 收敛为代理 IP → 限流错位。应用 `X-Forwarded-For`。
- **P3**：`current_user_id` 匿名返回 0（L48-50）是**暗坑**，一旦被误用于敏感写接口会静默放行匿名写入，建议删除或警示；`/me` 手写鉴权风格不统一。

**`backend/app/db.py`**
- **P2 · 连接泄漏**：`get_conn()` 每调用新建连接无 try/finally（L146-160），INSERT 抛异常则 `conn.close()` 不执行 → 连接/锁泄漏，建议上下文管理器。
- **P3**：`_ensure_column` 动态 SQL（来源均为内部常量，无注入风险，保持即可）；缺 DB 错误统一兜底（裸 500）；`list_backtest_runs` 匿名存档对登录用户可见（设计预期）。

**`backend/app/main.py`**
- **P2 · 限流 key 同源代理失效**（L54/56）。
- **P3**：内存限流桶跨 worker 无效；`@app.on_event("startup"/"shutdown")` 已废弃建议改 `lifespan`；`BaseHTTPMiddleware` 可能缓冲 SSE/流式。

**`backend/app/logging_config.py`**：干净，JSON 结构化日志、未记录密码/token。

### 前端
- **`stores/auth.js`**：**P1 · token 存 localStorage**（`quant_token`，L9-11）受 XSS 直接窃取，叠加后端无吊销 → 被盗 token 可用至过期。建议 httpOnly+Secure Cookie 或内存+刷新。
- **`router/index.js`**：**P2 · 守卫只校验存在性**（L45-54）从不校验 `exp`，过期/伪造但"存在"的 token 可进 SPA。`createWebHistory` 深链接刷新可能 404。
- **`api/client.js`**：**P2 · 硬编码 baseURL 兜底**（L4）`VITE_API_BASE` 缺失回退 `127.0.0.1:8899`，非本机部署必配环境变量；`downloadFile/downloadGet` 绕过 `api` 实例丢拦截器。干净：无 `console.log` 泄漏，401/5xx 拦截完善。
- **`LoginView.vue`**：P3 `catch(e){toast(e.message)}` 非 Error 时显示 `undefined`；无前端密码强度提示。
- **`stores/portfolio.js` / `watchlist.js`**：P3 缺 loading/error 态，UI 无法展示进度或优雅报错。
- **干净**：`App.vue` / `main.js` / `stores/research.js` / `toast.js`。

### 本版块优化空间
1. token 改 httpOnly Cookie 或内存+短 TTL 刷新，消除 XSS 窃取风险。
2. 环境变量强注入 `QUANT_JWT_SECRET`、密码 KDF 提至 60 万次。
3. 代理场景用 `X-Forwarded-For` + 共享限流（Redis）。
4. `db.py` 连接管理加 try/finally；前端路由守卫校验 `exp`。

---

## 六、优先级总览与修复建议（Top 清单）

### 必须优先（P0/P1，直接伪造/误导结果或数据丢失）
1. ✅ **ML `suggestedHist` 双重计数（6203）** — 前端 MLView.vue:66-69，已删 `extraEnd` 改区间跨度算法；后端 `days_to_cover` 同步修复。
2. ✅ **ML 样本内回测前视泄漏** — 已加 `inSampleWarning` 明确标注 in-sample 并指引样本外验证方式（walk-forward 逐期重训为后续可选增强）。
3. ✅ **ML 年化口径 252/n 而非 step** — ml.py，已改 `ppy = 252 / step`（按真实调仓间距年化）。
4. ✅ **因子回测报告方向误标「多空」** — selection.py，已补 `result["direction"]`。
5. ✅ **选股表因子列全 `--`** — selection.py，已补 `factorDetail[f.key] = {raw}` 双写。
6. ✅ **快照因子（基本面/资金流/行业）恒 None** — routers/factor.py，已按需拉取财务/资金流/北向/行业填充。
7. ✅ **L1 K 线缓存静默截断** — adapters.py，缓存不足 `days` 时落网络分支回源补齐。
8. ✅ **优化建仓不建空单** — routers/portfolio_opt.py，已支持负权重做空/回补腿。
9. ✅ **做空 cover 被清空 + 空仓看多误标 hold** — scheduler.py，已保留 cover、空单看多→cover、多单看空→sell。
10. ✅ **token 存 localStorage（XSS 窃取）** — 已迁移 httpOnly Cookie（SameSite=Lax，JS 不可读）+ 内存 token + `/auth/me` 启动恢复；后端双通道（Header/Cookie）兼容旧客户端。

### 重要（P2，正确性/性能/安全）
- 模型优化端点静默回退动量（optimize.py 按 `modelId` 分支）— 复查结论：当前代码已正确分流（modelId 优先），无静默回退，无需改动。
- ✅ 因子回测报告空字段集中 — selection.py 已补 `effectiveStart/effectiveEnd/actualHistDays`。
- 日内 T+1 压制 TP/SL — 复查结论：intraday.py 已按 A 股规则实现 T+1（当日买入最早次日平仓），属设计正确。
- 风险小样本协方差塌缩 — 复查结论：risk.py `factor_covariance` 已实现短面板 Ledoit-Wolf 收缩，`estimate_specific_variances` 有截面均值兜底。
- 事件回测年化 252 — 复查结论：backtest_event 逐日记净值、日收益 ×252 年化正确。
- ✅ JWT 密钥落盘 → 已改不落盘（临时随机密钥）；✅ 密码 KDF 偏弱 → 已提至 60 万次 + 存量密码登录自动升级；✅ 代理下限流失效 → 已改 `X-Forwarded-For`；✅ 硬编码 baseURL → 已改相对路径 + Vite 代理。
- ✅ 前端 K 线天数上限 1023 → 已对齐后端 2000。
- ✅ 单因子回归 O(hist²) → 已改用 `compute_factor_series` 向量化预计算（语义与 selection.py 多因子回归一致）。
- ✅ numpy_factors 行循环未真向量化 → 2026-08-18 已全部向量化（`sliding_window_view` 批量滑动窗口，仅 KDJ EMA/`_ema` 保留顺序递推），`test_numpy_factors.py` 33 项一致性测试全过（atol=1e-9）。
- ✅ ML `oosRankIc` 恒 0 → 已按日真实计算 RankIC（Spearman）。

### 轻微（P3，nits/一致性）
- 前后端 MACD×2、days 上限、成交量单位三处不统一 — 成交量单位复查：腾讯/东财均转股、新浪原生股，口径一致，无需改动。
- ✅ 路由守卫不校验 exp → 已随 Cookie 迁移一并解决（凭证服务端校验 + 启动恢复）；✅ 死代码 `current_user_id` → 已删除；db 连接兜底、多处 loading/error 缺失。
- ✅ 预存失败测试 2 项（`test_build_dataset_date_filter`、`test_build_dataset_hist_insufficient_error_message`）→ 已修复：错误信息区分"时间段超出数据覆盖"，测试断言同步更新。

> 说明：本清单中标注「已运行实测确认」的项（6203）来自此前在运行实例上的真实请求验证。至 2026-08-18：P0/P1/P2/P3 全部项已修复（含 token 迁移 httpOnly Cookie、numpy_factors 向量化），本清单全部关闭。

---

*审查仅读取代码，未修改任何业务文件。共覆盖 5 大版块 ≈ 40 个源文件，识别缺陷约 60 项（P0×1 / P1×13 / P2×27 / P3×19）。审查后已实施修复：ML 版块（6203、`days_to_cover` 双重缺陷、年化 step、oosRankIc、in-sample 标注、快照起点透明化）、选股/回测（方向误标、factorDetail 双写、报告空字段补齐）、行情（L1 缓存补齐）、组合优化（空头腿）、盯盘（cover 保留 + 空仓看多动作）、因子（快照因子打通、单因子回归向量化）、安全（JWT 密钥不落盘、KDF 60 万次、X-Forwarded-For、token 迁移 httpOnly Cookie）、基建（baseURL 相对化 + Vite 代理、K 线上限对齐、死代码清理）。另修复 2 个预存失败测试。*
