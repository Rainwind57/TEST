# 应用实跑排查 · 量化主链路（2026-08-18）

> 方法：绕过 HTTP/鉴权，直接用后端模块（`app.routers.selection`、`app.ml`、`app.optimize`）驱动 7 条量化主链路，端到端实跑并捕获异常与异常结果。仅诊断，未改任何业务代码。

## 一、实跑覆盖与结果总表

| # | 链路 | 入参要点 | 结果 | 关键现象 |
|---|------|----------|------|----------|
| 1 | 规则选股 | 动量/波动率/RSI（合法键） | ✅ 通过 | 返回带因子 z 分的候选股，universeSize≈58 |
| 2 | 规则因子回测 | momentum，pool=60，hist=200 | ✅ 通过 | 46 次调仓；cumReturn≈0.78、sharpe≈2.0、maxDD≈-0.42，均有限 |
| 3 | ML 评估（合法 3 键子集） | momentum/volatility/rsi | ✅ 通过 | 特征数=3，nSamples≈11210，oosIc/oosRankIc 正常 |
| 4 | ML 评估（混入非法键） | momentum/volatility/**pe_ttm** | ⚠️ 静默 | 特征数=2，pe_ttm 被悄悄丢弃，无任何告警 |
| 5 | ML 模型选股 | score_latest（训练模型） | ✅ 通过 | 返回 15 只打分排序股 |
| 6 | ML 回测（训练模型） | 训练模型 152244，hist=120 | ✅ 通过 | 70 次调仓，跑满历史；cumReturn/sharpe 有限（in-sample 警告已标注） |
| 7 | ML 回测（人造模型） | manual_20260818_161349，hist=500 | ⚠️ 压缩 | 仅 19 次调仓，起点被压到最近 60 个交易日（已知问题重确认） |
| 8 | 参数寻优 | momentum，nTrials=8 | ⚠️ 存档失败 | 算出 bestParams，但**每个 trial 都报"回测报告自动存档失败"** |

## 二、发现的问题

### 🔴 问题 1（确定性 bug）：参数寻优的回测报告无法落盘历史
- **现象**：跑参数寻优时，控制台每个 trial 重复打印 `回测报告自动存档失败: Error binding parameter 7: type 'Depends' is not supported`（8 次 trial 全部失败）。
- **根因**：规则回测函数（run_backtest）的 `uid` 参数默认值是 `Depends(require_user_id)`（FastAPI 依赖注入对象）。参数寻优内部直接以 `run_backtest(BacktestBody(**cfg))` 调用、未显式传 uid，于是 `uid` 取到了 **Depends 对象本身**；该对象随后被当作 `user_id` 传入回测报告存档函数写库，sqlite 无法绑定 `Depends` 类型 → 报错。寻优每个 trial 都会触发两次（样本内/样本外各一次），故每轮必报。
- **影响**：寻优能正常返回 bestParams/指标，但**对应的回测报告永远写不进历史库**，用户在"回测报告"里看不到这次寻优的明细，可追溯性丧失。错误被静默吞掉（仅日志打印），用户侧无感知。
- **修复方向**：
  - 直接调用处显式传 `uid=0`（`run_backtest(BacktestBody(**cfg), uid=0)`）；
  - 更彻底：把 run_backtest / run_select / run_factor_regression 的 `uid: int = Depends(...)` 默认值改为 `uid: int = 0`，由路由层用 `Depends` 注入，避免程序内调用拿到依赖对象（同类隐患在另两个函数也存在，属同一设计风险）。

### 🟠 问题 2（确定性 bug）：因子子集静默丢弃非法键，无告警
- **现象**：ML 训练/评估传入 `selectedFactors=["momentum","volatility","pe_ttm"]`，实际只产出 2 个特征（momentum、volatility）；`pe_ttm` 被悄悄去掉。换用确属合法的 3 个键（momentum/volatility/rsi）则正确产出 3 个特征。
- **根因**：数据集构建时只对"命中技术因子/快照因子键名"的因子做保留，**不在清单内的键被静默跳过**，既不报错也不提示。而后端真实键名与直观命名不一致（如市盈率键是 `pe` 而非 `pe_ttm`，量比键是 `volume_ratio5` 而非 `volume_rate`），前端若按显示名/习惯名传参就会整键落空。
- **影响**：用户以为勾选的因子生效了，实际被吃掉——这正是此前"勾选后因子仍是 36 个"困惑的同一种根因（默认不传=全量 36；一旦混入了与后端键名不匹配的因子，则静默缩减且毫无提示）。
- **对照不一致**：规则选股路径对未知因子会**显式抛 400**（`未知因子: [...][]`），而 ML 路径却静默丢弃——两条路径对"非法因子"的处理行为相反。
- **修复方向**：构建数据集时校验 selectedFactors，遇到未知键要么显式报错、要么在返回里回显"已忽略 N 个未知因子"，并在训练/评估响应中带上实际生效的特征清单。

### 🟡 问题 3（已知，本次重确认）：人造模型回测被压缩到最近 60 个交易日
- **现象**：人造模型（因子清单被写死成全部因子全集，必然含快照因子）+ hist=500，回测实际只 19 次调仓，起点被自动设为快照生效日（最近 60 个交易日），更早历史截面全部跳过。
- **影响**：无论设多长历史都只回测最后 60 天，与用户预期严重脱节（详见 `应用排查_人造模型跑不了回测_20260818.md`）。

### ⚪ 问题 4（次要/契约不一致，当前不致命）：ML 回测成功不返回 `ok` 字段
- **现象**：ML 回测（含训练模型、人造模型）跑通后，成功返回结果**不含 `ok` 字段**；只有失败路径才置 `ok:false`。
- **现状**：前端判断成功用的是 `btResult.value?.ok === false`（严格等于 false），成功结果（ok 为 undefined）不匹配该条件，故**当前不会被误判为失败**，功能暂不受影响。
- **风险**：契约不对称，属于脆弱设计，任何改为"真值判断"的调用方都会把成功当失败；建议成功路径统一补 `"ok": True`。

## 三、确认正常的部分（无 bug）
- 规则选股（合法因子键）、规则因子回测（指标有限合理）、ML 数据集构建与评估、ML 模型打分选股（score_latest）、ML 回测（训练模型，跑满历史、指标有限）、参数寻优核心计算（能算出 bestParams 与 IS/OOS 指标）——主流程均可跑通，未见崩溃、未见 nan/inf 指标。
- 说明：本轮未复跑"时间长度过长"链路（长窗口数据抓取静默丢弃 + 计算膨胀），其现象与修复方向见 `应用排查_机器学习时间长度过长_20260818.md`。

## 四、探针误报澄清（非应用 bug）
探针初期两次"失败"均为调用方式问题，已澄清、非应用缺陷：
- 规则选股传了 `pe_ttm` / `volume_rate`（均非合法键）→ 被后端显式 400；改用合法键后即通过。
- 参数寻优首个探针直接把 dict 传给 HTTP 路由函数（应调底层模块函数），属测试脚本自身错误；修正后寻优主流程正常，仅暴露上述"存档失败"真 bug。
