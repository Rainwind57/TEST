# 回测日期错配根因与修复建议

日期：2026-08-14
关联文档：《建议历史长度计算缺陷_2026-08-14.md》（训练侧 6203 bug）、《未解决问题清单_2026-08-12.md》

## 1. 现象

用户在 ML 回测面板设置回测时间段（如训练/回测 2017/01/14 ~ 2020/01/14，约三年自然日），但观察到"日期对不上"：

- 页面"建议历史长度"仍显示约 **6203 日**（与训练面板同款错误提示）；
- 历史长度被自动回填为一个离谱大值；
- 回测实际覆盖的日期范围与直觉不符，且回测很慢/易超时。

## 2. 后端已做的时间段适配（部分修复，非根因）

`backend/app/ml.py:1191-1242` 已有逻辑：

- 传 `start_date`/`end_date` 时，重算 `hist` 为 `days_to_cover`（:1197-1207）；
- **K 线抓取跟随终止日**：`if end_date: kline = await _kline_window_retry(code, end_date, hist+n+25)`（:1241-1242），否则从今天往前抓；
- `adapters.fetch_kline_window`（adapters.py:606-627）：窗口终止于 `end_date`，从今天回拉覆盖后裁剪掉 `end_date` 之后、取尾部 `total_days` 条。

**当 start_date 与 end_date 都传时，逻辑基本正确**：能正确回测 2017-2020（窗口跟随 end_date，调仓日过滤落在 [start, end] 内）。所以"后端完全没修"是过时判断，真正问题在以下几处。

## 3. 仍"对不上"的真实根因

### 3.1 主因：前端 `suggestedHist` 的 6203 算法 bug 同样作用于回测面板

`frontend/src/views/MLView.vue:62` 的循环**同时遍历** `[btStart, btEnd]` 和 `[trainStart, trainEnd]`，用同一套错误算法（today−from 与 today−to 两段相加）：

```
设回测 2017/01/14 ~ 2020/01/14：
  backToFrom = (today − 2017/01/14)/86400000 ≈ 3500
  extraEnd   = (today − 2020/01/14)/86400000 ≈ 2403   ← 重复加法
  need       = 3500 + 2403 + 300 = 6203
```

该值经 `watch(suggestedHist)`（:76-78）**自动把 `hist` 填成 6203**，并标红"当前不足"。于是用户在回测面板看到"三年区间被要求 6203 天历史"——这是纯前端算法 bug，与后端实际所需（约 3800 或更少）无关，也直接造成"回测日期对不上"的观感。

### 3.2 隐蔽坑：含快照因子且未设 start_date 时被静默改起点

`ml.py:1283-1284`：

```python
if not start_date and snapshot_cutoff_date:
    start_date = snapshot_cutoff_date   # 最近 60 个交易日
```

若模型含快照因子（财务/资金流：roe、mkt_cap、main_net_pct 等）且用户**未设 start_date**，回测起点被默默改成"最近 60 个交易日"。回测实际窗口变成"最近 60 天起"，与用户预期的"全历史/指定起点"严重不符，且**界面无任何提示**。

### 3.3 单向时间段的语义偏差

- **只设 start_date（不设 end_date）**：抓取从今天往前覆盖（含 today 之后调仓日），过滤只去掉 `< start_date` 的，保留 `start_date ~ 今天` 全部调仓日。用户若只想要某区间，会多出拖到今天的长尾巴。
- **只设 end_date（不设 start_date）**：窗口跟随 end_date 但无下限，回测从数据起点到 end_date（可能 8~10 年），与"只看最近某段"的预期不符。

### 3.4 连带：超额抓取导致慢/超时

前端回填 `hist=6203` 后传给后端，`_kline_window_retry(code, end_date, 6203+25)` → `fetch_kline_window` 内部 `fetch_days = max(6228, 6228+gap) + 60`（adapters.py:622），对 `end_date=2020` 约 8691 天（≈34 年）。每只股票走东财分页拉满 8691 天再裁剪——单票耗时长、易超时，进一步放大"不对劲"的感觉。

## 4. 与既有问题的关系

- 与《建议历史长度计算缺陷_2026-08-14.md》**同源**：前端 `suggestedHist` 算法错误，训练面板与回测面板共用同一段错误代码。
- 与 U1（《未解决问题清单》）关系：后端已部分修复"窗口跟随 end_date"，但**前端提示与自动回填仍错**，且 3.2 的快照因子静默改起点是本次新发现。
- 注意：早期报告中"回测时间段与 hist 完全无关（永远从今天往前）"的判定**已过时**——后端在带 `end_date` 时已修正，但前端 6203 提示 + 自动回填 + 快照静默改起点 仍会造成"对不上"。

## 5. 修复建议（P0）与实施状态

1. ✅ **已修复 · 修 `suggestedHist`（MLView.vue:59-72）**：删除 `extraEnd` 重复加法，改为按所选区间跨度 `(to−from)/86400000 + 260`；训练/回测共用同一正确公式。
2. ✅ **已修复 · 快照因子起点透明化（ml.py）**：未设 `start_date` 且用快照因子时，记日志并回传 `snapshotStartNote`，前端 toast 提示；报告同步渲染。
3. ✅ **已修复 · 单向时间段语义标注**：只传一端时在 UI 显示实际语义（只设 start→回测至今天；只设 end→取最近 hist 天截至该日）。
4. ⚠️ **部分完成 · 前后端口径统一**：后端已能算 `days_to_cover`，前端已改为与后端同口径的区间跨度公式，但仍是前端自算（非直接消费后端返回），留待后续统一。

## 6. 补充发现与追加修复（本次实施）

深究后发现后端 `days_to_cover` 公式存在本文第 3.4 节未识破的双重缺陷，已一并修复（`ml.py` 的 `build_dataset` 与 `backtest_model` 两处）：

- **双端日期超额抓取**：原用 `(today−from)` 计算窗口，但 `fetch_kline_window`（adapters.py:621-622）内部已自行加 `today→to_date` 的 gap，双端场景应只用区间跨度 `(to−from)`。已改：双端=区间跨度、仅 start=至今天、仅 end=基础量。
- **仅 end 日期 gap 翻倍**：原公式 `(today−to)+hist+60+n` 再叠加 `fetch_kline_window` 内部的 gap，gap 被计两次。已改为基础量（窗口终止于 end，hist 表示 end 之前的样本量）。
- **end<start 校验**：新增 `ValueError`，路由层转 422 提示。

> 状态更新（2026-08-14 下午）：上述修复已落地到 `frontend/src/views/MLView.vue`、`backend/app/ml.py`、`backend/app/reporting.py`、`backend/app/routers/reports.py`。
