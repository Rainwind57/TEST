# 功能验证报告：机器学习 & 盯盘/回测

> 验证时间：2026-08-05｜方式：在已启动的应用（后端 8899 / 前端 5899）上**真实调用 API 端到端跑通**，而非静态读码。
> 配套文档：前 5 篇《应用问题清单_*.md》。本文是**功能能否跑通**的实测结论，发现问题同样只列不修。

---

## 0. 一句话结论

**功能本身是“实现好了、能跑”的**，但**当前环境/默认参数下并不可靠**：

- ✅ 因子回测、ML 训练/评估/调参/打分、盯盘开关、扫描逻辑——**代码路径都能跑通**（实测返回 200 + 真实数据）。
- ❌ **回测类功能在数据源一抖动就整体瘫痪**，报的是误导性的“样本不足”，实际是“板块列表拉不到”。
- ❌ **ML 默认参数跑不起来**：默认 `hist=240` 远小于内置长周期因子所需的 250+ 天，开箱即“有效样本不足”。
- ⚠️ 盯盘扫描**只在交易日 15:05/15:10 的 cron 触发**，无法按需手动触发；且配置里残留了一个上一会话的失效 `modelId`，开“模型模式”会静默失败。

---

## 1. 逐项实测结果（功能矩阵）

| 功能 | 实测接口 | 结果 | 关键证据 |
|---|---|---|---|
| 注册/登录 | `POST /auth/register` | ✅ 正常 | 200，返回 token |
| 因子回测 | `POST /select/backtest` | ⚠️ 时好时坏 | 早段 `hist=120` 返回 200（含 IC 序列）；后段同参数返回 422「样本不足」 |
| ML 评估 | `POST /ml/evaluate` | ✅（需 hist≥1023）/ ❌（默认） | `hist=120` → 422「有效样本不足」；`hist=1023` → 200（nSamples=30934） |
| ML 训练 | `POST /ml/train` | ✅（需 hist≥1023） | `hist=1023` → 200（11.8s，落盘 `mlmodel_20260805_153122`） |
| ML 查看参数 | `GET /ml/models/{id}/params` | ✅ 正常 | 200，0.1s（**此前用户反馈的“调参 network error”本次未复现**） |
| ML 人工调参 | `POST /ml/models/{id}/adjust` | ✅ 正常 | 200，0.0s，返回 adjustConfig |
| ML 打分 | `POST /ml/score` | ✅ 正常 | 200，5.3s，返回 30 只打分排名 |
| ML 回测 | `POST /ml/backtest` | ❌ 失败 | 见下「根因 B/C」 |
| 盯盘开关 | `POST /monitor/toggle` | ✅ 正常 | 需带 `{enabled:true}` 体；返回 `enabled:true` |
| 盯盘状态/净值 | `GET /monitor/status`、`/equity` | ✅ 正常 | 200 |
| 盯盘扫描逻辑 | `scheduler._scan_signals()`（直接调用） | ⚠️ 不崩但 0 信号 | “scan completed；signals=0” |

---

## 2. 机器学习（ML）功能详细结论

### ✅ 核心链路可用（证明实现没问题）
在 `hist=1023` 下完整跑通：`train → params → adjust → score` 全部 200，且返回真实结果（样本中 30934 行 × 36 特征，打分能排出前 30 名）。说明**模型训练/调参/打分这条链路本身是好的**。

### ❌ 缺陷 A：默认 `hist` 太小，开箱即失败（真 bug）
- `routers/ml.py:16` `EvalBody.hist` 默认 `240`；`ml.py:87` `build_dataset` 内置因子含 `momentum120 / momentum250 / ma_dev250` 等长周期因子。
- 实测：`hist=120/240` 直接 422「有效样本不足，因子缺失切片 2223 个」；必须手动把 `hist` 调到 **≥1023** 才能成功。
- **影响**：用户用默认参数点“训练/评估”必失败，体感就是“机器学习跑不起来”。
- 修复方向：内置因子按长周期裁剪默认 `hist`（如默认 1024），或按所选因子动态计算最小所需历史。

### ❌ 缺陷 B：ML 回测高并发取数丢票（真 bug）
- `ml.py:802` `backtest_model`：对候选池逐只 `fetch_kline(code, hist+n+25)`（:850），只保留 `len(kl)>=40` 的票（:857）。
- 实测 `hist=1023, pool=30`：报错「候选池 30 只，K线≥40 日的仅 **3** 只」——即 27 只的并发 K 线请求**整体失败/超时**被静默丢弃（:851 `except: return code, []`）。
- 这是此前诊断的“无连接池 + 上游限流”在回测场景的具体表现：并发一高，多数请求被上游掐掉。

### ⚠️ 缺陷 C：上游板块列表抽风 → 候选池直接归零（最严重，但属数据层脆弱性）
- `adapters.py:361` `fetch_market_list` 先试新浪、再降级东财（:396-404）。
- 实测后段：连 `pool=80/120` 的 ML 回测都报「**候选池 0 只**」，因子回测同样 422「样本不足」。
- 根因探测：
  - 单只 K 线（腾讯）正常：`/kline?code=sh600000&days=300` → 200，300 行；
  - **东财 clist 当前 SSL 握手超时**；
  - 新浪 `Market_Center` 的 `json_v2` 单探正常，但 app 用的是 `jsonp.php/x/...` 变体（adapters.py:226），在连续并发请求下被限流返回空。
- **结论**：板块列表上游一旦抖动，整个选股/回测/ML 回测全部“候选池 0”，而报的却是“请增大候选池规模”——**把网络故障伪装成了参数错误**，这是严重的可观测性缺陷。

### 备注：用户此前反馈的“调参 network error”本次未复现
- `params`/`adjust` 在受控测试中 0.0~0.1s 返回 200，**没有 network error**。
- 合理推断原因为间歇性问题：① 某次模型在 lightgbm 版本不一致时 `joblib.load` 拖垮单 worker（routers/ml.py:169 是普通 `def`）；② 或当时恰好撞上上游超时超过前端 5 分钟阈值。需用户在失败瞬间抓 Network 面板状态才能定位，本文不强行归因。

---

## 3. 盯盘（monitor）功能详细结论

### ✅ 开关本身可用
- 后端 `routers/monitor.py:43` `toggle` **要求请求体 `ToggleBody{enabled:bool}`**（我首次测试因未带 body 才 422）。
- 前端 `MonitorView.vue:56` 正确发送 `{enabled:!enabled.value}`，**UI 按钮可用**，实测 `POST /monitor/toggle {"enabled":true}` → `enabled:true`。

### ⚠️ 缺陷 D：扫描只能靠 cron，无法手动触发（设计缺陷）
- `scheduler.py` 把 `_refresh_equity`(15:05)、`_scan_signals`(15:10) 挂成 `CronTrigger(day_of_week="mon-fri")`。
- 没有任何“立即扫描一次”的接口，也没在 `on_startup` 启动（呼应第 1 篇诊断：默认不跑，必须手动 toggle）。
- 实测直接调用 `scheduler._scan_signals()` 不崩、但 `signals=0`：因为目前无持仓/自选，且上游板块列表当时为空，扫不到东西。**“盯盘到底能不能产出信号”无法在任意时刻验证，只能等到交易日 15:10。**

### ⚠️ 缺陷 E：配置残留失效 modelId（潜在静默失败）
- `GET /monitor/status` 返回 `config.mode="model"`, `config.modelId="mlmodel_20260803_164048"`（上一会话留下的）。
- 若用户现在以“模型模式”开启盯盘，而该模型已被删/不存在，`_scan_signals` 加载模型会异常却可能被吞掉 → 默默无信号。
- 修复方向：开启/保存配置时校验 `modelId` 是否仍存在；不存在则提示或回退规则模式。

### ⚠️ 缺陷 F：交易日判断与节假日（已知，呼应第 1 篇）
- `_is_trading_day()` 靠拉 K 线推断，无本地交易日历，调休/节假日可能误触发或漏触发；UI 自身也标注“节假日未做日历过滤”。

---

## 4. 优先级汇总

| 级别 | 问题 | 位置 |
|---|---|---|
| **P0** | 上游板块列表抖动 → 候选池归零，却报“样本不足”（误导性错误，掩盖真实故障） | adapters.py:396-404 / ml.py:858-862 / selection 取数 |
| **P0** | ML 默认 `hist=240` 不足以训练内置长周期因子，开箱即失败 | routers/ml.py:16, ml.py:87 |
| **P1** | ML 回测高并发 K 线请求大量丢弃（无连接池/限流补偿） | ml.py:845-857 |
| **P1** | 盯盘扫描仅 cron 触发，不可手动验证；不随启动自启 | scheduler.py, main.py:on_startup |
| **P2** | 盯盘配置残留失效 modelId 不校验，模型模式会静默失败 | routers/monitor.py:34-40 |
| **P2** | 上游失败无重试/退避/告警，错误透出不友好 | adapters._http_get |

---

## 5. 给用户的直接回答

- **机器学习能正常用吗？** 底层能。但**默认参数下训练必失败**（要手动把历史长度拉到 1000+）；**ML 回测在数据源稍一繁忙就丢票/归零**。调参/打分/评估接口本身是好的。
- **盯盘能正常用吗？** 开关能用；扫描逻辑不崩。但**扫描只在交易日 15:10 自动跑、且当前因上游板块列表抽风扫不到票**，外加配置里残留的失效模型 ID 会在“模型模式”下静默失败。要验证它真的出信号，得等到交易日临近收盘后刷新页面看“盯盘信号”表。
- **回测能正常用吗？** 因子回测代码正确（早段实测出过真实 IC 报告），但**强依赖上游板块列表健康**；一旦新浪/东财限流，立刻全体“样本不足”。

> 以上均为只读验证，未改动任何源码。临时测试脚本（test_functional.py / probe2.py / probe3.py）已清理。
